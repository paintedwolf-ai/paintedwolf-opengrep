rules:
  - id: lisp-bindings
    languages: [scheme]
    severity: ERROR
    message: Source reaches sink
    mode: taint
    pattern-sources:
      - pattern: (source)
        exact: true
    pattern-sinks:
      - pattern: (sink $X)
