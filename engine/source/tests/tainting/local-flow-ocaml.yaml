rules:
  - id: local-flow
    languages: [ocaml]
    severity: ERROR
    message: Local source reaches sink
    mode: taint
    pattern-sources:
      - pattern: source ()
        exact: true
    pattern-sinks:
      - pattern: sink $X
