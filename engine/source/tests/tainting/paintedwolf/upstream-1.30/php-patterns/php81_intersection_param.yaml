{
  "rules": [
    {
      "id": "upstream-php",
      "languages": [
        "php"
      ],
      "severity": "ERROR",
      "message": "Upstream PHP pattern regression",
      "pattern": "function $F(X&Y $P) { ... }\n"
    }
  ]
}
