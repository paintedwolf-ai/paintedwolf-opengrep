{
  "rules": [
    {
      "id": "upstream-php",
      "languages": [
        "php"
      ],
      "severity": "ERROR",
      "message": "Upstream PHP pattern regression",
      "pattern": "$X = \"a\\\nb\";\n"
    }
  ]
}
