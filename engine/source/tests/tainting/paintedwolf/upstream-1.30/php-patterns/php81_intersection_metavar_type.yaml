{
  "rules": [
    {
      "id": "upstream-php",
      "languages": [
        "php"
      ],
      "severity": "ERROR",
      "message": "Upstream PHP pattern regression",
      "pattern": "class $C { public A&$T $P; }\n"
    }
  ]
}
