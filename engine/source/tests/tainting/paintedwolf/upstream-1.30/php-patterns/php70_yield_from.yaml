{
  "rules": [
    {
      "id": "upstream-php",
      "languages": [
        "php"
      ],
      "severity": "ERROR",
      "message": "Upstream PHP pattern regression",
      "pattern": "yield from $X\n"
    }
  ]
}
