rules:
  - id: alias-flow
    languages: [javascript, typescript]
    message: Aliased object value reaches a sink
    severity: ERROR
    mode: taint
    pattern-sources:
      - pattern: source()
        exact: true
    pattern-sinks:
      - patterns:
          - pattern: sink($VALUE)
          - focus-metavariable: $VALUE
