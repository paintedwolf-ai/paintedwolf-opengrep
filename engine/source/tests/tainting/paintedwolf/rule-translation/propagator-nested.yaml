{
  "rules": [
    {
      "id": "labels",
      "languages": [
        "javascript"
      ],
      "message": "Taint label translation",
      "severity": "ERROR",
      "mode": "taint",
      "pattern-sources": [
        {
          "pattern": "source()",
          "label": "A"
        }
      ],
      "pattern-sinks": [
        {
          "pattern": "sink($X)",
          "requires": "CHECK"
        }
      ],
      "pattern-propagators": [
        {
          "pattern": "copy($FROM, $TO)",
          "from": "$FROM",
          "to": "$TO",
          "label": "CHECK",
          "requires": "(B or A) and not (B and A)"
        }
      ]
    }
  ]
}
