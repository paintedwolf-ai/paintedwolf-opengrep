{
  "rules": [
    {
      "id": "flow",
      "languages": [
        "javascript"
      ],
      "severity": "ERROR",
      "message": "A constructed model reaches its typed sink",
      "mode": "taint",
      "options": {
        "taint_intrafile": false
      },
      "pattern-sources": [
        {
          "pattern": "new Model(...)",
          "exact": true,
          "model-type": "example.Model"
        }
      ],
      "pattern-sinks": [
        {
          "patterns": [
            {
              "pattern": "sink($VALUE)"
            },
            {
              "focus-metavariable": "$VALUE"
            }
          ],
          "requires-model-type": {
            "metavariable": "$VALUE",
            "type": "example.Model"
          }
        }
      ]
    }
  ]
}
