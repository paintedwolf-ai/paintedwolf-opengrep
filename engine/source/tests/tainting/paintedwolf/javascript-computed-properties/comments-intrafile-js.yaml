rules:
  - id: computed-properties
    languages: [javascript]
    mode: taint
    severity: ERROR
    message: Tainted value reaches sink
    options:
      taint_intrafile: true
    pattern-sources:
      - pattern: source(...)
    pattern-sinks:
      - pattern: sink(...)
