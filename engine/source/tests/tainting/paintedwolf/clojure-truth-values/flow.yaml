rules:
  - id: flow
    languages: [clojure]
    message: Input reaches a sink
    severity: ERROR
    mode: taint
    pattern-sources:
      - pattern: (source)
        exact: true
    pattern-sinks:
      - patterns:
          - pattern: (sink $VALUE)
          - focus-metavariable: $VALUE
