rules:
  - id: format-flow
    languages: [rust]
    message: Value reaches sink through formatting
    severity: ERROR
    mode: taint
    pattern-sources:
      - pattern: source()
        exact: true
    pattern-sinks:
      - patterns:
          - pattern: sink($VALUE)
          - focus-metavariable: $VALUE
