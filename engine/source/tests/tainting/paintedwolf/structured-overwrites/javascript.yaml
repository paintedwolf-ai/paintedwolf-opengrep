rules:
  - id: flow
    languages: [javascript]
    message: Input reaches a sink through assignment effects
    severity: ERROR
    mode: taint
    pattern-sources:
      - pattern: source()
        exact: true
    pattern-sinks:
      - patterns:
          - pattern: sink($VALUE)
          - focus-metavariable: $VALUE
