rules:
  - id: flow
    languages: [groovy]
    message: Request input reaches a command
    severity: ERROR
    mode: taint
    pattern-sources:
      - pattern: params.INPUT
        exact: true
    pattern-sinks:
      - patterns:
          - pattern: sh($VALUE)
          - focus-metavariable: $VALUE
