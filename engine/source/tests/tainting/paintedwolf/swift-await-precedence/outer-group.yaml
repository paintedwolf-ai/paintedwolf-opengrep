{
  "rules": [
    {
      "id": "flow",
      "languages": [
        "swift"
      ],
      "severity": "ERROR",
      "message": "Postfix effect boundary",
      "pattern": "$DB.raw($X).all()"
    }
  ]
}
