{
  "rules": [
    {
      "id": "raw-interpolation",
      "languages": [
        "swift"
      ],
      "severity": "ERROR",
      "message": "Raw interpolation label",
      "pattern": "sink(\"...\\(raw: $VALUE)...\")"
    }
  ]
}
