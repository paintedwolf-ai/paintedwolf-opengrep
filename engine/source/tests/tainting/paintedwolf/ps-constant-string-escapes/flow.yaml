{
  "rules": [
    {
      "id": "flow",
      "languages": [
        "powershell"
      ],
      "severity": "ERROR",
      "message": "Native string semantics",
      "pattern": "Write-Output \"it's literal\""
    }
  ]
}
