rules:
  - id: lisp-bindings
    languages: [lisp]
    severity: ERROR
    message: Source reaches sink
    mode: taint
    pattern-sources:
      - pattern: (source)
        exact: true
    pattern-sinks:
      - pattern: (sink $X)
