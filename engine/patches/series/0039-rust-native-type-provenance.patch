--- a/libs/ast_generic/IdFlags.ml
+++ b/libs/ast_generic/IdFlags.ml
@@ -41,3 +41,7 @@
   let flags = if final then set_final flags else flags in
   let flags = if static then set_static flags else flags in
   flags
+
+let native_type_mask = 0x10
+let is_native_type, set_native_type = make_flag native_type_mask
+let clear_native_type flags = Int.logand flags (Int.lognot native_type_mask)
--- a/libs/ast_generic/IdFlags.mli
+++ b/libs/ast_generic/IdFlags.mli
@@ -60,3 +60,8 @@
 
 val to_int : t -> int
 (** Get the underlying representation of a set of flags. *)
+
+val is_native_type : t -> bool
+val set_native_type : t -> t
+val clear_native_type : t -> t
+(** Native type identity established by the language's type resolver. *)
--- a/src/naming/Naming_AST.ml
+++ b/src/naming/Naming_AST.ml
@@ -1364,5 +1364,7 @@
   let visitor = new resolve_visitor env lang
   in
   visitor#visit_program () prog;
-  if Lang.is_js lang then Javascript_bindings.resolve prog else prog
+  if Lang.is_js lang then Javascript_bindings.resolve prog
+  else if lang =*= Lang.Rust then Rust_type_names.resolve_program prog
+  else prog
 [@@profiling]
--- a/languages/rust/generic/Parse_rust_tree_sitter.ml
+++ b/languages/rust/generic/Parse_rust_tree_sitter.ml
@@ -2747,8 +2747,7 @@
             Some (Left n))
     | None -> None
   in
-  (* TODO: QTop *)
-  let _colons = token env v2 (* "::" *) in
+  let colons = token env v2 (* "::" *) in
   let last_id =
     match v3 with
     | `Id tok
@@ -2756,18 +2755,18 @@
         ident env tok
   in
   (* pattern (r#)?[a-zA-Zα-ωΑ-Ωµ_][a-zA-Zα-ωΑ-Ωµ\d_]* *)
-  (* TODO: use either_opt *)
   match prefix_info with
   | Some (Left name) -> H2.add_suffix_to_name last_id name
   | Some (Right (l, ty, r)) ->
       H2.add_type_args_to_name (H2.name_of_id last_id) (l, [ ty ], r)
-  | None -> H2.name_of_id last_id
+  | None ->
+      G.IdQualified {name_last = (last_id, None); name_middle = None;
+        name_top = Some colons; name_info = G.empty_id_info ()}
 
 and map_scoped_type_identifier_name (env : env)
     ((v1, v2, v3) : CST.scoped_type_identifier) : G.name =
-  (* TODO: QTop *)
-  let _colons = token env v2 (* "::" *) in
-  let _either_optTODO =
+  let colons = token env v2 (* "::" *) in
+  let prefix_info =
     match v1 with
     | Some x -> (
         match x with
@@ -2782,7 +2781,12 @@
   in
   let ident = ident env v3 in
   (* pattern (r#)?[a-zA-Zα-ωΑ-Ωµ_][a-zA-Zα-ωΑ-Ωµ\d_]* *)
-  H2.name_of_id ident
+  match prefix_info with
+  | Some (Left name) -> H2.add_suffix_to_name ident name
+  | Some (Right args) -> H2.add_type_args_to_name (H2.name_of_id ident) args
+  | None ->
+      G.IdQualified {name_last = (ident, None); name_middle = None;
+        name_top = Some colons; name_info = G.empty_id_info ()}
 
 and map_scoped_type_identifier_in_expression_position (env : env)
     ((v1, v2, v3) : CST.scoped_type_identifier_in_expression_position) : G.name
@@ -2937,10 +2941,6 @@
       let name = map_generic_type_name env x in
       G.TyN name |> G.t
   | `Scoped_type_id x ->
-      (*TODO Record the fully-qualified name.
-        We only record `B` for `A::B` right now.
-        We have examples of this sort of pattern
-        in tests/parsing/rust/types.rs *)
       let n = map_scoped_type_identifier_name env x in
       G.TyN n |> G.t
   | `Tuple_type x -> map_tuple_type env x
@@ -3195,10 +3195,10 @@
     G.stmt list =
   match x with
   (* was moved out of declaration_statement in source_file or item by ruin *)
-  | `Inner_attr_item v1 ->
-      (* TODO: Somehow map this to the containing StmtDef *)
-      let _xTODO = map_inner_attribute_item env v1 in
-      []
+  | `Inner_attr_item ((hash, _, _, _, _) as item) ->
+      let attribute = map_inner_attribute_item env item in
+      [G.DirectiveStmt (G.OtherDirective (("rust_inner_attribute", token env hash),
+         [G.At attribute]) |> G.d) |> G.s]
   | `Attr_item v1 ->
       (* TODO: Should be unreachable now. *)
       let _xTODO = map_outer_attribute_item env v1 in
