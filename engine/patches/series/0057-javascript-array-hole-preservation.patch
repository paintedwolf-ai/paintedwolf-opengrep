--- a/languages/javascript/ast/ast_js.ml
+++ b/languages/javascript/ast/ast_js.ml
@@ -195,13 +195,8 @@
   | Assign of a_pattern * tok * expr
   (* less: could be transformed in a series of Assign(ObjAccess, ...) *)
   | Obj of a_obj
-  (* we could transform it in an Obj but it can be useful to remember
-   * the difference in further analysis (e.g., in the abstract interpreter).
-   * This can also contain "holes" when the array is used in lhs of an assign
-   * called "elision" which currently are skipped
-   * TODO: have an (expr, elision) Common.either list bracket here.
-   *)
   | Arr of expr list bracket
+  | ArrayHole of tok
   (* ident is None when assigned in module.exports  *)
   | Class of class_definition * a_ident option
   | ObjAccess of expr * dot_operator wrap * property_name
--- a/languages/javascript/ast/visitor_js.ml
+++ b/languages/javascript/ast/visitor_js.ml
@@ -237,6 +237,7 @@
           let t = v_tok t in
           let v1 = v_expr v1 and v2 = v_bracket (v_list v_expr) v2 in
           ()
+      | ArrayHole tok -> v_tok tok
       | Arr v1 ->
           let v1 = v_bracket (v_list v_expr) v1 in
           ()
--- a/languages/javascript/generic/js_to_generic.ml
+++ b/languages/javascript/generic/js_to_generic.ml
@@ -377,6 +377,7 @@
       let e = expr e in
       let args = bracket (list (fun arg -> G.Arg (expr arg))) args in
       G.New (tok, H.expr_to_type e, G.empty_id_info (), args) |> G.e
+  | ArrayHole tok -> G.ArrayHole (info tok) |> G.e
   | Arr v1 ->
       let v1 = bracket (list expr) v1 in
       G.Container (G.Array, v1) |> G.e
--- a/languages/javascript/menhir/parser_js.mly
+++ b/languages/javascript/menhir/parser_js.mly
@@ -686,16 +686,12 @@
 
 (* array destructuring *)
 
-(* TODO use elision below.
- * invent a new Hole category or maybe an array_argument special
- * type like for the (call)argument type.
- *)
 array_binding_pattern:
  | "[" "]"                      { Arr ($1, [], $2) }
  | "[" binding_element_list "]" { Arr ($1, $2, $3) }
 
 binding_start_element:
- | ","                  { [] (* TODO elision *) }
+ | ","                  { [ArrayHole $1] }
  | binding_element ","  { [$1] }
 
 binding_start_list:
@@ -1505,16 +1501,18 @@
 (* array *)
 (*----------------------------*)
 
-(* TODO: use elision below *)
 array_literal:
- | "[" optl(elision) "]"                   { Arr($1, [], $3) }
- | "[" element_list_rev optl(elision) "]"  { Arr($1, List.rev $2, $4) }
+ | "[" optl(elision) "]" { Arr($1, List.map (fun tok -> ArrayHole tok) $2, $3) }
+ | "[" element_list_rev optl(elision) "]" {
+     let holes = match $3 with [] -> [] | _separator :: holes -> holes in
+     Arr($1, List.rev $2 @ List.map (fun tok -> ArrayHole tok) holes, $4)
+   }
 
 (* TODO: conflict on ",", *)
 element_list_rev:
- | optl(elision)   element               { [$2] }
+ | optl(elision)   element               { $2 :: List.rev_map (fun tok -> ArrayHole tok) $1 }
  | element_list_rev "," element          { $3::$1 }
- | element_list_rev "," elision element  { $4::$1 }
+ | element_list_rev "," elision element  { $4 :: List.rev_map (fun tok -> ArrayHole tok) $3 @ $1 }
 
 element:
  | assignment_expr       { $1 }
--- a/languages/typescript/tree-sitter/Parse_typescript_tree_sitter.ml
+++ b/languages/typescript/tree-sitter/Parse_typescript_tree_sitter.ml
@@ -909,11 +909,7 @@
       let elements =
         match v2 with
         | Some (v1, v2) ->
-            map_sep_list env v1 v2 (fun env x ->
-                match x with
-                | Some x -> [ pat_or_assign_pat env x |> sub_pattern ]
-                | None -> [])
-            |> List_.flatten
+            array_elements env (fun x -> pat_or_assign_pat env x |> sub_pattern) v1 v2
         | None -> []
       in
       let close =
@@ -1924,9 +1920,11 @@
   let v2 = module__ env v2 in
   v2
 
-and anon_opt_opt_choice_exp_rep_COMMA_opt_choice_exp_208ebb4 (env : env)
+and anon_opt_opt_choice_exp_rep_COMMA_opt_choice_exp_208ebb4 ?(preserve_holes = false) (env : env)
     (opt : CST.anon_opt_opt_choice_exp_rep_COMMA_opt_choice_exp_208ebb4) =
   match opt with
+  | Some (first, rest) when preserve_holes ->
+      array_elements env (anon_choice_exp_9818c1b env) first rest
   | Some (v1, v2) ->
       let v1 =
         match v1 with
@@ -2933,12 +2931,25 @@
   in
   (basic_entity v3, ClassDef c)
 
+and array_elements : 'a. env -> ('a -> expr) -> 'a option -> (Tree_sitter_run.Token.t * 'a option) list -> expr list =
+ fun env convert first rest ->
+  let rec collect value = function
+    | [] -> Option.to_list (Option.map convert value)
+    | (comma, next) :: rest ->
+        let element = match value with
+          | Some value -> convert value
+          | None -> ArrayHole (token env comma)
+        in
+        element :: collect next rest
+  in
+  collect first rest
+
 and array_ (env : env) ((v1, v2, v3) : CST.array_) =
   let v1 =
     token env v1
     (* "[" *)
   in
-  let v2 = anon_opt_opt_choice_exp_rep_COMMA_opt_choice_exp_208ebb4 env v2 in
+  let v2 = anon_opt_opt_choice_exp_rep_COMMA_opt_choice_exp_208ebb4 ~preserve_holes:true env v2 in
   let v3 =
     token env v3
     (* "]" *)
--- a/libs/ast_generic/AST_generic.ml
+++ b/libs/ast_generic/AST_generic.ml
@@ -680,6 +680,7 @@
    * ArrayInitDesignator [x] = ... use ArrayAccess in container?
    *)
   | Container of container_operator * expr list bracket
+  | ArrayHole of tok
   | Comprehension of container_operator * comprehension bracket
   (* And-type (field.vinit should be a Some) *)
   | Record of field list bracket
--- a/libs/ast_generic/AST_generic_helpers.ml
+++ b/libs/ast_generic/AST_generic_helpers.ml
@@ -203,6 +203,7 @@
   | Container (Tuple, (t1, xs, t2)) ->
       PatTuple (t1, xs |> List_.map expr_to_pattern, t2)
   | L l -> PatLiteral l
+  | ArrayHole tok -> PatWildcard tok
   | Container ((List | Array), (t1, xs, t2)) ->
       PatList (t1, xs |> List_.map expr_to_pattern, t2)
   (* JS/TS rest binding [...rest_target] inside a destructure. Recogniser
--- a/libs/ast_generic/AST_generic_to_v1.ml
+++ b/libs/ast_generic/AST_generic_to_v1.ml
@@ -233,6 +233,7 @@
 
 and map_expr x : B.expr =
   match x.e with
+  | ArrayHole tok -> `OtherExpr (("ArrayHole", map_tok tok), [])
   | N v1 ->
       let v1 = map_name v1 in
       `N v1
--- a/libs/ast_generic/Meta_AST.ml
+++ b/libs/ast_generic/Meta_AST.ml
@@ -297,6 +297,7 @@
 and vof_expr e =
   (* TODO: also dump e_id? *)
   match e.e with
+  | ArrayHole tok -> OCaml.VSum ("ArrayHole", [vof_tok tok])
   | LocalImportAll (v1, v2, v3) ->
       let v1 = vof_module_name v1 in
       let v2 = vof_tok v2 in
--- a/src/analyzing/AST_to_IL.ml
+++ b/src/analyzing/AST_to_IL.ml
@@ -2299,6 +2299,7 @@
       let tok = G.fake "call" in
       call_generic env ~void tok eorig e args
   | G.L lit -> ([], mk_e (Literal lit) eorig)
+  | G.ArrayHole tok -> ([], mk_e (ArrayHole tok) eorig)
   | G.DotAccess ({ e = N (Id (("var", _), _)); _ }, _, FN (Id ((s, t), id_info)))
     when is_hcl env.lang ->
       (* We need to change all uses of a variable, which looks like a DotAccess, to a name which
--- a/src/analyzing/Eval_il_partial.ml
+++ b/src/analyzing/Eval_il_partial.ml
@@ -274,6 +274,7 @@
       (* Fall-back to default evaluation strategy. *)
       | __else__ -> eval_op memo env op args)
   | Operator (op, args) -> eval_op memo env op args
+  | ArrayHole _
   | Composite _
   | RecordOrDict _
   | Cast _
--- a/src/il/Display_IL.ml
+++ b/src/il/Display_IL.ml
@@ -61,6 +61,7 @@
   match e with
   | Fetch l -> string_of_lval l
   | Literal lit -> string_of_literal lit
+  | ArrayHole _ -> "<hole>"
   | Operator ((op, _), [ Unnamed e1; Unnamed e2 ]) ->
       Common.spf "(%s `%s` %s)" (string_of_exp e1) (G.show_operator op)
         (string_of_exp e2)
--- a/src/il/IL.ml
+++ b/src/il/IL.ml
@@ -332,6 +332,7 @@
 and exp_kind =
   | Fetch of lval (* lvalue used in a rvalue context *)
   | Literal of G.literal
+  | ArrayHole of tok
   | Composite of composite_kind * exp list bracket
   (* Records and dictionaries could also be 'Composite's, encoded as lists of
    * tuples, with the first element of the tuple being the field name or key.
--- a/src/il/IL_helpers.ml
+++ b/src/il/IL_helpers.ml
@@ -82,6 +82,7 @@
   | Cast _ -> 4
   | Operator _ -> 5
   | FixmeExp _ -> 6
+  | ArrayHole _ -> 7
 
 let rec compare_exp (e1 : exp) (e2 : exp) : int =
   if phys_equal e1 e2 then 0
@@ -92,6 +93,7 @@
       match (e1.e, e2.e) with
       | Fetch l1, Fetch l2 -> compare_lval l1 l2
       | Literal a, Literal b -> G.compare_literal a b
+      | ArrayHole _, ArrayHole _ -> 0
       | Composite (k1, (_, xs1, _)), Composite (k2, (_, xs2, _)) ->
           let c = compare_composite_kind k1 k2 in
           if c <> 0 then c else List.compare compare_exp xs1 xs2
@@ -183,6 +185,7 @@
             G.hash_operator op,
             List.map (fun a -> hash_exp_fuel f (exp_of_arg a)) args )
     | FixmeExp _ -> 6
+    | ArrayHole _ -> 7
 
 and hash_lval (fuel : int) (lval : lval) : int =
   Stdlib.Hashtbl.hash
@@ -210,6 +213,7 @@
     'a * exp =
   match e.e with
   | Literal _
+  | ArrayHole _
   | Fetch _
   | FixmeExp (_, _, None) ->
       (acc, e)
@@ -319,6 +323,7 @@
   and of_exp_compute (e : IL.exp) : (IL.name * int) list option =
     match e.e with
     | IL.Literal _ -> Some []
+    | IL.ArrayHole _ -> unanchorable
     | IL.Fetch lval -> (
         match lval.base with
         | IL.Var name -> (
@@ -382,7 +387,7 @@
 let rec lvals_of_exp e =
   match e.e with
   | Fetch lval -> lval :: lvals_in_lval lval
-  | Literal _ -> []
+  | Literal _ | ArrayHole _ -> []
   | Cast (_, e) -> lvals_of_exp e
   | Composite (_, (_, xs, _)) -> lvals_of_exps xs
   | Operator (_, xs) -> lvals_of_exps (List_.map exp_of_arg xs)
--- a/src/il/IL_pp.ml
+++ b/src/il/IL_pp.ml
@@ -144,6 +144,7 @@
   match ek with
   | Fetch lv -> pp_lval lv
   | Literal lit -> pp_literal lit
+  | ArrayHole _ -> "<hole>"
   | Composite (ck, (_, exps, _)) -> pp_composite ck exps
   | RecordOrDict fields -> pp_record_or_dict fields
   | Cast (ty, e) -> Printf.sprintf "(%s)%s" (pp_type_ ty) (pp_exp e)
@@ -222,6 +223,7 @@
     match ek with
     | Fetch lv -> go_lval lv
     | Literal lit -> emit (pp_literal lit)
+    | ArrayHole _ -> emit "<hole>"
     | Composite (ck, (_, exps, _)) -> go_composite ck exps
     | RecordOrDict fields -> go_record_or_dict fields
     | Cast (ty, e) ->
--- a/src/matching/Generic_vs_generic.ml
+++ b/src/matching/Generic_vs_generic.ml
@@ -895,6 +895,9 @@
   in
   match (a.G.e, b.G.e) with
   (* the order of the matches matters! take care! *)
+  | G.ArrayHole a, B.ArrayHole b -> m_tok a b
+  | G.Ellipsis _, B.ArrayHole _ -> return ()
+  | _, B.ArrayHole _ | G.ArrayHole _, _ -> fail ()
   (* alias: match on the expr inside *)
   | G.Alias (_alias, a1), _ -> m_expr a1 b
   | _, B.Alias (_alias, b1) -> m_expr a b1
--- a/src/matching/SubAST_generic.ml
+++ b/src/matching/SubAST_generic.ml
@@ -122,6 +122,7 @@
   | N (Id (_, { id_svalue = { contents = Some (Sym e1) }; _ }))
     when with_symbolic_propagation ->
       [ e1 ]
+  | ArrayHole _
   | L _
   | N _
   | IdSpecial _
@@ -255,6 +256,7 @@
   | Seq xs -> xs (* TODO: Take up to N from xs? *)
   | OtherExpr (("ExprBlock", _tk), xs) (* when lang =*= Clojure *) ->
       subexprs_of_any_list xs
+  | ArrayHole _
   | L _
   | N _
   | IdSpecial _
--- a/src/tainting/Dataflow_tainting.ml
+++ b/src/tainting/Dataflow_tainting.ml
@@ -1728,6 +1728,7 @@
         (Taints.empty, S.Scalar R.StringOrNull, env.lval_env)
     | Fetch _
     (* TODO: 'Fetch' is handled specially, this case should not never be taken.  *)
+    | ArrayHole _
     | Literal _
     | FixmeExp (_, _, None) ->
         (Taints.empty, S.Bot, env.lval_env)
--- a/src/tainting/Effect_guard.ml
+++ b/src/tainting/Effect_guard.ml
@@ -109,6 +109,7 @@
  * ([Entry] contributes key then value). *)
 let node_hash (node : IL.exp) (kid_hashes : int list) : int =
   match node.e with
+  | IL.ArrayHole _
   | IL.Literal _
   | IL.Fetch _ ->
       IL_helpers.hash_exp node
--- a/src/tainting/Sig_inst.ml
+++ b/src/tainting/Sig_inst.ml
@@ -832,7 +832,7 @@
         { e with IL.e = IL.RecordOrDict entries }
     | IL.FixmeExp (k, any, Some sub) ->
         { e with IL.e = IL.FixmeExp (k, any, Some (walk sub)) }
-    | IL.Literal _ | IL.FixmeExp (_, _, None) -> e
+    | IL.Literal _ | IL.ArrayHole _ | IL.FixmeExp (_, _, None) -> e
   and walk_arg = function
     | IL.Unnamed sub -> IL.Unnamed (walk sub)
     | IL.Named (id, sub) -> IL.Named (id, walk sub)
