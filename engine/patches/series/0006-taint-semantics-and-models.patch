diff --git a/src/analyzing/AST_to_IL.ml b/src/analyzing/AST_to_IL.ml
index 7f087fe..acc1866 100644
--- a/src/analyzing/AST_to_IL.ml
+++ b/src/analyzing/AST_to_IL.ml
@@ -113,9 +113,33 @@ let todo any_generic : 'a = raise (Fixme (ToDo, any_generic))
 let impossible any_generic : 'a =
   raise (Fixme (Impossible, any_generic))
 
+let translation_gaps : (string * Tok.location option) list ref Domain.DLS.key =
+  Domain.DLS.new_key (fun () -> ref [])
+
+let reset_translation_gaps () = Domain.DLS.get translation_gaps := []
+let take_translation_gaps () =
+  let gaps = Domain.DLS.get translation_gaps in
+  let result = List.rev !gaps in
+  gaps := [];
+  result
+
 let log_fixme kind gany : unit =
   let toks = AST_generic_helpers.ii_of_any gany in
   let tok = Common2.hd_opt toks in
+  let code = match kind with
+    | ToDo -> "unsupported_ast_to_il"
+    | Sgrep_construct -> "pattern_construct_in_target"
+    | Impossible -> "invalid_ast_to_il" in
+  let location = List.find_map (fun tok -> Result.to_option (Tok.loc_of_tok tok)) toks in
+  let code = match gany with
+    | G.TodoK (tag, _) -> code ^ ":" ^ tag
+    | G.E {e = G.OtherExpr ((tag, _), _); _} -> code ^ ":" ^ tag
+    | G.S {s = G.OtherStmt (op, _); _} -> code ^ ":" ^ G.show_other_stmt_operator op
+    | _ -> code in
+  let gap = (code, location) in
+  let gaps = Domain.DLS.get translation_gaps in
+  if List.length !gaps < 256 && not (List.exists (fun other -> other =*= gap) !gaps) then
+    gaps := gap :: !gaps;
   match kind with
   | ToDo ->
       log_warning ?tok
@@ -126,11 +150,11 @@ let log_fixme kind gany : unit =
       log_error ?tok "Impossible happened during AST-to-IL translation"
 
 let fixme_exp ?partial kind gany eorig : exp =
-  log_fixme kind (any_of_orig eorig);
+  log_fixme kind gany;
   { e = FixmeExp (kind, gany, partial); eorig }
 
 let fixme_instr kind gany eorig : instr =
-  log_fixme kind (any_of_orig eorig);
+  log_fixme kind gany;
   { i = FixmeInstr (kind, gany); iorig = eorig }
 
 let fixme_stmt kind gany : stmts =
@@ -149,7 +173,7 @@ let fresh_var ?(str = "_tmp") tok : name =
     if Tok.is_fake tok then tok else Tok.fake_tok tok str
   in
   let i = G.SId.mk () in
-  { ident = (str, tok); sid = i; id_info = G.empty_id_info () }
+  { ident = (str, tok); sid = i; id_info = G.empty_id_info (); value_origin = None }
 
 let fresh_label ?(label = "_label") tok : label =
   let i = G.SId.mk () in
@@ -169,7 +193,7 @@ let var_of_id_info id id_info : name =
         log_debug ~tok:id_tok msg;
         G.SId.unsafe_default
   in
-  { ident = id; sid; id_info }
+  { ident = id; sid; id_info; value_origin = None }
 
 let var_of_name name : name =
   match name with
@@ -219,7 +243,12 @@ let call_instr tok eorig ~void mk_call : stmts * exp =
     ([mk_s (Instr (mk_i (mk_call None) eorig))], mk_unit tok NoOrig)
   else
     let lval = fresh_lval tok in
-    ([mk_s (Instr (mk_i (mk_call (Some lval)) eorig))], mk_e (Fetch lval) NoOrig)
+    let source = match eorig with SameAs expression | Related (G.E expression) -> Some expression | _ -> None in
+    let lval = match lval.base with Var name -> { lval with base = Var { name with value_origin = source } } | _ -> lval in
+    let value_origin = match eorig with
+      | SameAs expression | Related (G.E expression) -> ValueOf expression
+      | _ -> NoOrig in
+    ([mk_s (Instr (mk_i (mk_call (Some lval)) eorig))], mk_e (Fetch lval) value_origin)
 
 let ident_of_entity_opt ent : (G.ident * G.id_info) option =
   match ent.G.name with
@@ -413,8 +442,7 @@ let key_offset_of_pattern (key_pat : G.pattern) : offset option =
           {
             ident = (s, tok);
             sid = G.SId.unsafe_default;
-            id_info = G.empty_id_info ();
-          };
+            id_info = G.empty_id_info (); value_origin = None };
       oorig = NoOrig;
     }
   in
@@ -689,7 +717,7 @@ and pattern env pat : stmts * lval * stmts =
     let tmp = fresh_var tok2 in
     let tmp_lval = lval_of_base (Var tmp) in
     let synth_name ident =
-      { ident; sid = G.SId.unsafe_default; id_info = G.empty_id_info () }
+      { ident; sid = G.SId.unsafe_default; id_info = G.empty_id_info (); value_origin = None }
     in
     let last_ident_of dot_ident =
       match List.rev dot_ident with
@@ -1250,7 +1278,7 @@ and assign_to_record env (tok1, fields, tok2) rhs_exp lhs_orig : stmts * exp =
           {
             ident = xi;
             sid = G.SId.unsafe_default;
-            id_info = G.empty_id_info ();
+            id_info = G.empty_id_info (); value_origin = None;
           }
         in
         let ei = fixme_exp ToDo (G.Fld field) (related_tok tok1) in
@@ -1275,6 +1303,21 @@ and assign_to_record env (tok1, fields, tok2) rhs_exp lhs_orig : stmts * exp =
 and expr_aux env ?(void = false) g_expr : stmts * exp =
   let eorig = SameAs g_expr in
   match g_expr.G.e with
+  | G.OtherExpr (("NamedExpression", _), [G.Name (G.Id (id, info)); G.E expression])
+    when Lang.is_js env.lang ->
+      let statements, value = expr env expression in
+      let target = lval_of_id_info id info in
+      (statements @ [mk_s (Instr (mk_i (Assign (target, value)) eorig))],
+       mk_e (Fetch target) eorig)
+  | G.OtherExpr (("Try", _), [G.E expression]) when env.lang =*= Lang.Swift ->
+      expr env ~void expression
+  | G.OtherExpr (("ArrayAccess[[xs]]", tok), [G.E base; G.Args [G.Arg index]]) when env.lang =*= Lang.R ->
+      expr env ~void {g_expr with G.e = G.ArrayAccess (base, (tok, index, tok))}
+  | G.OtherExpr ((":", tok), [G.E {e = G.N (G.Id (id, _)); _}]) when env.lang =*= Lang.Julia ->
+      ([], mk_e (Literal (G.Atom (tok, id))) eorig)
+  | G.OtherExpr (("Atom", tok), [G.Name name]) when env.lang =*= Lang.Clojure ->
+      let atom = match name with G.Id (id, _) -> id | G.IdQualified qualified -> fst qualified.G.name_last in
+      ([], mk_e (Literal (G.Atom (tok, atom))) eorig)
   | G.Call
       ( { e = G.IdSpecial (G.Op ((G.And | G.Or) as op), tok); _ },
         (_, arg0 :: args, _) )
@@ -1480,7 +1523,7 @@ and expr_aux env ?(void = false) g_expr : stmts * exp =
                 [ G.Name (G.IdQualified {
                     name_last = (s, atom_tok), _;
                     name_middle = Some (G.QDots [ ((prefix, _), _) ]);
-                    _ }) ] ); _ } as callee,
+                    _ }) ] ); _ },
         ( _, [ G.Arg map_expr ], _ ) )
     when env.lang =*= Lang.Clojure
          && (String.equal prefix ":" || String.equal prefix "::") ->
@@ -1488,10 +1531,10 @@ and expr_aux env ?(void = false) g_expr : stmts * exp =
       let field_name =
         { ident = (prefix ^ bare, atom_tok);
           sid = G.SId.unsafe_default;
-          id_info = G.empty_id_info () }
+          id_info = G.empty_id_info (); value_origin = None }
       in
       let ss_map, map_lval = nested_lval env atom_tok map_expr in
-      let offset = { o = Dot field_name; oorig = SameAs callee } in
+      let offset = { o = Dot field_name; oorig = eorig } in
       let field_lval =
         { map_lval with rev_offset = offset :: map_lval.rev_offset }
       in
@@ -1506,7 +1549,7 @@ and expr_aux env ?(void = false) g_expr : stmts * exp =
                 [ G.Name (G.IdQualified {
                     name_last = (s, atom_tok), _;
                     name_middle = Some (G.QDots [ ((prefix, _), _) ]);
-                    _ }) ] ); _ } as callee,
+                    _ }) ] ); _ },
         ( _, [ G.Arg map_expr; G.Arg default_expr ], _ ) )
     when env.lang =*= Lang.Clojure
          && (String.equal prefix ":" || String.equal prefix "::") ->
@@ -1514,10 +1557,10 @@ and expr_aux env ?(void = false) g_expr : stmts * exp =
       let field_name =
         { ident = (prefix ^ bare, atom_tok);
           sid = G.SId.unsafe_default;
-          id_info = G.empty_id_info () }
+          id_info = G.empty_id_info (); value_origin = None }
       in
       let ss_map, map_lval = nested_lval env atom_tok map_expr in
-      let offset = { o = Dot field_name; oorig = SameAs callee } in
+      let offset = { o = Dot field_name; oorig = eorig } in
       let field_lval =
         { map_lval with rev_offset = offset :: map_lval.rev_offset }
       in
@@ -1759,7 +1802,7 @@ and expr_aux env ?(void = false) g_expr : stmts * exp =
       let ss_map, map_lval = nested_lval env key_tok map_expr in
       let field_name =
         { ident = id; sid = G.SId.unsafe_default;
-          id_info = G.empty_id_info () }
+          id_info = G.empty_id_info (); value_origin = None }
       in
       let offset = { o = Dot field_name; oorig = SameAs callee } in
       let field_lval =
@@ -2578,6 +2621,9 @@ and expr_aux env ?(void = false) g_expr : stmts * exp =
       let aux_ss, _, tmp = aux_var ~str env tok other_expr in
       let partial = mk_e (Fetch tmp) (related_tok tok) in
       (ss @ aux_ss, fixme_exp ToDo (G.E g_expr) (related_tok tok) ~partial)
+  | G.RawExpr (Raw_tree.Case ("Quot_lit", _))
+    when List.exists (fun lang -> env.lang =*= lang) [Lang.Lisp; Lang.Scheme; Lang.Clojure] ->
+      ([], mk_e (Literal (G.Unit (G.fake "quoted data"))) eorig)
   | G.RawExpr _ -> todo (G.E g_expr)
 
 and expr env ?void e_gen : stmts * exp =
@@ -2972,8 +3018,7 @@ and build_field_lval env ~callee (receiver : G.expr) (id : G.ident) :
     {
       ident = id;
       sid = G.SId.unsafe_default;
-      id_info = G.empty_id_info ();
-    }
+      id_info = G.empty_id_info (); value_origin = None }
   in
   let offset = { o = Dot field_name; oorig = SameAs callee } in
   let field_lval =
@@ -2989,8 +3034,7 @@ and extend_field_lval ~callee (id : G.ident) (lval : lval) : lval =
     {
       ident = id;
       sid = G.SId.unsafe_default;
-      id_info = G.empty_id_info ();
-    }
+      id_info = G.empty_id_info (); value_origin = None }
   in
   let offset = { o = Dot field_name; oorig = SameAs callee } in
   { lval with rev_offset = offset :: lval.rev_offset }
@@ -3282,7 +3326,7 @@ and xml_expr env ~void eorig xml : stmts * exp =
                   {
                     ident = id;
                     sid = G.SId.unsafe_default;
-                    id_info = G.empty_id_info ();
+                    id_info = G.empty_id_info (); value_origin = None;
                   }
                 in
                 let ss_e, e = expr env eorig in
@@ -3307,7 +3351,7 @@ and xml_expr env ~void eorig xml : stmts * exp =
         {
           ident = ("children", G.fake "children");
           sid = G.SId.unsafe_default;
-          id_info = G.empty_id_info ();
+          id_info = G.empty_id_info (); value_origin = None;
         }
       in
       let filtered_fields = List.filter_map snd attr_results in
@@ -3470,6 +3514,7 @@ and stmt_expr env ?g_expr st : stmts * exp =
         | Some e_gen -> SameAs e_gen
       in
       (ss @ [if_stmt], mk_e (Fetch fresh) eorig)
+  | G.Block (tok, [], _) -> expr_opt env tok None
   | G.Block (_, block, _) -> (
       (* See 'AST_generic.stmt_to_expr' *)
       match List.rev block with
@@ -3942,10 +3987,8 @@ and stmt_aux env st : stmts =
       (* We want to analyze any expressions in 'ty'. *)
       let ss, _ = type_ env ty in
       ss
-  | G.DefStmt (ent, G.FuncDef fdef) when env.inside_function ->
-      (* Translate nested function declarations as lambda assignments so that
-       * the CFG builder extracts them into lambdas_cfgs, enabling the taint
-       * engine to propagate closure-captured variables through them. *)
+  | G.DefStmt (ent, G.FuncDef fdef) when env.inside_function || Lang.is_js env.lang ->
+      (* Function values retain their closure identity at callback use sites. *)
       let ss_lv, lv = lval_of_ent env ent in
       let il_fdef =
         (* See NOTE about resetting control-flow labels for lambdas. *)
@@ -3961,6 +4004,12 @@ and stmt_aux env st : stmts =
   | G.DirectiveStmt dir -> [ mk_s (MiscStmt (DirectiveStmt dir)) ]
   | G.Block xs ->
       let xs = xs |> Tok.unbracket in
+      let xs = if Lang.is_js env.lang then
+        let functions, statements = List.partition (function
+          | { G.s = G.DefStmt (_, G.FuncDef _); _ } -> true
+          | _ -> false) xs in
+        functions @ statements
+      else xs in
       List.concat_map (stmt env) xs
   (* Rust: if let Some(x) = some_x { ... } etc. *)
   (* TODO: Handle LetChain too, see Parse_rust_tree_sitter. *)
@@ -4109,6 +4158,10 @@ and stmt_aux env st : stmts =
   | G.Throw (tok, e, _) ->
       let ss, e = expr env e in
       ss @ [ mk_s (Throw (tok, e)) ]
+  | G.OtherStmt (G.OS_Pass, _) -> []
+  | G.OtherStmt (G.OS_ExprStmt2, [G.E expression]) ->
+      if expression.G.is_implicit_return then implicit_return env expression G.sc
+      else expr_stmt env expression G.sc
   | G.OtherStmt (G.OS_Go, [G.E call]) ->
       expr_stmt env call G.sc
   | G.OtherStmt (G.OS_ThrowNothing, [ G.Tk tok ]) ->
@@ -4162,8 +4215,18 @@ and stmt_aux env st : stmts =
   | G.RawStmt _ -> todo (G.S st)
 
 and for_each env tok (pat, tok2, e) st : stmts =
+  let e, next = match e.G.e with
+    | G.Call ({e = G.IdSpecial (G.ForOf, _); _}, (_, [G.Arg iterable], _))
+      when Lang.is_js env.lang -> (iterable, ForeachNext)
+    | _ when Lang.is_js env.lang -> (e, ForeachKeys)
+    | _ -> (e, ForeachNext) in
   let cont_label_s, break_label_s, st_env = break_continue_labels env tok in
   let ss, e' = expr env e in
+  let ss, e' = if Lang.is_js env.lang then
+      let iterable = fresh_lval tok2 in
+      (ss @ [mk_s (Instr (mk_i (Assign (iterable, e')) (related_exp e)))],
+       mk_e (Fetch iterable) (related_exp e))
+    else (ss, e') in
   let st = stmt st_env st in
   let next_lval = fresh_lval tok2 in
   let hasnext_lval = fresh_lval tok2 in
@@ -4179,7 +4242,7 @@ and for_each env tok (pat, tok2, e) st : stmts =
     mk_s
       (Instr
          (mk_i
-            (CallSpecial (Some next_lval, (ForeachNext, tok2), [ Unnamed e' ]))
+            (CallSpecial (Some next_lval, (next, tok2), [ Unnamed e' ]))
             (related_tok tok2)))
   in
   (* same semantic? or need to take Ref? or pass lval
diff --git a/src/analyzing/AST_to_IL.mli b/src/analyzing/AST_to_IL.mli
index 3e6ce6a..f1c2cbe 100644
--- a/src/analyzing/AST_to_IL.mli
+++ b/src/analyzing/AST_to_IL.mli
@@ -9,3 +9,6 @@ val lval : Lang.t -> AST_generic.expr -> IL.lval
 val name_of_entity : AST_generic.entity -> IL.name option
 val var_of_name : AST_generic.name -> IL.name
 val var_of_id_info : AST_generic.ident -> AST_generic.id_info -> IL.name
+
+val reset_translation_gaps : unit -> unit
+val take_translation_gaps : unit -> (string * Tok.location option) list
diff --git a/src/analyzing/tests/Test_analyze_generic.ml b/src/analyzing/tests/Test_analyze_generic.ml
index 7829310..3d72ef6 100644
--- a/src/analyzing/tests/Test_analyze_generic.ml
+++ b/src/analyzing/tests/Test_analyze_generic.ml
@@ -4,7 +4,7 @@ module H = AST_generic_helpers
 let test_typing_generic ~parse_program file =
   let ast = parse_program file in
   let lang = Lang.lang_of_filename_exn file in
-  Naming_AST.resolve lang ast;
+  let ast = Naming_AST.resolve lang ast in
 
   let v =
     object
@@ -25,7 +25,7 @@ let test_typing_generic ~parse_program file =
 let test_constant_propagation ~parse_program file =
   let ast = parse_program file in
   let lang = Lang.lang_of_filename_exn file in
-  Naming_AST.resolve lang ast;
+  let ast = Naming_AST.resolve lang ast in
   Constant_propagation.propagate_basic lang ast;
   let s = AST_generic.show_any (AST_generic.Pr ast) in
   UCommon.pr2 s
@@ -33,7 +33,7 @@ let test_constant_propagation ~parse_program file =
 let test_il_generic ~parse_program file =
   let ast = parse_program file in
   let lang = Lang.lang_of_filename_exn file in
-  Naming_AST.resolve lang ast;
+  let ast = Naming_AST.resolve lang ast in
   Implicit_return.mark_implicit_return lang ast;
 
   let v =
@@ -56,7 +56,7 @@ let test_il_generic ~parse_program file =
 let test_cfg_il (caps : < Cap.exec >) ~parse_program file =
   let ast = parse_program file in
   let lang = Lang.lang_of_filename_exn file in
-  Naming_AST.resolve lang ast;
+  let ast = Naming_AST.resolve lang ast in
   Implicit_return.mark_implicit_return lang ast;
   Visit_function_defs.visit
     (fun _ent fdef ->
@@ -79,7 +79,7 @@ end)
 let test_dfg_svalue ~parse_program file =
   let ast = parse_program file in
   let lang = Lang.lang_of_filename_exn file in
-  Naming_AST.resolve lang ast;
+  let ast = Naming_AST.resolve lang ast in
   let v =
     object
       inherit [_] AST_generic.iter_no_id_info
diff --git a/src/analyzing/tests/Unit_dataflow.ml b/src/analyzing/tests/Unit_dataflow.ml
index 83e059b..aba59aa 100644
--- a/src/analyzing/tests/Unit_dataflow.ml
+++ b/src/analyzing/tests/Unit_dataflow.ml
@@ -24,7 +24,7 @@ let tests (caps : < Cap.time_limit >)
           |> List.iter (fun file ->
                  let ast = parse_program file in
                  let lang = Lang.lang_of_filename_exn file in
-                 Naming_AST.resolve lang ast;
+                 let ast = Naming_AST.resolve lang ast in
                  match
                    Time_limit.set_timeout caps ~name:"cst_prop" timeout_secs
                      (fun () ->
diff --git a/src/core_cli/Core_actions.ml b/src/core_cli/Core_actions.ml
index d38bf45..04a6d3e 100644
--- a/src/core_cli/Core_actions.ml
+++ b/src/core_cli/Core_actions.ml
@@ -76,7 +76,7 @@ let dump_elixir_ast file =
 let dump_il_all (caps : < Cap.stdout >) file =
   let ast = Parse_target.parse_program file in
   let lang = Lang.lang_of_filename_exn file in
-  Naming_AST.resolve lang ast;
+  let ast = Naming_AST.resolve lang ast in
   let xs = AST_to_IL.stmt lang (AST_generic.stmt1 ast) in
   xs |> List.iter (fun stmt -> CapConsole.print caps#stdout (IL.show_stmt stmt))
 [@@action]
@@ -86,7 +86,7 @@ let dump_il (caps : < Cap.stdout >) file =
   let print s = CapConsole.print caps#stdout s in
   let ast = Parse_target.parse_program file in
   let lang = Lang.lang_of_filename_exn file in
-  Naming_AST.resolve lang ast;
+  let ast = Naming_AST.resolve lang ast in
   let report_func_def_with_name ent_opt fdef =
     let name =
       match ent_opt with
diff --git a/src/engine/Match_env.ml b/src/engine/Match_env.ml
index daa135a..251443d 100644
--- a/src/engine/Match_env.ml
+++ b/src/engine/Match_env.ml
@@ -61,6 +61,7 @@ type env = {
   pattern_matches : id_to_match_results;
   (* used by metavariable-pattern to recursively call evaluate_formula *)
   xtarget : Xtarget.t;
+  imported_bindings : Imported_bindings.t Lazy.t;
   rule : Rule.t;
   (* as-metavariable: This is here so we can easily pass down
      `has_as_metavariable` to `evaluate_formula`, which will dictate
diff --git a/src/engine/Match_search_mode.ml b/src/engine/Match_search_mode.ml
index bdab189..fa94ef1 100644
--- a/src/engine/Match_search_mode.ml
+++ b/src/engine/Match_search_mode.ml
@@ -748,13 +748,17 @@ let rec filter_ranges (env : env) (xs : (RM.t * MV.bindings list) list)
                  None)
          | R.CondName ({ mvar; _ } as cond) -> (
              let find_name env e cond =
-               match !hook_pro_metavariable_name with
+               match cond.R.kind, cond.R.modules with
+               | Some R.UnboundName, None -> Imported_bindings.unbound e
+               | None, Some modules ->
+                   Imported_bindings.matches (Lazy.force env.imported_bindings) e ?symbol:cond.symbol modules
+               | _ -> (match !hook_pro_metavariable_name with
                | None ->
                    error env
                      "semgrep-internal-metavariable-name operator is only \
                       supported in the Pro engine";
                    false
-               | Some f -> f e cond
+               | Some f -> f e cond)
              in
              let* mval = List.assoc_opt mvar bindings in
              match Metavariable.mvalue_to_expr mval with
@@ -1122,6 +1126,9 @@ and matches_of_formula xconf rule xtarget formula opt_context :
       xconf;
       pattern_matches = pattern_matches_per_id;
       xtarget;
+      imported_bindings = lazy (
+        let ast, _ = Lazy.force xtarget.Xtarget.lazy_ast_and_errors in
+        Imported_bindings.of_program ast);
       rule;
       has_as_metavariable;
       errors = ref E.ErrorSet.empty;
diff --git a/src/engine/Match_tainting_mode.ml b/src/engine/Match_tainting_mode.ml
index 9147e03..7a749ac 100644
--- a/src/engine/Match_tainting_mode.ml
+++ b/src/engine/Match_tainting_mode.ml
@@ -88,14 +88,40 @@ let lazy_force x = Lazy.force x [@@profiling]
 (* Pattern match from finding *)
 (*****************************************************************************)
 
-(* If the 'requires' has the shape 'A and ...' then we assume that 'A' is the
- * preferred label for reporting the taint trace. *)
-let preferred_label_of_sink ({ rule_sink; _ } : Effect.sink) =
+let source_labels_of_sink taints ({ rule_sink; _ } : Effect.sink) =
+  let rec supported_labels positive condition =
+    let tested = if positive then condition else R.PNot condition in
+    if not (T.taints_satisfy_requires taints tested) then None
+    else
+      match condition with
+      | R.PLabel label when positive -> Some [ label ]
+      | R.PAnd conditions -> combine positive positive conditions
+      | R.POr conditions -> combine (not positive) positive conditions
+      | R.PNot condition -> supported_labels (not positive) condition
+      | R.PLabel _
+      | R.PBool _
+      | R.PVariable _ ->
+          Some []
+  and combine conjunction positive conditions =
+    let supported = List.map (supported_labels positive) conditions in
+    if conjunction then
+      if List.exists Option.is_none supported then None
+      else Some (List.concat (List.filter_map Fun.id supported))
+    else if
+      List.exists
+        (function
+          | Some [] -> true
+          | _ -> false)
+        supported
+    then Some []
+    else
+      match List.filter_map Fun.id supported with
+      | [] -> None
+      | branches -> Some (List.concat branches)
+  in
   match rule_sink.sink_requires with
-  | Some { precondition = PAnd (PLabel label :: _); _ } -> Some label
-  | Some _
-  | None ->
-      None
+  | Some { precondition; _ } -> supported_labels true precondition
+  | None -> Some []
 
 let rec convert_taint_call_trace = function
   | Taint.PM (pm, _) ->
@@ -111,60 +137,45 @@ let rec convert_taint_call_trace = function
           call_trace = convert_taint_call_trace ct;
         }
 
-(* For now CLI does not support multiple taint traces for a finding, and it
- * simply picks the _first_ trace from this list. So here we apply a number
- * of heuristics to make sure the first trace in this list is the most
- * relevant one. This is particularly important when using (experimental)
- * taint labels, because not all labels are equally relevant for the finding. *)
-let sources_of_taints ?preferred_label taints =
-  (* We only report actual sources reaching a sink. If users want Semgrep to
-   * report function parameters reaching a sink without sanitization, then
-   * they need to specify the parameters as taint sources. *)
+let sources_of_taints ~labels taints =
   let taint_sources =
     taints
     |> List_.filter_map
          (fun { Effect.taint = { orig; tokens }; sink_trace; guard = _ } ->
            match orig with
-           | Src src -> Some (src, tokens, sink_trace)
-           (* even if there is any taint "variable", it's irrelevant for the
-            * finding, since the precondition is satisfied. *)
+           | Src src
+             when Option.fold ~none:false
+                    ~some:(function
+                      | [] -> true
+                      | labels -> List.mem src.T.label labels)
+                    labels ->
+               Some (src, tokens, sink_trace)
+           | Src _
            | Var _
            | Shape_var _
            | Control ->
                None)
   in
-  let taint_sources =
-    (* If there is a "preferred label", then sort sources to make sure this
-       label is picked before others. See 'preferred_label_of_sink'. *)
-    match preferred_label with
-    | None -> taint_sources
-    | Some label ->
-        taint_sources
-        |> List.stable_sort (fun (src1, _, _) (src2, _, _) ->
-               match (src1.T.label = label, src2.T.label = label) with
-               | true, false -> -1
-               | false, true -> 1
-               | false, false
-               | true, true ->
-                   0)
+  let labels = Option.value ~default:[] labels in
+  let rec label_rank label index = function
+    | [] -> max_int
+    | head :: _ when String.equal head label -> index
+    | _ :: rest -> label_rank label (index + 1) rest
   in
-  (* We prioritize taint sources without preconditions,
-     selecting their traces first, and then consider sources
-     with preconditions as a secondary choice. *)
-  let with_req, without_req =
+  (* Source selection follows the sink's declared positive labels. *)
+  List.stable_sort
+    (fun (left, _, _) (right, _, _) ->
+      match
+        Int.compare
+          (label_rank left.T.label 0 labels)
+          (label_rank right.T.label 0 labels)
+      with
+      | 0 ->
+          Bool.compare
+            (Option.is_some (get_source_requires left))
+            (Option.is_some (get_source_requires right))
+      | result -> result)
     taint_sources
-    |> Either_.partition (fun (src, tokens, sink_trace) ->
-           match get_source_requires src with
-           | Some _ -> Left (src, tokens, sink_trace)
-           | None -> Right (src, tokens, sink_trace))
-  in
-  if without_req <> [] then without_req
-  else (
-    Log.warn (fun m ->
-        m
-          "Taint source without precondition wasn't found. Displaying the \
-           taint trace from the source with precondition.");
-    with_req)
 
 let trace_of_source source =
   let src, tokens, sink_trace = source in
@@ -222,8 +233,8 @@ let pms_of_effect ~lang ~match_on (effect_ : Effect.t) =
       in
       if not satisfies then []
       else
-        let preferred_label = preferred_label_of_sink sink in
-        let taint_sources = sources_of_taints ?preferred_label taints in
+        let labels = source_labels_of_sink actual_taints sink in
+        let taint_sources = sources_of_taints ~labels taints in
         match match_on with
         | `Sink ->
             (* The old behavior used to be that, for sinks with a `requires`, we would
@@ -274,16 +285,17 @@ let pms_of_effect ~lang ~match_on (effect_ : Effect.t) =
 (* Main entry points *)
 (*****************************************************************************)
 
-let check_fundef (taint_inst : Taint_rule_inst.t) (name : IL.name) ?glob_env ?class_name
-    ?signature_db ?builtin_signature_db ?call_graph fdef =
+let check_fundef (taint_inst : Taint_rule_inst.t) (name : IL.name) ?glob_env
+    ?lambda_bodies ?class_name ?signature_db ?builtin_signature_db ?call_graph
+    fdef =
   let fdef = AST_to_IL.function_definition taint_inst.lang fdef in
   let fcfg = CFG_build.cfg_of_fdef fdef in
   let in_env, env_effects =
     Taint_input_env.mk_fun_input_env taint_inst ?glob_env fdef.fparams
   in
   let effects, mapping =
-    Dataflow_tainting.fixpoint taint_inst ~in_env ~name ?class_name
-      ?signature_db ?builtin_signature_db ?call_graph fcfg
+    Dataflow_tainting.fixpoint taint_inst ~in_env ~name ?lambda_bodies
+      ?class_name ?signature_db ?builtin_signature_db ?call_graph fcfg
   in
   let effects = Effects.union env_effects effects in
   (fcfg, effects, mapping)
@@ -312,8 +324,7 @@ let get_arity params info lang =
 let check_rule per_file_formula_cache (rule : R.taint_rule) match_hook
     ?(signature_db : Shape_and_sig.signature_database option)
     ?(builtin_signature_db : Shape_and_sig.builtin_signature_database option)
-    ?(shared_call_graph :
-        (Call_graph.G.t * (G.name * G.name) list) option =
+    ?(shared_call_graph : (Call_graph.G.t * (G.name * G.name) list) option =
       None) (xconf : Match_env.xconfig) (xtarget : Xtarget.t) =
   Log.info (fun m ->
       m
@@ -357,31 +368,75 @@ let check_rule per_file_formula_cache (rule : R.taint_rule) match_hook
   let (ast, skipped_tokens), parse_time =
     Common.with_time (fun () -> lazy_force lazy_ast_and_errors)
   in
-  (* TODO: 'debug_taint' should just be part of 'res'
-   * (i.e., add a "debugging" field to 'Report.match_result'). *)
-  match
-    Match_taint_spec.taint_config_of_rule ~per_file_formula_cache xconf lang
-      file (ast, []) rule
-  with
-  | None -> (None, None)
-  | Some (taint_inst, spec_matches, expls) ->
-      let glob_env, glob_effects = Taint_input_env.mk_file_env taint_inst ast in
-      record_matches glob_effects;
-
-      (* Only use signature database if cross-function taint analysis is enabled *)
-      let final_signature_db, relevant_graph =
-        if taint_inst.options.taint_intrafile then (
-          (* Detect object initialization mappings for this file *)
-          let object_mappings =
-            Taint_signature_extractor.detect_object_initialization ast
-              taint_inst.lang
-          in
-          (* Build user signature database *)
-          let base_db = Builtin_models.init_signature_database signature_db in
-          (* Note: object_mappings will be combined with anonymous class mappings
-           * and added to the signature database after IL conversion *)
+  if List.is_empty ast && List.is_empty skipped_tokens then (None, None)
+  else
+    (* TODO: 'debug_taint' should just be part of 'res'
+     * (i.e., add a "debugging" field to 'Report.match_result'). *)
+    match
+      Match_taint_spec.taint_config_of_rule ~per_file_formula_cache xconf lang
+        file (ast, []) rule
+    with
+    | None -> (None, None)
+    | Some (taint_inst, spec_matches, expls) ->
+        let import_env, import_effects =
+          Taint_input_env.mk_import_env taint_inst ast
+        in
+        let glob_env, glob_effects =
+          Taint_input_env.mk_file_env taint_inst ~in_env:import_env ast
+        in
+        record_matches (Effects.union import_effects glob_effects);
+        let model_values =
+          Lang.is_js lang
+          && List.exists
+               (fun (_, source) -> Option.is_some source.R.source_model_type)
+               spec_matches.sources
+        in
+        let deferred_functions = ref IL.NameSet.empty in
+        if model_values then
+          Visit_function_defs.visit
+            (fun entity definition ->
+              match (fst definition.G.fkind, entity) with
+              | (G.Function | G.Method | G.BlockCases), Some entity ->
+                  Option.iter
+                    (fun name ->
+                      deferred_functions :=
+                        IL.NameSet.add name !deferred_functions)
+                    (AST_to_IL.name_of_entity entity)
+              | _ -> ())
+            ast;
+        let deferred_functions = !deferred_functions in
+        let module_cfg =
+          if model_values then
+            let statements = AST_to_IL.stmt lang (G.stmt1 ast) in
+            let cfg, lambdas = CFG_build.cfg_of_stmts statements in
+            Some IL.{ params = []; cfg; lambdas }
+          else None
+        in
+        let glob_env, lambda_bodies =
+          match module_cfg with
+          | Some body ->
+              let _, mapping =
+                Dataflow_tainting.fixpoint taint_inst ~in_env:import_env
+                  ~deferred_functions ?builtin_signature_db body
+              in
+              ( mapping.(body.cfg.exit).Dataflow_core.out_env,
+                Dataflow_tainting.available_lambdas body )
+          | None -> (glob_env, IL.NameMap.empty)
+        in
+
+        (* Only use signature database if cross-function taint analysis is enabled *)
+        let final_signature_db, relevant_graph =
+          if taint_inst.options.taint_intrafile then (
+            (* Detect object initialization mappings for this file *)
+            let object_mappings =
+              Taint_signature_extractor.detect_object_initialization ast
+                taint_inst.lang
+            in
+            (* Build user signature database *)
+            let base_db = Builtin_models.init_signature_database signature_db in
+            (* Note: object_mappings will be combined with anonymous class mappings
+             * and added to the signature database after IL conversion *)
 
-          (* Collect function metadata and prepare call graph based ordering. *)
           let add_info info (infos, info_map) =
             let infos = info :: infos in
             let info_map =
@@ -501,116 +556,114 @@ let check_rule per_file_formula_cache (rule : R.taint_rule) match_hook
 
           (* Optimize: filter call graph to only functions relevant for this rule
              Use the already-computed source/sink ranges from spec_matches *)
-          let source_ranges =
-            spec_matches.sources
-            |> List.map (fun (rwm, _src) -> rwm.Range_with_metavars.r)
-          in
-          let sink_ranges =
-            spec_matches.sinks
-            |> List.map (fun (rwm, _sink) -> rwm.Range_with_metavars.r)
-          in
-          let source_functions =
-            Graph_from_AST.find_functions_containing_ranges ~lang ast
-              source_ranges
-          in
-          let sink_functions =
-            Graph_from_AST.find_functions_containing_ranges ~lang ast
-              sink_ranges
-          in
+            let source_ranges =
+              spec_matches.sources
+              |> List.map (fun (rwm, _src) -> rwm.Range_with_metavars.r)
+            in
+            let sink_ranges =
+              spec_matches.sinks
+              |> List.map (fun (rwm, _sink) -> rwm.Range_with_metavars.r)
+            in
+            let source_functions =
+              Graph_from_AST.find_functions_containing_ranges ~lang ast
+                source_ranges
+            in
+            let sink_functions =
+              Graph_from_AST.find_functions_containing_ranges ~lang ast
+                sink_ranges
+            in
 
-          Log.debug (fun m ->
-              m "SUBGRAPH: Found %d source functions and %d sink functions"
-                (List.length source_functions)
-                (List.length sink_functions));
-          List.iteri
-            (fun i id ->
-              Log.debug (fun m ->
-                  let name = Function_id.show id in
-                  m "SUBGRAPH: source_function[%d] = %s" i name))
-            source_functions;
-          List.iteri
-            (fun i id ->
-              Log.debug (fun m ->
-                  let name = Function_id.show id in
-                  m "SUBGRAPH: sink_function[%d] = %s" i name))
-            sink_functions;
-
-          (* Write FULL call graph to dot file for debugging. Keeping for debugger *)
-          (* let full_dot_file = open_out "call_graph_full.dot" in
+            Log.debug (fun m ->
+                m "SUBGRAPH: Found %d source functions and %d sink functions"
+                  (List.length source_functions)
+                  (List.length sink_functions));
+            List.iteri
+              (fun i id ->
+                Log.debug (fun m ->
+                    let name = Function_id.show id in
+                    m "SUBGRAPH: source_function[%d] = %s" i name))
+              source_functions;
+            List.iteri
+              (fun i id ->
+                Log.debug (fun m ->
+                    let name = Function_id.show id in
+                    m "SUBGRAPH: sink_function[%d] = %s" i name))
+              sink_functions;
+
+            (* Write FULL call graph to dot file for debugging. Keeping for debugger *)
+            (* let full_dot_file = open_out "call_graph_full.dot" in
           Call_graph.Dot.output_graph full_dot_file call_graph;
           close_out full_dot_file;
           Log.debug (fun m -> m "FULL GRAPH: Wrote full call graph to call_graph_full.dot"); *)
-          let relevant_graph =
-            Graph_reachability.compute_relevant_subgraph call_graph
-              ~sources:source_functions ~sinks:sink_functions
-          in
+            let relevant_graph =
+              Graph_reachability.compute_relevant_subgraph call_graph
+                ~sources:source_functions ~sinks:sink_functions
+            in
 
-          (* Write call graph to dot file for debugging *)
-          (* let dot_file = open_out "call_graph.dot" in
+            (* Write call graph to dot file for debugging *)
+            (* let dot_file = open_out "call_graph.dot" in
           Call_graph.Dot.output_graph dot_file relevant_graph;
           close_out dot_file;
           Log.debug (fun m -> m "SUBGRAPH: Wrote call graph to call_graph.dot"); *)
-          let analysis_order =
-            Call_graph.Topo.fold
-              (fun fn acc -> fn :: acc)
-              relevant_graph []
-            |> List.rev
-          in
-          Log.debug (fun m ->
-              m "TAINT_TOPO: Analysis order has %d functions"
-                (List.length analysis_order));
-          List.iteri
-            (fun i node ->
-              Log.debug (fun m ->
-                  m "TAINT_TOPO: [%d] %s" i (Function_id.show node)))
-            analysis_order;
-
-          let run_check_fundef_if_needed (info : fun_info)
-              (updated_db : Shape_and_sig.signature_database) :
-              Shape_and_sig.signature_database =
-            let _flow, fdef_effects, _mapping =
-              check_fundef taint_inst info.name ~glob_env
-                ?class_name:info.class_name_str ~signature_db:updated_db
-                ?builtin_signature_db
-                ?call_graph:(Some relevant_graph) info.fdef
+            let analysis_order =
+              Call_graph.Topo.fold (fun fn acc -> fn :: acc) relevant_graph []
+              |> List.rev
             in
-            (* For lambda assignments we only record "unconditional" ToSink
+            Log.debug (fun m ->
+                m "TAINT_TOPO: Analysis order has %d functions"
+                  (List.length analysis_order));
+            List.iteri
+              (fun i node ->
+                Log.debug (fun m ->
+                    m "TAINT_TOPO: [%d] %s" i (Function_id.show node)))
+              analysis_order;
+
+            let run_check_fundef_if_needed (info : fun_info)
+                (updated_db : Shape_and_sig.signature_database) :
+                Shape_and_sig.signature_database =
+              let _flow, fdef_effects, _mapping =
+                check_fundef taint_inst info.name ~glob_env
+                  ?class_name:info.class_name_str ~signature_db:updated_db
+                  ?builtin_signature_db ?call_graph:(Some relevant_graph)
+                  info.fdef
+              in
+              (* For lambda assignments we only record "unconditional" ToSink
                effects — those where the taint at the sink comes from a
                concrete pattern-source match (e.g. a parameter declared as a
                source via `pattern-inside: function $X(..., $RES, ...) {...}`).
                Effects whose taint is purely parameterized (BArg) still ride
                through the signature at resolved call sites; effects mixing
                both get an Src-only slice surfaced here. *)
-            let keep_src_toSink_only (eff : Effect.t) : Effect.t option =
-              match eff with
-              | Effect.ToSink si ->
-                  let items, precond = si.taints_with_precondition in
-                  let src_items =
-                    List.filter
-                      (fun (i : Effect.taint_to_sink_item) ->
-                        match i.taint.orig with
-                        | Taint.Src _ -> true
-                        | _ -> false)
-                      items
-                  in
-                  if List_.null src_items then None
-                  else
-                    Some
-                      (Effect.ToSink
-                         {
-                           si with
-                           taints_with_precondition = (src_items, precond);
-                         })
-              | _ -> None
-            in
-            let effects_to_record =
-              if info.is_lambda_assignment then
-                Effects.filter_map keep_src_toSink_only fdef_effects
-              else fdef_effects
+              let keep_src_toSink_only (eff : Effect.t) : Effect.t option =
+                match eff with
+                | Effect.ToSink si ->
+                    let items, precond = si.taints_with_precondition in
+                    let src_items =
+                      List.filter
+                        (fun (i : Effect.taint_to_sink_item) ->
+                          match i.taint.orig with
+                          | Taint.Src _ -> true
+                          | _ -> false)
+                        items
+                    in
+                    if List_.null src_items then None
+                    else
+                      Some
+                        (Effect.ToSink
+                           {
+                             si with
+                             taints_with_precondition = (src_items, precond);
+                           })
+                | _ -> None
+              in
+              let effects_to_record =
+                if info.is_lambda_assignment then
+                  Effects.filter_map keep_src_toSink_only fdef_effects
+                else fdef_effects
+              in
+              record_matches effects_to_record;
+              updated_db
             in
-            record_matches effects_to_record;
-            updated_db
-          in
 
           let process_fun_info info db =
             let params = Tok.unbracket info.fdef.fparams in
@@ -650,147 +703,160 @@ let check_rule per_file_formula_cache (rule : R.taint_rule) match_hook
             run_check_fundef_if_needed info updated_db
           in
 
-          let signature_db_after_order =
-            List.fold_left
-              (fun db node ->
-                Log.debug (fun m ->
-                    m "TAINT_SIGBUILD: Processing %s" (Function_id.show node));
-                match Shape_and_sig.FunctionMap.find_opt node info_map with
-                | None ->
-                    Log.debug (fun m ->
-                        m "TAINT_SIGBUILD: fn_id NOT FOUND in info_map!");
-                    db
-                | Some info ->
-                    Log.debug (fun m ->
-                        m
-                          "TAINT_SIGBUILD: fn_id found in info_map, \
-                           processing...");
-                    let new_db = process_fun_info info db in
-                    Log.debug (fun m ->
-                        m
-                          "TAINT_SIGBUILD: After processing, db.signatures \
-                           size=%d"
-                          (Shape_and_sig.FunctionMap.cardinal
-                             new_db.Shape_and_sig.signatures));
-                    new_db)
-              initial_signature_db analysis_order
-          in
+            let signature_db_after_order =
+              List.fold_left
+                (fun db node ->
+                  Log.debug (fun m ->
+                      m "TAINT_SIGBUILD: Processing %s" (Function_id.show node));
+                  match Shape_and_sig.FunctionMap.find_opt node info_map with
+                  | None ->
+                      Log.debug (fun m ->
+                          m "TAINT_SIGBUILD: fn_id NOT FOUND in info_map!");
+                      db
+                  | Some info ->
+                      Log.debug (fun m ->
+                          m
+                            "TAINT_SIGBUILD: fn_id found in info_map, \
+                             processing...");
+                      let new_db = process_fun_info info db in
+                      Log.debug (fun m ->
+                          m
+                            "TAINT_SIGBUILD: After processing, db.signatures \
+                             size=%d"
+                            (Shape_and_sig.FunctionMap.cardinal
+                               new_db.Shape_and_sig.signatures));
+                      new_db)
+                initial_signature_db analysis_order
+            in
 
-          (* Skip the "remaining functions" phase entirely - if a function isn't
+            (* Skip the "remaining functions" phase entirely - if a function isn't
              in the relevant subgraph, we don't need to analyze it *)
-          let final_signature_db = signature_db_after_order in
-          (Some final_signature_db, Some relevant_graph))
-        else (
-          (* Cross-function taint analysis disabled: use main branch behavior *)
-          Visit_function_defs.visit
-            (fun opt_ent fdef ->
-              match fst fdef.fkind with
-              | LambdaKind
-              | Arrow ->
-                  (* We do not need to analyze lambdas here, they will be analyzed
+            let final_signature_db = signature_db_after_order in
+            (Some final_signature_db, Some relevant_graph))
+          else (
+            (* Cross-function taint analysis disabled: use main branch behavior *)
+            Visit_function_defs.visit
+              (fun opt_ent fdef ->
+                match fst fdef.fkind with
+                | LambdaKind
+                | Arrow ->
+                    (* We do not need to analyze lambdas here, they will be analyzed
                together with their enclosing function. This would just duplicate
                work. *)
-                  ()
-              | Function
-              | Method
-              | BlockCases ->
-                  let opt_name =
-                    let* ent = opt_ent in
-                    AST_to_IL.name_of_entity ent
-                  in
-                  match opt_name with
-                  | None -> ()
-                  | Some name ->
-                      Log.info (fun m ->
-                          m
-                            "Match_tainting_mode:\n\
-                             --------------------\n\
-                             Checking func def: %s\n\
-                             --------------------"
-                            (IL.str_of_name name));
-                      let _flow, fdef_effects, _mapping =
-                        check_fundef taint_inst name ~glob_env
-                          ?builtin_signature_db fdef
-                      in
-                      record_matches fdef_effects)
-            ast;
-          (None, None))
-      in
+                    ()
+                | Function
+                | Method
+                | BlockCases -> (
+                    let opt_name =
+                      let* ent = opt_ent in
+                      AST_to_IL.name_of_entity ent
+                    in
+                    match opt_name with
+                    | None -> ()
+                    | Some name ->
+                        Log.info (fun m ->
+                            m
+                              "Match_tainting_mode:\n\
+                               --------------------\n\
+                               Checking func def: %s\n\
+                               --------------------"
+                              (IL.str_of_name name));
+                        let _flow, fdef_effects, _mapping =
+                          check_fundef taint_inst name ~glob_env ~lambda_bodies
+                            ?builtin_signature_db fdef
+                        in
+                        record_matches fdef_effects))
+              ast;
+            (None, None))
+        in
 
-      (* Check execution of statements during object initialization. *)
-      Visit_class_defs.visit
-        (fun opt_ent cdef ->
-          let opt_name =
-            let* ent = opt_ent in
-            AST_to_IL.name_of_entity ent
-          in
-          let fields =
-            cdef.G.cbody |> Tok.unbracket
-            |> List_.map (function G.F x -> x)
-            |> G.stmt1
-          in
-          let stmts = AST_to_IL.stmt taint_inst.lang fields in
-          let cfg, lambdas = CFG_build.cfg_of_stmts stmts in
-          let init_effects, _mapping =
-            Dataflow_tainting.fixpoint taint_inst ?name:opt_name
-              ?signature_db:final_signature_db ?builtin_signature_db
-              ?call_graph:relevant_graph
-              IL.{ params = []; cfg; lambdas }
-          in
-          record_matches init_effects)
-        ast;
-
-      (* Check the top-level statements.
-       * In scripting languages it is not unusual to write code outside
-       * function declarations and we want to check this too. We simply
-       * treat the program itself as an anonymous function. *)
-      let (), match_time =
-        Common.with_time (fun () ->
-            let xs = AST_to_IL.stmt taint_inst.lang (G.stmt1 ast) in
-            let cfg, lambdas = CFG_build.cfg_of_stmts xs in
-            let top_level_name =
-              let fake_tok = Tok.unsafe_fake_tok "<top_level>" in
-              IL.{ ident = ("<top_level>", fake_tok); sid = G.SId.unsafe_default; id_info = G.empty_id_info () }
+        (* Check execution of statements during object initialization. *)
+        Visit_class_defs.visit
+          (fun opt_ent cdef ->
+            let opt_name =
+              let* ent = opt_ent in
+              AST_to_IL.name_of_entity ent
             in
-            let top_effects, _mapping =
-              Dataflow_tainting.fixpoint taint_inst ~name:top_level_name
+            let fields =
+              cdef.G.cbody |> Tok.unbracket
+              |> List_.map (function G.F x -> x)
+              |> G.stmt1
+            in
+            let stmts = AST_to_IL.stmt taint_inst.lang fields in
+            let cfg, lambdas = CFG_build.cfg_of_stmts stmts in
+            let init_effects, _mapping =
+              Dataflow_tainting.fixpoint taint_inst ?name:opt_name
                 ?signature_db:final_signature_db ?builtin_signature_db
                 ?call_graph:relevant_graph
                 IL.{ params = []; cfg; lambdas }
             in
-            record_matches top_effects)
-      in
-      let matches =
-        !matches
-        (* same post-processing as for search-mode in Match_rules.ml *)
-        |> PM.uniq
-        |> PM.no_submatches (* see "Taint-tracking via ranges" *) |> match_hook
-      in
+            record_matches init_effects)
+          ast;
 
-      let errors = Parse_target.errors_from_skipped_tokens skipped_tokens in
-      let report =
-        RP.mk_match_result matches errors
-          {
-            Core_profiling.rule_id = fst rule.R.id;
-            rule_parse_time = parse_time;
-            rule_match_time = match_time;
-          }
-      in
-      let explanations =
-        if xconf.matching_explanations then
-          [
+        (* Check the top-level statements.
+         * In scripting languages it is not unusual to write code outside
+         * function declarations and we want to check this too. We simply
+         * treat the program itself as an anonymous function. *)
+        let (), match_time =
+          Common.with_time (fun () ->
+              let cfg, lambdas =
+                match module_cfg with
+                | Some body -> (body.cfg, body.lambdas)
+                | None ->
+                    let xs = AST_to_IL.stmt taint_inst.lang (G.stmt1 ast) in
+                    CFG_build.cfg_of_stmts xs
+              in
+              let top_level_name =
+                let fake_tok = Tok.unsafe_fake_tok "<top_level>" in
+                IL.
+                  {
+                    ident = ("<top_level>", fake_tok);
+                    sid = G.SId.unsafe_default;
+                    id_info = G.empty_id_info ();
+                    value_origin = None;
+                  }
+              in
+              let top_effects, _mapping =
+                Dataflow_tainting.fixpoint taint_inst ~in_env:import_env
+                  ~name:top_level_name ~deferred_functions
+                  ?signature_db:final_signature_db ?builtin_signature_db
+                  ?call_graph:relevant_graph
+                  IL.{ params = []; cfg; lambdas }
+              in
+              record_matches top_effects)
+        in
+        let matches =
+          !matches
+          (* same post-processing as for search-mode in Match_rules.ml *)
+          |> PM.uniq
+          |> PM.no_submatches (* see "Taint-tracking via ranges" *)
+          |> match_hook
+        in
+
+        let errors = Parse_target.errors_from_skipped_tokens skipped_tokens in
+        let report =
+          RP.mk_match_result matches errors
             {
-              ME.op = OutJ.Taint;
-              children = expls;
-              matches = report.matches;
-              pos = snd rule.id;
-              extra = None;
-            };
-          ]
-        else []
-      in
-      let report = { report with explanations } in
-      (Some report, final_signature_db)
+              Core_profiling.rule_id = fst rule.R.id;
+              rule_parse_time = parse_time;
+              rule_match_time = match_time;
+            }
+        in
+        let explanations =
+          if xconf.matching_explanations then
+            [
+              {
+                ME.op = OutJ.Taint;
+                children = expls;
+                matches = report.matches;
+                pos = snd rule.id;
+                extra = None;
+              };
+            ]
+          else []
+        in
+        let report = { report with explanations } in
+        (Some report, final_signature_db)
 
 let check_rules ~match_hook
     ~(per_rule_boilerplate_fn :
@@ -800,6 +866,10 @@ let check_rules ~match_hook
     (rules : R.taint_rule list) (xconf : Match_env.xconfig)
     (xtarget : Xtarget.t) :
     Core_profiling.rule_profiling Core_result.match_result list =
+  AST_to_IL.reset_translation_gaps ();
+  Taint_aliases.reset_diagnostics ();
+  Taint_callback_models.reset_diagnostics ();
+  Taint_model_types.reset_diagnostics ();
   (* Check for language support warnings when taint_intrafile is enabled *)
   (Dataflow_tainting.reset_constructor ();
    (* Clear the per-domain guard-cond intern table: rules run on a domain in
@@ -937,4 +1007,37 @@ let check_rules ~match_hook
                    report)))
   in
 
-  results
+  let semantic_gaps =
+    AST_to_IL.take_translation_gaps ()
+    @ List.map
+        (fun location -> ("object_alias_limit", location))
+        (Taint_aliases.take_diagnostics ())
+    @ Taint_callback_models.take_diagnostics ()
+    @ Taint_model_types.take_diagnostics ()
+  in
+  let semantic_errors =
+    semantic_gaps
+    |> List.map (fun (construct, loc) ->
+           Core_error.mk_error ?loc
+             ~target_path:xtarget.path.internal_path_to_content
+             ~msg:"Unsupported dataflow semantics; analysis is incomplete"
+             (Semgrep_output_v1_t.PartialSemantics construct))
+    |> Core_error.ErrorSet.of_list
+  in
+  match results with
+  | result :: rest ->
+      {
+        result with
+        errors = Core_error.ErrorSet.union result.errors semantic_errors;
+      }
+      :: rest
+  | [] when not (Core_error.ErrorSet.is_empty semantic_errors) ->
+      [
+        {
+          Core_result.matches = [];
+          errors = semantic_errors;
+          profiling = None;
+          explanations = [];
+        };
+      ]
+  | [] -> []
diff --git a/src/engine/Match_tainting_mode.mli b/src/engine/Match_tainting_mode.mli
index 7902db6..3d5b71f 100644
--- a/src/engine/Match_tainting_mode.mli
+++ b/src/engine/Match_tainting_mode.mli
@@ -1,35 +1,31 @@
 val hook_setup_hook_function_taint_signature :
   (Rule.taint_rule -> Taint_rule_inst.t -> Xtarget.t -> unit) option ref
 (** This is used for intra-file inter-procedural taint-tracking, and the idea is
-  * that this hook will do top-sorting and infer the signature of each function
-  * in the file, and while doing this it will also setup
-  * 'Dataflow_tainting.hook_function_taint_signature'.
-  *
-  * Doing it here (vs what DeepSemgrep does) has the advantage that we can re-use
-  * the same 'Dataflow_tainting.config' without having to do any caching on disk.
-  *
-  * FIXME: Once we have the taint signature of a function we do not need to run
-  *   taint tracking on it anymore... but we still do it hence duplicating work.
-  *   We only need to analyze anonymous functions which do not get taint signatures
-  *   (or we could infer a signature for them too...).
-  *)
+    * that this hook will do top-sorting and infer the signature of each
+    function * in the file, and while doing this it will also setup *
+    'Dataflow_tainting.hook_function_taint_signature'. * * Doing it here (vs
+    what DeepSemgrep does) has the advantage that we can re-use * the same
+    'Dataflow_tainting.config' without having to do any caching on disk. * *
+    FIXME: Once we have the taint signature of a function we do not need to run
+    * taint tracking on it anymore... but we still do it hence duplicating work.
+    * We only need to analyze anonymous functions which do not get taint
+    signatures * (or we could infer a signature for them too...). *)
 
 val check_fundef :
   Taint_rule_inst.t ->
   IL.name (** entity being analyzed *) ->
   ?glob_env:Taint_lval_env.t ->
+  ?lambda_bodies:IL.lambdas_cfgs ->
   ?class_name:string ->
   ?signature_db:Shape_and_sig.signature_database ->
   ?builtin_signature_db:Shape_and_sig.builtin_signature_database ->
   ?call_graph:Call_graph.G.t ->
   AST_generic.function_definition ->
   IL.fun_cfg * Shape_and_sig.Effects.t * Dataflow_tainting.mapping
-(** Check a function definition using a [Dataflow_tainting.config] (which can
-  * be obtained with [taint_config_of_rule]). Findings are passed on-the-fly
-  * to the [handle_findings] callback in the dataflow config.
-  *
-  * This is a low-level function exposed for debugging purposes (-dfg_tainting).
-  *)
+(** Check a function definition using a [Dataflow_tainting.config] (which can *
+    be obtained with [taint_config_of_rule]). Findings are passed on-the-fly *
+    to the [handle_findings] callback in the dataflow config. * * This is a
+    low-level function exposed for debugging purposes (-dfg_tainting). *)
 
 val check_rule :
   Formula_cache.t ->
@@ -37,13 +33,14 @@ val check_rule :
   (Core_match.t list -> Core_match.t list) ->
   ?signature_db:Shape_and_sig.signature_database ->
   ?builtin_signature_db:Shape_and_sig.builtin_signature_database ->
-  ?shared_call_graph:(Call_graph.G.t * (AST_generic.name * AST_generic.name) list) option ->
+  ?shared_call_graph:
+    (Call_graph.G.t * (AST_generic.name * AST_generic.name) list) option ->
   Match_env.xconfig ->
   Xtarget.t ->
-  Core_profiling.rule_profiling Core_result.match_result option * Shape_and_sig.signature_database option
+  Core_profiling.rule_profiling Core_result.match_result option
+  * Shape_and_sig.signature_database option
 (** Check a single taint rule on a target. Returns both the match result and the
-  * computed signature database (when taint_intrafile is enabled).
-  *)
+    * computed signature database (when taint_intrafile is enabled). *)
 
 val check_rules :
   match_hook:(Core_match.t list -> Core_match.t list) ->
@@ -56,7 +53,6 @@ val check_rules :
   Xtarget.t ->
   (* timeout function *)
   Core_profiling.rule_profiling Core_result.match_result list
-(** Runs the engine on a group of taint rules, which should be for the
-  * same language. Running on multiple rules at once enables inter-rule
-  * optimizations.
-  *)
+(** Runs the engine on a group of taint rules, which should be for the * same
+    language. Running on multiple rules at once enables inter-rule *
+    optimizations. *)
diff --git a/src/engine/tests/Test_dataflow_tainting.ml b/src/engine/tests/Test_dataflow_tainting.ml
index bc0586c..3a34003 100644
--- a/src/engine/tests/Test_dataflow_tainting.ml
+++ b/src/engine/tests/Test_dataflow_tainting.ml
@@ -27,7 +27,7 @@ let test_tainting taint_inst def =
   UCommon.pr2 "--------";
   let test_name =
     let fake_tok = Tok.unsafe_fake_tok "<test>" in
-    IL.{ ident = ("<test>", fake_tok); sid = AST_generic.SId.unsafe_default; id_info = AST_generic.empty_id_info () }
+    IL.{ ident = ("<test>", fake_tok); sid = AST_generic.SId.unsafe_default; id_info = AST_generic.empty_id_info (); value_origin = None }
   in
   let fcfg, _effects_IGNORED, mapping =
     Match_tainting_mode.check_fundef taint_inst
diff --git a/src/experiments/datalog/Datalog_experiment.ml b/src/experiments/datalog/Datalog_experiment.ml
index ec0dd7b..f78e39f 100644
--- a/src/experiments/datalog/Datalog_experiment.ml
+++ b/src/experiments/datalog/Datalog_experiment.ml
@@ -107,7 +107,7 @@ let facts_of_function lang def =
 let gen_facts file outdir =
   let ast = Parse_target.parse_program file in
   let lang = Lang.lang_of_filename_exn file in
-  Naming_AST.resolve lang ast;
+  let ast = Naming_AST.resolve lang ast in
 
   (* less: use treesitter also later
    *  Parse_code.parse_and_resolve_name_use_pfff_or_treesitter lang file in
diff --git a/src/experiments/synthesizing/Pattern_from_diff.ml b/src/experiments/synthesizing/Pattern_from_diff.ml
index ba1067e..06039a0 100644
--- a/src/experiments/synthesizing/Pattern_from_diff.ml
+++ b/src/experiments/synthesizing/Pattern_from_diff.ml
@@ -22,7 +22,7 @@ open Fpath_.Operators
         let assign_to_vardef = true in
         let ast = Parse_python.parse_program file in
         let ast = Python_to_generic.program ~assign_to_vardef ast in
-        Naming_AST.resolve lang ast;
+        let ast = Naming_AST.resolve lang ast in
         ast
 
       let get_root_dir filename =
diff --git a/src/experiments/synthesizing/Synthesizer.ml b/src/experiments/synthesizing/Synthesizer.ml
index 1cf02c3..38bff57 100644
--- a/src/experiments/synthesizing/Synthesizer.ml
+++ b/src/experiments/synthesizing/Synthesizer.ml
@@ -8,7 +8,7 @@ let range_to_ast (file : Fpath.t) (lang : Lang.t) (s : string) : AST_generic.any
   let r = Range.range_of_linecol_spec s file in
   let ast = Parse_target.parse_and_resolve_name_fail_if_partial lang file in
   let a_opt = Range_to_AST.any_at_range_all r ast in
-  Naming_AST.resolve lang ast;
+  let ast = Naming_AST.resolve lang ast in
   Constant_propagation.propagate_basic lang ast;
   Constant_propagation.propagate_dataflow lang ast;
   match a_opt with
diff --git a/src/experiments/synthesizing/Test_synthesizing.ml b/src/experiments/synthesizing/Test_synthesizing.ml
index b778929..5c321b5 100644
--- a/src/experiments/synthesizing/Test_synthesizing.ml
+++ b/src/experiments/synthesizing/Test_synthesizing.ml
@@ -22,7 +22,7 @@ let expr_at_range (s : string) (file : Fpath.t) : unit =
   let ast = Parse_target.parse_program file in
   (* just to see if it works with Naming on *)
   let lang = Lang.lang_of_filename_exn file in
-  Naming_AST.resolve lang ast;
+  let ast = Naming_AST.resolve lang ast in
   let e_opt = Range_to_AST.expr_at_range r ast in
   match e_opt with
   | Some e -> UCommon.pr (AST_generic.show_expr e)
diff --git a/src/experiments/synthesizing/Unit_synthesizer.ml b/src/experiments/synthesizing/Unit_synthesizer.ml
index 4a74edf..1bad8d1 100644
--- a/src/experiments/synthesizing/Unit_synthesizer.ml
+++ b/src/experiments/synthesizing/Unit_synthesizer.ml
@@ -198,7 +198,7 @@ let tests =
                           file
                       in
                       (* BUG? resolve again? already done above *)
-                      Naming_AST.resolve lang ast;
+                      let ast = Naming_AST.resolve lang ast in
 
                       let r = Range.range_of_linecol_spec range file in
 
diff --git a/src/il/IL.ml b/src/il/IL.ml
index 20b9f79..5db082a 100644
--- a/src/il/IL.ml
+++ b/src/il/IL.ml
@@ -102,14 +102,14 @@ type ident = G.ident [@@deriving show, eq]
  * can become control-flow sensitive? (e.g., DOOP)
  *
  *)
-type name = { ident : ident; sid : G.sid; id_info : G.id_info }
+type name = { ident : ident; sid : G.sid; id_info : G.id_info; value_origin : G.expr option [@equal fun _ _ -> true] }
 [@@deriving show,eq]
 
 let str_of_name name = Common.spf "%s:%s" (fst name.ident) (G.SId.show name.sid)
 
 let compare_name name1 name2 =
-  let { ident = str1, _tok1; sid = sid1; id_info = _ } = name1 in
-  let { ident = str2, _tok2; sid = sid2; id_info = _ } = name2 in
+  let { ident = str1, _tok1; sid = sid1; id_info = _; value_origin = _ } = name1 in
+  let { ident = str2, _tok2; sid = sid2; id_info = _; value_origin = _ } = name2 in
   match G.SId.compare sid1 sid2 with
   | 0 -> String.compare str1 str2
   | cmp -> cmp
@@ -161,11 +161,14 @@ type fixme_kind =
  * so make sure that the annotation makes sense, or it could lead to weird taint
  * findings.
  *
+ * ValueOf connects a computed temporary to the expression producing its value.
+ * Callback binding uses that origin without repeating source/sink matching.
+ *
  * When no orig info is needed, or it cannot be provided, then use NoOrig. For
  * example, auxiliary assignments where the RHS has the right orig-info can be
  * annotated with NoOrig. This also helps making -dump_il more readable.
  *)
-type orig = SameAs of G.expr | Related of G.any | NoOrig
+type orig = SameAs of G.expr | Related of G.any | ValueOf of G.expr | NoOrig
 [@@deriving eq, ord, show { with_path = false }]
 
 let related_tok tok = Related (G.Tk tok)
@@ -174,7 +177,11 @@ let related_exp exp_gen = Related (G.E exp_gen)
 let any_of_orig = function
   | SameAs e -> G.E e
   | Related any -> any
-  | NoOrig -> G.Anys []
+  | NoOrig | ValueOf _ -> G.Anys []
+
+let any_of_value_orig = function
+  | ValueOf expression -> G.E expression
+  | original -> any_of_orig original
 
 (*****************************************************************************)
 (* Parameters and arguments *)
@@ -396,6 +403,7 @@ and call_special =
   | Ref (* TODO: lift up, have AssignRef? *)
   (* when transpiling certain features (e.g., patterns, foreach) *)
   | ForeachNext
+  | ForeachKeys
   | ForeachHasNext
   (* JS: require('foo') *)
   | Require
diff --git a/src/il/IL_pp.ml b/src/il/IL_pp.ml
index 0da2959..c3627d9 100644
--- a/src/il/IL_pp.ml
+++ b/src/il/IL_pp.ml
@@ -333,6 +333,7 @@ let pp_call_special cs =
   | Assert -> "assert"
   | Ref -> "__ref__"
   | ForeachNext -> "__foreach_next__"
+  | ForeachKeys -> "__foreach_keys__"
   | ForeachHasNext -> "__foreach_has_next__"
   | Require -> "require"
 
diff --git a/src/metachecking/Translate_rule.ml b/src/metachecking/Translate_rule.ml
index 460c590..2697e47 100644
--- a/src/metachecking/Translate_rule.ml
+++ b/src/metachecking/Translate_rule.ml
@@ -68,11 +68,13 @@ let rec translate_metavar_cond cond : [> `O of (string * Yaml.value) list ] =
         match lang with
         | None -> []
         | Some x -> [ ("language", `String (Xlang.to_string x)) ])
-  | CondName { mvar; kind; modules } ->
+  | CondName { mvar; kind; modules; symbol } ->
       `O
         ([ ("metavariable", `String mvar) ]
+        @ (match symbol with None -> [] | Some names -> [("symbol", `A (List_.map (fun name -> `String name) names))])
         @ (match kind with
           | Some DjangoView -> [ ("kind", `String "django-view") ]
+          | Some UnboundName -> [ ("kind", `String "unbound") ]
           | None -> [])
         @
         match modules with
@@ -103,9 +105,28 @@ let rec translate_metavar_cond cond : [> `O of (string * Yaml.value) list ] =
         | None -> []
         | Some x -> [ ("language", `String (Xlang.to_string x)) ])
 
+and translate_callback_parameter = function
+    | None -> [] | Some { index; path; arities; array_elements; array_head_callable; preserves_array } ->
+      let fields = [("preserves-array", `Bool preserves_array); ("array-head-callable", `Bool array_head_callable); ("array-elements", `Bool array_elements); ("index", `Float (float_of_int index));
+        ("path", `A (List.map (fun field -> `String field) path))] in
+      let arities = match arities with None -> [] | Some values ->
+        [("arities", `A (List.map (fun count -> `Float (float_of_int count)) values))] in
+      [("to-parameter", `O (fields @ arities))]
+
+and translate_model_constraint = function
+  | None -> []
+  | Some (variable, name, member) ->
+      let member = match member with None -> [] | Some name -> [("member", `String name)] in
+      [("requires-model-type", `O ([("metavariable", `String variable); ("type", `String name)] @ member))]
+
 and translate_taint_source
     {
       source_id = _;
+      source_value_type;
+      source_model_type;
+      source_model_singleton;
+      source_to_parameter;
+      source_requires_model_type;
       source_formula;
       source_exact;
       source_by_side_effect;
@@ -113,7 +134,13 @@ and translate_taint_source
       source_control;
       source_requires;
     } : [> `O of (string * Yaml.value) list ] =
+  let model_obj = (match source_model_type with None -> [] | Some name -> [("model-type", `String name)])
+    @ (if source_model_singleton then [("model-singleton", `Bool true)] else []) in
+  let model_constraint = translate_model_constraint source_requires_model_type in
   let (`O source_f) = translate_formula source_formula in
+  let value_type_obj = match source_value_type with
+    | None -> []
+    | Some kind -> [("value-type", `String (match kind with Rule.StringOrNull -> "string-or-null" | Rule.PosixShellWord -> "posix-shell-word"))] in
   let label_obj =
     if label = Rule.default_source_label then []
     else [ ("label", `String label) ]
@@ -137,6 +164,7 @@ and translate_taint_source
     (List_.flatten
        [
          source_f;
+         value_type_obj; model_obj; model_constraint; translate_callback_parameter source_to_parameter;
          exact_obj;
          control_obj;
          label_obj;
@@ -150,6 +178,7 @@ and translate_taint_sink
       sink_formula;
       sink_exact;
       sink_requires;
+      sink_requires_model_type;
       sink_at_exit;
       sink_has_focus = _;
     } : [> `O of (string * Yaml.value) list ] =
@@ -161,13 +190,15 @@ and translate_taint_sink
     | None -> []
     | Some { range; _ } -> [ ("requires", `String (range_to_string range)) ]
   in
-  `O (List_.flatten [ sink_f; exact_obj; requires_obj; at_exit_obj ])
+  `O (List_.flatten [ sink_f; exact_obj; requires_obj; at_exit_obj; translate_model_constraint sink_requires_model_type ])
 
 and translate_taint_sanitizer
     {
       sanitizer_id = _;
       sanitizer_formula;
       sanitizer_exact;
+      sanitizer_output_type;
+      sanitizer_requires_type;
       sanitizer_by_side_effect;
       not_conflicting;
     } : [> `O of (string * Yaml.value) list ] =
@@ -179,19 +210,30 @@ and translate_taint_sanitizer
   let not_conflicting_obj =
     if not_conflicting then [ ("not-conflicting", `Bool true) ] else []
   in
-  `O (List_.flatten [ san_f; exact_obj; side_effect_obj; not_conflicting_obj ])
+  let type_name = function Rule.StringOrNull -> "string-or-null" | Rule.PosixShellWord -> "posix-shell-word" in
+  let output = match sanitizer_output_type with None -> [] | Some kind -> [("output-value-type", `String (type_name kind))] in
+  let requires = match sanitizer_requires_type with None -> [] | Some (variable, kind) ->
+    [("requires-value-type", `O [("metavariable", `String variable); ("type", `String (type_name kind))])] in
+  `O (List_.flatten [ san_f; exact_obj; side_effect_obj; not_conflicting_obj; output; requires ])
 
 and translate_taint_propagator
     {
       propagator_id = _;
       propagator_formula;
       propagator_by_side_effect;
+      propagator_to_parameter;
+      propagator_from_model_type;
+      propagator_from_model_member;
+      propagator_preserves_model_type;
       from;
       to_;
       propagator_requires;
       propagator_replace_labels;
       propagator_label;
     } : [> `O of (string * Yaml.value) list ] =
+  let model_obj = match propagator_from_model_type with None -> [] | Some name -> [("from-model-type", `String name)] in
+  let preserves_obj = if propagator_preserves_model_type then [("preserves-model-type", `Bool true)] else [] in
+  let member_obj = match propagator_from_model_member with None -> [] | Some name -> [("from-model-member", `String name)] in
   let (`O prop_f) = translate_formula propagator_formula in
   let side_effect_obj =
     if propagator_by_side_effect then []
@@ -213,12 +255,14 @@ and translate_taint_propagator
     | Some strs ->
         [ ("replace-labels", `A (List_.map (fun s -> `String s) strs)) ]
   in
+  let parameter_obj = translate_callback_parameter propagator_to_parameter in
   let from_obj = [ ("from", `String (fst from)) ] in
   let to_obj = [ ("to", `String (fst to_)) ] in
   `O
     (List_.flatten
        [
-         prop_f;
+         parameter_obj; model_obj; member_obj; preserves_obj;
+        prop_f;
          side_effect_obj;
          from_obj;
          to_obj;
diff --git a/src/parsing/Check_pattern.ml b/src/parsing/Check_pattern.ml
index 554213a..820cc3b 100644
--- a/src/parsing/Check_pattern.ml
+++ b/src/parsing/Check_pattern.ml
@@ -32,6 +32,9 @@ let lang_has_no_dollar_ids =
     | Xml
     | Yaml ->
         true
+    | Groovy
+    | Powershell
+    | Perl
     | Bash
     | Cairo
     | Clojure
diff --git a/src/parsing/Parse_rule.ml b/src/parsing/Parse_rule.ml
index 7d9ee2b..aec5617 100644
--- a/src/parsing/Parse_rule.ml
+++ b/src/parsing/Parse_rule.ml
@@ -269,6 +269,69 @@ let parse_taint_requires env key x =
   Ok { R.precondition; range }
 
 (* TODO: can add a case where these take in only a single string *)
+let parse_taint_value_type env key value =
+  let/ name = parse_string env key value in
+  match name with
+  | "string-or-null" -> Ok R.StringOrNull
+  | "posix-shell-word" -> Ok R.PosixShellWord
+  | _ -> error_at_key env.id key "unknown taint value type"
+
+let parse_model_type env key value =
+  match value.G.e with
+  | G.L (String (_, (name, _), _)) when String.length name > 0 && String.length name <= 128 -> Ok name
+  | _ -> error_at_key env.id key "model type must be a nonempty string of at most 128 bytes"
+
+let parse_model_constraint env key value =
+  let/ dict = parse_dict env key value in
+  let/ variable = take_key dict env parse_string "metavariable" in
+  let/ name = take_key dict env parse_model_type "type" in
+  let/ member = take_opt dict env parse_model_type "member" in
+  if Hashtbl.length dict.h > 0 || not (Mvar.is_metavar_name variable)
+  then error_at_key env.id key "model constraint requires a metavariable and a type"
+  else Ok (variable, name, member)
+
+let parse_callback_parameter env key value =
+  let natural_number env key expression = match expression.G.e with
+    | G.L (Int (Some number, _))
+      when Int64.compare number 0L >= 0 && Int64.compare number 32L <= 0 ->
+        Ok (Int64.to_int number)
+    | G.L (Float (Some number, _))
+      when number >= 0. && number <= 32. && Float.is_integer number ->
+        Ok (int_of_float number)
+    | _ -> error_at_key env.id key "Expected an integer from 0 through 32" in
+  let boolean env key expression = match expression.G.e with
+    | G.L (Bool (value, _)) -> Ok value
+    | _ -> error_at_key env.id key "Expected a boolean" in
+  let field_name env key expression = match expression.G.e with
+    | G.L (String (_, (value, _), _)) -> Ok value
+    | _ -> error_at_key env.id key "Expected a string field name" in
+
+      let/ target = parse_dict env key value in
+      let/ index = take_key target env natural_number "index" in
+      let/ path = take_opt target env (fun env key value ->
+        parse_listi env key (fun env value -> field_name env key value) value) "path"
+        |> Result.map (Option.value ~default:[]) in
+      let/ arities = take_opt target env (fun env key value ->
+        parse_listi env key (fun env value -> natural_number env key value) value) "arities" in
+      let/ array_elements = take_opt target env boolean "array-elements"
+        |> Result.map (Option.value ~default:false) in
+      let/ array_head_callable = take_opt target env boolean "array-head-callable"
+        |> Result.map (Option.value ~default:false) in
+      let/ preserves_array = take_opt target env boolean "preserves-array"
+        |> Result.map (Option.value ~default:false) in
+      let/ () = if (array_head_callable || preserves_array) && not array_elements
+        then error_at_key env.id key "Array qualifiers require array-elements"
+        else Ok () in
+      let/ () = if Hashtbl.length target.h > 0
+        then error_at_key env.id key "Unknown or duplicate to-parameter properties"
+        else Ok () in
+      if Option.fold ~none:false ~some:(fun values -> List.is_empty values || List.length values > 33
+          || List.exists (fun count -> count < 0 || count > 32) values) arities
+        || index < 0 || index > 31 || List.length path > 16
+        || List.exists (fun field -> String.equal field "" || String.length field > 128) path
+      then error_at_key env.id key "to-parameter requires index 0..31 and up to 16 nonempty field names"
+      else Ok { R.index; path; arities; array_elements; array_head_callable; preserves_array }
+
 let parse_taint_source ~(is_old : bool) env (key : key) (value : G.expr) :
     (Rule.taint_source, Rule_error.t) result =
   let source_id = "source:" ^ String.concat ":" env.path in
@@ -277,14 +340,31 @@ let parse_taint_source ~(is_old : bool) env (key : key) (value : G.expr) :
       take_opt dict env parse_bool "exact"
       |> Result.map (Option.value ~default:false)
     in
+    let/ source_to_parameter = take_opt dict env parse_callback_parameter "to-parameter" in
+    let/ source_model_type = take_opt dict env parse_model_type "model-type" in
+    let/ source_model_singleton = take_opt dict env (fun env key expression -> match expression.G.e with
+      | G.L (Bool (value, _)) -> Ok value
+      | _ -> error_at_key env.id key "Expected a boolean") "model-singleton"
+      |> Result.map (Option.value ~default:false) in
+    let/ () = if source_model_singleton && Option.is_none source_model_type
+      then error_at_key env.id key "model-singleton requires model-type" else Ok () in
+    let/ source_requires_model_type = take_opt dict env parse_model_constraint "requires-model-type" in
+    let/ () = if Option.is_some source_model_type && not source_exact then error_at_key env.id key "model-type requires an exact source" else Ok () in
+    let/ source_value_type = take_opt dict env parse_taint_value_type "value-type" in
+    let/ () = if Option.is_some source_model_type && Option.is_some source_value_type then error_at_key env.id key "model-type and value-type are mutually exclusive" else Ok () in
     let/ source_by_side_effect =
       take_opt dict env parse_by_side_effect "by-side-effect"
       |> Result.map (Option.value ~default:R.No)
     in
+    let/ () = if Option.is_some source_value_type && (not source_exact || not (source_by_side_effect =*= R.No))
+      then error_at_key env.id key "value-type requires exact, value-only source semantics" else Ok () in
     let/ source_control =
       take_opt dict env parse_bool "control"
       |> Result.map (Option.value ~default:false)
     in
+    let/ () = if source_control && (Option.is_some source_model_type || Option.is_some source_value_type)
+      then error_at_key env.id key "Value types cannot annotate a control source" else Ok () in
+    let/ () = if Option.is_some source_to_parameter && (not source_exact || source_control || not (source_by_side_effect =*= R.No) || Option.is_some source_model_type || Option.is_some source_value_type) then error_at_key env.id key "callback sources require exact, value-only semantics without a model or scalar type" else Ok () in
     let/ label =
       take_opt dict env parse_string "label"
       |> Result.map (Option.value ~default:R.default_source_label)
@@ -296,6 +376,11 @@ let parse_taint_source ~(is_old : bool) env (key : key) (value : G.expr) :
         R.source_id;
         source_formula;
         source_exact;
+        source_value_type;
+        source_model_type;
+        source_model_singleton;
+        source_to_parameter;
+        source_requires_model_type;
         source_by_side_effect;
         source_control;
         label;
@@ -316,6 +401,11 @@ let parse_taint_source ~(is_old : bool) env (key : key) (value : G.expr) :
             Rule.source_id;
             source_formula;
             source_exact = false;
+            source_value_type = None;
+            source_model_type = None;
+            source_model_singleton = false;
+            source_to_parameter = None;
+            source_requires_model_type = None;
             source_by_side_effect = R.No;
             source_control = false;
             label = R.default_source_label;
@@ -332,10 +422,24 @@ let parse_taint_propagator ~(is_old : bool) env (key : key) (value : G.expr) :
     else Parse_rule_formula.parse_formula_from_dict
   in
   let parse_from_dict dict f =
+    let/ propagator_from_model_type = take_opt dict env parse_model_type "from-model-type" in
+    let/ propagator_from_model_member = take_opt dict env parse_model_type "from-model-member" in
+    let/ () = if Option.is_some propagator_from_model_member && Option.is_none propagator_from_model_type then error_at_key env.id key "from-model-member requires from-model-type" else Ok () in
+    let/ propagator_preserves_model_type = take_opt dict env (fun env key expression ->
+      match expression.G.e with
+      | G.L (Bool (value, _)) -> Ok value
+      | _ -> error_at_key env.id key "Expected a boolean") "preserves-model-type"
+      |> Result.map (Option.value ~default:false) in
+    let/ () = if propagator_preserves_model_type && Option.is_none propagator_from_model_type
+      then error_at_key env.id key "preserves-model-type requires from-model-type" else Ok () in
+    let/ propagator_to_parameter = take_opt dict env parse_callback_parameter "to-parameter" in
     let/ propagator_by_side_effect =
       take_opt dict env parse_bool "by-side-effect"
-      |> Result.map (Option.value ~default:true)
+      |> Result.map (Option.value ~default:(Option.is_none propagator_to_parameter))
     in
+    let/ () = if Option.is_some propagator_to_parameter && propagator_by_side_effect
+      then error_at_key env.id key "to-parameter cannot mutate the callback value"
+      else Ok () in
     let/ from = take_key dict env parse_string_wrap "from" in
     let/ to_ = take_key dict env parse_string_wrap "to" in
     let/ propagator_requires =
@@ -354,6 +458,10 @@ let parse_taint_propagator ~(is_old : bool) env (key : key) (value : G.expr) :
         R.propagator_id;
         propagator_formula;
         propagator_by_side_effect;
+        propagator_to_parameter;
+        propagator_from_model_type;
+        propagator_from_model_member;
+        propagator_preserves_model_type;
         from;
         to_;
         propagator_requires;
@@ -371,10 +479,20 @@ let parse_taint_sanitizer ~(is_old : bool) env (key : key) (value : G.expr) =
       take_opt dict env parse_bool "exact"
       |> Result.map (Option.value ~default:false)
     in
+    let/ sanitizer_output_type = take_opt dict env parse_taint_value_type "output-value-type" in
+    let/ sanitizer_requires_type = take_opt dict env (fun env key value ->
+      let/ constraint_ = parse_dict env key value in
+      let/ variable = take_key constraint_ env parse_string "metavariable" in
+      let/ kind = take_key constraint_ env parse_taint_value_type "type" in
+      if Mvar.is_metavar_name variable then Ok (variable, kind)
+      else error_at_key env.id key "requires-value-type requires a metavariable") "requires-value-type" in
     let/ sanitizer_by_side_effect =
       take_opt dict env parse_bool "by-side-effect"
       |> Result.map (Option.value ~default:false)
     in
+    let/ () = if (Option.is_some sanitizer_output_type || Option.is_some sanitizer_requires_type)
+      && (not sanitizer_exact || sanitizer_by_side_effect)
+      then error_at_key env.id key "value-type models require exact, value-only sanitizer semantics" else Ok () in
     let/ not_conflicting =
       take_opt dict env parse_bool
         (if is_old then "not_conflicting" else "not-conflicting")
@@ -387,6 +505,8 @@ let parse_taint_sanitizer ~(is_old : bool) env (key : key) (value : G.expr) =
           sanitizer_id;
           sanitizer_formula;
           sanitizer_exact;
+          sanitizer_output_type;
+          sanitizer_requires_type;
           sanitizer_by_side_effect;
           not_conflicting;
         }
@@ -405,6 +525,8 @@ let parse_taint_sanitizer ~(is_old : bool) env (key : key) (value : G.expr) =
             sanitizer_id;
             sanitizer_formula;
             sanitizer_exact = false;
+            sanitizer_output_type = None;
+            sanitizer_requires_type = None;
             sanitizer_by_side_effect = false;
             R.not_conflicting = false;
           }
@@ -420,6 +542,7 @@ let parse_taint_sink ~(is_old : bool) env (key : key) (value : G.expr) :
       take_opt dict env parse_bool "at-exit"
       |> Result.map (Option.value ~default:false)
     in
+    let/ sink_requires_model_type = take_opt dict env parse_model_constraint "requires-model-type" in
     let/ sink_exact =
       take_opt dict env parse_bool "exact"
       |> Result.map (Option.value ~default:true)
@@ -433,6 +556,7 @@ let parse_taint_sink ~(is_old : bool) env (key : key) (value : G.expr) :
           sink_formula;
           sink_exact;
           sink_requires;
+          sink_requires_model_type;
           sink_at_exit;
           sink_has_focus;
         }
@@ -454,6 +578,7 @@ let parse_taint_sink ~(is_old : bool) env (key : key) (value : G.expr) :
               sink_formula;
               sink_exact = true;
               sink_requires = None;
+              sink_requires_model_type = None;
               sink_at_exit = false;
               sink_has_focus;
             }
diff --git a/src/parsing/Parse_rule_formula.ml b/src/parsing/Parse_rule_formula.ml
index 274f0f7..a6dc62b 100644
--- a/src/parsing/Parse_rule_formula.ml
+++ b/src/parsing/Parse_rule_formula.ml
@@ -236,7 +236,7 @@ type extra =
   | MetavarPattern of MV.mvar * Xlang.t option * Rule.formula
   | MetavarComparison of metavariable_comparison
   | MetavarAnalysis of MV.mvar * Rule.metavar_analysis_kind
-  | MetavarName of MV.mvar * Rule.metavar_name_kind option * string list option
+  | MetavarName of MV.mvar * Rule.metavar_name_kind option * string list option * string list option
 
 (* old: | PatWherePython of string, but it was too dangerous.
  * MetavarComparison is not as powerful, but safer.
@@ -440,8 +440,15 @@ and parse_pair_old env ((key, value) : key * G.expr) :
                     | Some true -> rewrite_metavar_comparison_strip comparison
                     | _ -> comparison)
               | MetavarAnalysis (mvar, kind) -> R.CondAnalysis (mvar, kind)
-              | MetavarName (mvar, kind, modules) ->
-                  R.CondName { mvar; kind; modules }
+              | MetavarName (mvar, kind, modules, symbol) ->
+                  R.CondName { mvar; kind; modules; symbol }
+            in
+            let/ name_constraint =
+              match (H.dict_take_opt dict "metavariable-name",
+                     H.dict_take_opt dict "semgrep-internal-metavariable-name") with
+              | Some _, Some _ -> error_at_expr env.id expr "duplicate name constraint"
+              | Some value, None | None, Some value -> Ok (Some value)
+              | None, None -> Ok None
             in
             match
               ( H.dict_take_opt dict "focus-metavariable",
@@ -450,7 +457,7 @@ and parse_pair_old env ((key, value) : key * G.expr) :
                 H.dict_take_opt dict "metavariable-type",
                 H.dict_take_opt dict "metavariable-pattern",
                 H.dict_take_opt dict "metavariable-comparison",
-                H.dict_take_opt dict "semgrep-internal-metavariable-name" )
+                name_constraint )
             with
             | None, None, None, None, None, None, None ->
                 let+ nested_formula = get_nested_formula_in_list env i expr in
@@ -672,23 +679,44 @@ and parse_extra (env : env) (key : key) (value : G.expr) :
         | __else__ -> Ok ()
       in
       Ok (MetavarComparison { metavariable; comparison; strip; base })
+  | "metavariable-name"
   | "semgrep-internal-metavariable-name" -> (
       let/ mv_name_dict = parse_dict env key value in
       let/ mvar = take_key mv_name_dict env parse_string "metavariable" in
       let parse_kind = function
         | "django-view", _ -> Ok R.DjangoView
+        | "unbound", _ -> Ok R.UnboundName
         | str, _ -> error_at_key env.id key ("unsupported kind: " ^ str)
       in
       let/ kind_str = take_opt mv_name_dict env parse_string_wrap "kind" in
+      let/ () =
+        if fst key = "metavariable-name"
+          && Option.fold ~none:false ~some:(fun (kind, _) -> kind <> "unbound") kind_str then
+          error_at_key env.id key "metavariable-name accepts module, modules, or kind: unbound"
+        else Ok ()
+      in
       let/ module_ = take_opt mv_name_dict env parse_string "module" in
       let/ modules =
         take_opt mv_name_dict env
           (fun e k -> parse_list e k (fun e -> parse_string e k))
           "modules"
       in
+      let/ symbol = take_opt mv_name_dict env (fun env key value ->
+        let/ fields = parse_list env key (fun env value -> match value.G.e with
+          | G.L (G.String (_, (name, _), _)) when name <> "" -> Ok name
+          | _ -> error_at_key env.id key "Expected a nonempty export name") value in
+        if List.length fields > 16 then error_at_key env.id key "Export path exceeds 16 components"
+        else Ok fields) "symbol" in
+      let/ () = if Option.is_some symbol && Option.is_none module_ && Option.is_none modules
+        then error_at_key env.id key "symbol requires module or modules" else Ok () in
+      let/ () = if Hashtbl.length mv_name_dict.h > 0
+        then error_at_key env.id key "Unknown or duplicate name constraint properties" else Ok () in
       match (kind_str, module_, modules) with
       | None, None, None ->
           error_at_key env.id key "expected at least one of kind, module(s)"
+      | Some ("unbound", _), (Some _), _
+      | Some ("unbound", _), _, (Some _) ->
+          error_at_key env.id key "kind: unbound cannot select a module"
       | _, Some _, Some _ ->
           error_at_key env.id key "expected only one of module, modules"
       | _ ->
@@ -702,9 +730,9 @@ and parse_extra (env : env) (key : key) (value : G.expr) :
             (MetavarName
                ( mvar,
                  kind,
-                 match modules with
+                 (match modules with
                  | None -> Option.map (fun x -> [ x ]) module_
-                 | _ -> modules )))
+                 | _ -> modules), symbol )))
   | _ -> error_at_key env.id key ("wrong parse_extra field: " ^ fst key)
 
 (*****************************************************************************)
diff --git a/src/printing/Pretty_print_AST.ml b/src/printing/Pretty_print_AST.ml
index 06f6f3c..bd279f5 100644
--- a/src/printing/Pretty_print_AST.ml
+++ b/src/printing/Pretty_print_AST.ml
@@ -213,6 +213,9 @@ and if_stmt env (tok, e, s, sopt) =
   let newline_body = F.sprintf "%s\n%s\n" in
   let format_cond, elseif_str, format_block =
     match env.lang with
+    | Lang.Groovy
+    | Lang.Powershell
+    | Lang.Perl
     | Lang.Circom
     | Lang.Cairo
     | Lang.Xml
@@ -296,6 +299,9 @@ and while_stmt env (tok, e, s) =
   let ruby_while = F.sprintf "%s %s\n %s\nend" in
   let while_format =
     match env.lang with
+    | Lang.Groovy
+    | Lang.Powershell
+    | Lang.Perl
     | Lang.Circom
     | Lang.Cairo
     | Lang.Xml
@@ -354,6 +360,9 @@ and do_while stmt env (s, e) =
   let c_do_while = F.sprintf "do %s\nwhile(%s)" in
   let do_while_format =
     match env.lang with
+    | Lang.Groovy
+    | Lang.Powershell
+    | Lang.Perl
     | Lang.Circom
     | Lang.Cairo
     | Lang.Xml
@@ -410,6 +419,9 @@ and do_while stmt env (s, e) =
 and for_stmt env (for_tok, hdr, s) =
   let for_format =
     match env.lang with
+    | Lang.Groovy
+    | Lang.Powershell
+    | Lang.Perl
     | Lang.Circom
     | Lang.Cairo
     | Lang.Xml
@@ -514,7 +526,10 @@ and def_stmt env (entity, def_kind) =
   let var_def (ent, def) =
     let no_val, with_val =
       match env.lang with
-      | Lang.Circom
+      | Lang.Groovy
+    | Lang.Powershell
+    | Lang.Perl
+    | Lang.Circom
       | Lang.Cairo
       | Lang.Xml
       | Lang.Dart
diff --git a/src/rule/Rule.ml b/src/rule/Rule.ml
index 8f000b3..4f70395 100644
--- a/src/rule/Rule.ml
+++ b/src/rule/Rule.ml
@@ -143,6 +143,7 @@ and metavar_cond =
 and metavar_cond_name = {
   mvar : Mvar.t;
   kind : metavar_name_kind option;
+  symbol : string list option;
   modules : string list option;
       (** A list of user-facing module names.
 
@@ -169,7 +170,7 @@ and entropy_analysis_mode = Lax | Default | Strict
  * instead improve the engine (e.g., finish steps mode) instead of
  * writing adhoc analysis.
  *)
-and metavar_name_kind = DjangoView
+and metavar_name_kind = DjangoView | UnboundName
 
 (* Represents all of the metavariables that are being focused by a single
    `focus-metavariable`. *)
@@ -214,6 +215,8 @@ type precondition_with_range = {
 }
 [@@deriving show]
 
+type taint_value_type = StringOrNull | PosixShellWord [@@deriving show, eq, ord]
+
 type by_side_effect = Only | Yes | No [@@deriving show]
 
 (* The sources/sanitizers/sinks used to be a simple 'formula list',
@@ -234,6 +237,11 @@ and taint_source = {
       * `ok` inside `source(sink(ok))` is considered tainted, and `sink(ok)` is
       * reported. If 'true', only the entire `source(sink(ok))` expression will be
       * considered tainted, and `sink(ok)` is fine. *)
+  source_value_type : taint_value_type option;
+  source_model_type : string option;
+  source_model_singleton : bool;
+  source_to_parameter : callback_parameter option;
+  source_requires_model_type : (string * string * string option) option;
   source_by_side_effect : by_side_effect;
   source_control : bool;
   label : string;
@@ -270,6 +278,8 @@ and taint_sanitizer = {
       * and `sink(tainted)` is fine. If 'true', only the entire expression
       * `sanitize(sink(tainted))` will be considered sanitized, and `sink(tainted)`
       * will be reported. *)
+  sanitizer_output_type : taint_value_type option;
+  sanitizer_requires_type : (string * taint_value_type) option;
   sanitizer_by_side_effect : bool;
   not_conflicting : bool;
       (* If [not_conflicting] is enabled, the sanitizer cannot conflict with
@@ -296,6 +306,7 @@ and taint_sink = {
       * sink, and nothing is reported. If 'false', then every subexpression in
       * `sink(if tainted then ok1 else ok2)` is considered a sink, and we report
       * a finding due to `tainted`. *)
+  sink_requires_model_type : (string * string * string option) option;
   sink_requires : precondition_with_range option;
       (* A Boolean expression over taint labels. See also 'taint_source'.
        * The sink will only trigger a finding if the data that reaches it
@@ -317,10 +328,16 @@ and taint_sink = {
  * with "from" being `$X` and "to" being `$H`. So if `$X` is tainted then `$H`
  * will also be marked as tainted.
  *)
+and callback_parameter = { index : int; path : string list; arities : int list option; array_elements : bool; array_head_callable : bool; preserves_array : bool }
+
 and taint_propagator = {
   propagator_id : string;
   propagator_formula : formula;
   propagator_by_side_effect : bool;
+  propagator_to_parameter : callback_parameter option;
+  propagator_from_model_type : string option;
+  propagator_from_model_member : string option;
+  propagator_preserves_model_type : bool;
   from : Mvar.t wrap;
   to_ : Mvar.t wrap;
   propagator_requires : precondition_with_range option;
diff --git a/src/tainting/Builtin_models.ml b/src/tainting/Builtin_models.ml
index 5bd1448..937bc9b 100644
--- a/src/tainting/Builtin_models.ml
+++ b/src/tainting/Builtin_models.ml
@@ -7,7 +7,7 @@ let make_callback_var () =
   {
     IL.ident = ("callback", Tok.unsafe_fake_tok "callback");
     sid = AST_generic.SId.unsafe_default;
-    id_info = AST_generic.empty_id_info ();
+    id_info = AST_generic.empty_id_info (); value_origin = None;
   }
 
 (** Synthetic [params_il] for builtin signatures. Builtins have no
@@ -62,7 +62,7 @@ let add_hof_returning_function_signatures db method_names ?(taint_arg_index = 0)
   let params = [ Signature.P "callback" ] in
   let returned_fun_sig =
     {
-      Signature.params;
+      Signature.closures = IL.NameSet.empty; params;
       params_il = synthetic_params_il params;
       effects = Effects.singleton hof_effect;
     }
@@ -81,7 +81,7 @@ let add_hof_returning_function_signatures db method_names ?(taint_arg_index = 0)
   in
   let method_sig =
     {
-      Signature.params = [];
+      Signature.closures = IL.NameSet.empty; params = [];
       params_il = synthetic_params_il [];
       effects = Effects.singleton return_effect;
     }
@@ -153,7 +153,7 @@ let add_function_hof_signatures db function_names arity ?(callback_index = 0)
 
   let hof_sig =
     {
-      Signature.params;
+      Signature.closures = IL.NameSet.empty; params;
       params_il = synthetic_params_il params;
       effects = Effects.of_list [ hof_effect; return_effect ];
     }
@@ -227,7 +227,7 @@ let add_hof_signatures db method_names arity ?(callback_index = 0)
 
   let hof_sig =
     {
-      Signature.params;
+      Signature.closures = IL.NameSet.empty; params;
       params_il = synthetic_params_il params;
       effects = Effects.of_list [ hof_effect; return_effect ];
     }
@@ -285,8 +285,7 @@ let clojure_hof_effects ~arity ~callback_index ~data_index ~taint_arg_index =
       {
         ident = ("impl", fake_tok);
         sid = AST_generic.SId.unsafe_default;
-        id_info = AST_generic.empty_id_info ();
-      }
+        id_info = AST_generic.empty_id_info (); value_origin = None }
     in
     let impl_fetch : IL.exp =
       {
@@ -384,6 +383,7 @@ let add_function_hof_signatures_clojure db (grouped : (string * Lang_config.hof_
       let hof_sig =
         {
           Signature.params;
+          closures = IL.NameSet.empty;
           params_il = synthetic_params_il params;
           effects = Effects.of_list effects;
         }
@@ -429,12 +429,12 @@ let create_builtin_models (lang : Lang.t) : builtin_signature_database =
 
 let this_taint_set () =
   let taint = Taint.{ orig = Var { base = BThis; offset = [] }; tokens = [] } in
-  Taint.Taint_set.singleton taint
+  Taint.Taint_set.of_taint_list [taint; {taint with orig = Taint.Shape_var { base = BThis; offset = [] }}]
 
 let arg_taint_set index =
   let arg = { Taint.name = "value"; index } in
   let taint = Taint.{ orig = Var { base = BArg arg; offset = [] }; tokens = [] } in
-  Taint.Taint_set.singleton taint
+  Taint.Taint_set.of_taint_list [taint; {taint with orig = Taint.Shape_var { base = BArg arg; offset = [] }}]
 
 let return_effect taint_set =
   Effect.ToReturn
@@ -457,7 +457,7 @@ let to_lval_this taint_set =
 let add_method_signatures db method_names arity effects =
   let params = List.init arity (fun _ -> Signature.Other) in
   let sig_ =
-    { Signature.params; params_il = synthetic_params_il params; effects }
+    { Signature.closures = IL.NameSet.empty; params; params_il = synthetic_params_il params; effects }
   in
   List.fold_left
     (fun acc_db name -> add_builtin_signature acc_db name { sig_; arity = Arity_exact arity })
diff --git a/src/tainting/Callback_extraction.ml b/src/tainting/Callback_extraction.ml
index 045490c..219d35b 100644
--- a/src/tainting/Callback_extraction.ml
+++ b/src/tainting/Callback_extraction.ml
@@ -107,7 +107,7 @@ let rec extract_callbacks_from_arg ~(lang : Lang.t) (arg_expr : G.expr) :
           {
             ident = atom_ident;
             sid = G.SId.unsafe_default;
-            id_info = G.empty_id_info ();
+            id_info = G.empty_id_info (); value_origin = None;
           }
       in
       [ (synthetic_name, snd atom_ident, None) ]
diff --git a/src/tainting/Dataflow_tainting.ml b/src/tainting/Dataflow_tainting.ml
index 5d8ed29..924bf68 100644
--- a/src/tainting/Dataflow_tainting.ml
+++ b/src/tainting/Dataflow_tainting.ml
@@ -107,6 +107,9 @@ type func = {
           [ToLval] consumer's bound-vs-free check. *)
   best_matches : TM.Best_matches.t;
       (** Best matches for the taint sources/etc, see 'Taint_spec_match'. *)
+  lambda_bodies : IL.lambdas_cfgs;
+  active_lambdas : IL.NameSet.t;
+  deferred_functions : IL.NameSet.t;
   used_lambdas : IL.NameSet.t;
       (** Set of lambda names that are *used* within the function. If a lambda
           is used, we analyze it at use-site, otherwise we analyze it at def
@@ -134,6 +137,8 @@ type env = {
           current pass. Used to gate the outer self-sig convergence loop in
           [fixpoint_aux]: a pass without any self-recursive call needs no
           retry. *)
+  callback_requests : Taint_callback_models.request list ref;
+  model_preservations : Range.t list ref;
   signature_db : Shape_and_sig.signature_database option;
       (** Signature database for inter-procedural taint analysis *)
   builtin_signature_db : Shape_and_sig.builtin_signature_database option;
@@ -223,16 +228,53 @@ let gather_all_taints_in_args_taints args_taints =
              Shape.gather_all_taints_in_shape shape |> Taints.union acc)
        Taints.empty
 
+let sanitizer_type_holds env (m : R.taint_sanitizer TM.t) =
+  match m.spec.sanitizer_requires_type with
+  | None -> true
+  | Some (variable, kind) -> (
+      match List.assoc_opt variable m.spec_pm.env with
+      | Some value -> (
+          match Metavariable.mvalue_to_any value with
+          | G.E { e = G.N name; _ } -> (
+              let lval = LV.lval_of_var (AST_to_IL.var_of_name name) in
+              match Lval_env.find_lval env.taint_inst.lang env.lval_env lval with
+              | Some (S.Cell (_, S.Scalar actual)) ->
+                  R.equal_taint_value_type actual kind
+              | _ -> false)
+          | _ -> false)
+      | None -> false)
+
+let modeled_sanitizer_shape env orig original =
+  let matches = env.taint_inst.preds.is_sanitizer (any_of_orig orig) in
+  match
+    List.find_opt
+      (fun (m : R.taint_sanitizer TM.t) ->
+        m.spec.sanitizer_exact && TM.is_exact m
+        && Option.is_some m.spec.sanitizer_output_type
+        && sanitizer_type_holds env m)
+      matches
+  with
+  | Some m -> (
+      match m.spec.sanitizer_output_type with
+      | Some kind -> S.Scalar kind
+      | None -> original)
+  | None -> original
+
 let any_is_best_sanitizer env any =
   env.taint_inst.preds.is_sanitizer any
   |> List.filter (fun (m : R.taint_sanitizer TM.t) ->
-         (not m.spec.sanitizer_exact)
-         || TM.is_best_match env.func.best_matches m)
+         Option.is_none m.spec.sanitizer_output_type
+         && sanitizer_type_holds env m
+         && ((not m.spec.sanitizer_exact)
+            || TM.is_best_match env.func.best_matches m))
 
 (* TODO: We could return source matches already split by `by-side-effect` here ? *)
 let any_is_best_source ?(is_lval = false) env any =
   env.taint_inst.preds.is_source any
   |> List.filter (fun (m : R.taint_source TM.t) ->
+         Taint_model_types.constraint_holds env.taint_inst.lang env.lval_env m.spec_pm.env
+           m.spec.source_requires_model_type
+         &&
          match m.spec.source_by_side_effect with
          | Only -> is_lval && TM.is_exact m
          (* 'Yes' should probably require an exact match like 'Only' but for
@@ -246,7 +288,10 @@ let any_is_best_sink env any =
   env.taint_inst.preds.is_sink any
   |> List.filter (fun (tm : R.taint_sink TM.t) ->
          (* at-exit sinks are handled in 'check_tainted_at_exit_sinks' *)
-         (not tm.spec.sink_at_exit) && TM.is_best_match env.func.best_matches tm)
+         (not tm.spec.sink_at_exit)
+         && TM.is_best_match env.func.best_matches tm
+         && Taint_model_types.constraint_holds env.taint_inst.lang env.lval_env tm.spec_pm.env
+              tm.spec.sink_requires_model_type)
 
 let orig_is_source (taint_inst : Taint_rule_inst.t) orig =
   taint_inst.preds.is_source (any_of_orig orig)
@@ -289,12 +334,16 @@ let lval_is_sink env lval =
   sinks
   |> List.filter (fun (tm : R.taint_sink TM.t) ->
          (* at-exit sinks are handled in 'check_tainted_at_exit_sinks' *)
-         not tm.spec.sink_at_exit)
+         (not tm.spec.sink_at_exit)
+         && Taint_model_types.constraint_holds env.taint_inst.lang env.lval_env tm.spec_pm.env
+              tm.spec.sink_requires_model_type)
 [@@profiling]
 
 let taints_of_matches env ~incoming sources =
   let control_sources, data_sources =
     sources
+    |> List.filter (fun (m : R.taint_source TM.t) ->
+           Option.is_none m.spec.source_to_parameter)
     |> List.partition (fun (m : R.taint_source TM.t) -> m.spec.source_control)
   in
   (* THINK: It could make sense to merge `incoming` with `control_incoming`, so
@@ -365,8 +414,9 @@ let unify_mvars_sets env mvars1 mvars2 =
         let* xs = xs_opt in
         match List.assoc_opt mvar mvars2 with
         | None -> Some ((mvar, mval) :: xs)
-        | Some mval' when Matching_generic.equal_ast_bound_code
-                            env.taint_inst.options mval mval' ->
+        | Some mval'
+          when Matching_generic.equal_ast_bound_code env.taint_inst.options mval
+                 mval' ->
             Some ((mvar, mval) :: xs)
         | _ -> None)
       (Some []) mvars1
@@ -750,28 +800,27 @@ let effects_of_call_func_arg fun_exp fun_shape args_taints =
             (S.show_shape fun_shape));
       []
 
-
 let get_signature_for_object graph caller_node db method_name arity =
   let caller = Option.map Function_id.of_il_name caller_node in
   let method_tok = Function_id.tok method_name in
   (* Look up via method name token — call graph edges for DotAccess calls
      are stored at the method token position (see extract_calls). *)
   match Call_graph.lookup_callee_from_graph graph caller method_tok with
-  | Some callee_node ->
-      Shape_and_sig.(lookup_signature db callee_node arity)
+  | Some callee_node -> Shape_and_sig.(lookup_signature db callee_node arity)
   | None -> Shape_and_sig.lookup_signature db method_name arity
 
 (* Helper to fallback to builtin signature database if regular lookup fails *)
 let try_builtin_fallback env func_name arity result =
   match result with
   | Some _ -> result
-  | None ->
-      (match env.builtin_signature_db with
+  | None -> (
+      match env.builtin_signature_db with
       | Some builtin_db ->
-          let builtin_result = Shape_and_sig.(lookup_builtin_signature builtin_db func_name arity) in
+          let builtin_result =
+            Shape_and_sig.(lookup_builtin_signature builtin_db func_name arity)
+          in
           Log.debug (fun m ->
-              m "TAINT_SIG: Builtin lookup for %s: %s"
-                func_name
+              m "TAINT_SIG: Builtin lookup for %s: %s" func_name
                 (if Option.is_some builtin_result then "FOUND" else "NOT FOUND"));
           builtin_result
       | None -> None)
@@ -829,7 +878,8 @@ let ruby_method_ref_atom (svalue : G.svalue option) =
 let lookup_signature_with_object_context env fun_exp arity =
   Log.debug (fun m ->
       m "TAINT_SIG_LOOKUP: Looking up %s with arity %d"
-        (Display_IL.string_of_exp fun_exp) arity);
+        (Display_IL.string_of_exp fun_exp)
+        arity);
   match env.signature_db with
   | None ->
       Log.debug (fun m -> m "TAINT_SIG: No signature database available");
@@ -850,7 +900,7 @@ let lookup_signature_with_object_context env fun_exp arity =
           let synthetic_name : IL.name =
             { ident = atom_ident;
               sid = G.SId.unsafe_default;
-              id_info = G.empty_id_info () }
+              id_info = G.empty_id_info (); value_origin = None }
           in
           lookup_bare_function_name env db synthetic_name arity
       | Fetch { base = Var name; rev_offset = [] } ->
@@ -864,21 +914,20 @@ let lookup_signature_with_object_context env fun_exp arity =
           (* Method call on self/this: self.method() or this.method() *)
           let method_tok = snd method_name.IL.ident in
           match
-            Call_graph.lookup_callee_from_graph
-              env.call_graph
+            Call_graph.lookup_callee_from_graph env.call_graph
               (Option.map Function_id.of_il_name env.func.name)
               method_tok
           with
           | Some callee_node ->
               Shape_and_sig.(lookup_signature db callee_node arity)
           | None ->
-              Shape_and_sig.lookup_signature db (Function_id.of_il_name method_name) arity)
-      | Fetch { base = Var obj; rev_offset = [ { o = Dot method_name; _ } ] } -> (
+              Shape_and_sig.lookup_signature db
+                (Function_id.of_il_name method_name)
+                arity)
+      | Fetch { base = Var obj; rev_offset = [ { o = Dot method_name; _ } ] }
+        -> (
           match
-            get_signature_for_object
-              env.call_graph
-              env.func.name
-              db
+            get_signature_for_object env.call_graph env.func.name db
               (Function_id.of_il_name method_name)
               arity
           with
@@ -887,14 +936,23 @@ let lookup_signature_with_object_context env fun_exp arity =
               (* Fallback: try qualified function name (Module.function for Elixir, etc.) *)
               let qualified_name =
                 {
-                  ident = (fst obj.ident ^ "." ^ fst method_name.ident, snd method_name.ident);
+                  ident =
+                    ( fst obj.ident ^ "." ^ fst method_name.ident,
+                      snd method_name.ident );
                   sid = method_name.sid;
                   id_info = method_name.id_info;
+                  value_origin = None;
                 }
               in
-              let result = Shape_and_sig.lookup_signature db (Function_id.of_il_name qualified_name) arity in
+              let result =
+                Shape_and_sig.lookup_signature db
+                  (Function_id.of_il_name qualified_name)
+                  arity
+              in
               (* Try builtin fallback - first with qualified name, then with just method name *)
-              let result = try_builtin_fallback env (fst qualified_name.ident) arity result in
+              let result =
+                try_builtin_fallback env (fst qualified_name.ident) arity result
+              in
               try_builtin_fallback env (fst method_name.ident) arity result)
       | _ -> None)
 
@@ -917,6 +975,7 @@ let self_sig_if_recursive env fun_exp =
           Some
             {
               Signature.params = env.func.sig_params;
+              closures = Option.fold ~none:IL.NameSet.empty ~some:IL.NameSet.singleton env.func.name;
               params_il = env.func.il_params;
               effects = !(env.effects_acc);
             }
@@ -962,20 +1021,29 @@ let lambdas_to_analyze_in_node env lambdas node =
     in
     let* lval = LV.lval_of_instr_opt instr in
     let* ((lname, _) as lambda) = LV.lval_is_lambda lambdas lval in
-    if IL.NameSet.mem lname env.func.used_lambdas then None else Some lambda
+    if
+      IL.NameSet.mem lname env.func.used_lambdas
+      || Option.is_none env.in_lambda
+         && IL.NameSet.mem lname env.func.deferred_functions
+    then None
+    else Some lambda
   in
   Option.to_list unused_lambda_def @ lambdas_used_in_node lambdas node
 
 (* Collect ALL lambdas recursively from a fun_cfg, in innermost-first order.
    This ensures nested lambda signatures are extracted before their parents. *)
-let rec collect_all_lambdas_innermost_first (fun_cfg : IL.fun_cfg)
-    : (IL.name * IL.fun_cfg) list =
-  IL.NameMap.fold (fun name lcfg results ->
-    (* First collect nested lambdas from this lambda *)
-    let nested = collect_all_lambdas_innermost_first lcfg in
-    (* Then add this lambda after its nested ones *)
-    results @ nested @ [(name, lcfg)]
-  ) fun_cfg.lambdas []
+let rec collect_all_lambdas_innermost_first (fun_cfg : IL.fun_cfg) :
+    (IL.name * IL.fun_cfg) list =
+  IL.NameMap.fold
+    (fun name lcfg results ->
+      (* First collect nested lambdas from this lambda *)
+      let nested = collect_all_lambdas_innermost_first lcfg in
+      (* Then add this lambda after its nested ones *)
+      results @ nested @ [ (name, lcfg) ])
+    fun_cfg.lambdas []
+
+let available_lambdas body =
+  collect_all_lambdas_innermost_first body |> List.to_seq |> IL.NameMap.of_seq
 
 (*****************************************************************************)
 (* Miscellaneous *)
@@ -999,7 +1067,7 @@ let check_orig_if_sink env ?filter_sinks orig taints shape =
       let range_str =
         let any =
           match orig with
-          | IL.SameAs e -> G.E e
+          | IL.SameAs e | IL.ValueOf e -> G.E e
           | IL.Related a -> a
           | IL.NoOrig -> G.Anys []
         in
@@ -1064,6 +1132,49 @@ let exp_is_sanitized env exp =
 (* Checks if `thing' is a propagator `from' and if so propagates `taints' through it.
    Checks if `thing` is a propagator `'to' and if so fetches any taints that had been
    previously propagated. Returns *only* the newly propagated taint. *)
+let queue_callback env prop shape parameter =
+  let closures, limitation = Taint_callback_models.targets parameter shape in
+  env.callback_requests :=
+    {
+      Taint_callback_models.closures;
+      limitation;
+      value_range = prop.TM.range;
+      parameter;
+      origin = Taint_callback_models.Propagation prop.TM.spec.TRI.var;
+      location = fst prop.spec_pm.range_loc;
+    }
+    :: !(env.callback_requests)
+
+let queue_callback_sources env incoming shape matches =
+  List.iter
+    (fun (source : R.taint_source TM.t) ->
+      match source.spec.source_to_parameter with
+      | Some parameter when TM.is_exact source ->
+          let taints =
+            T.taints_of_pms ~incoming [ (source.spec_pm, source.spec) ]
+          in
+          if not (Taints.is_empty taints) then
+            let closures, limitation =
+              Taint_callback_models.targets parameter shape
+            in
+            env.callback_requests :=
+              {
+                Taint_callback_models.closures;
+                limitation;
+                value_range = source.range;
+                parameter;
+                origin = Taint_callback_models.Source taints;
+                location = fst source.spec_pm.range_loc;
+              }
+              :: !(env.callback_requests)
+      | _ -> ())
+    matches
+
+let any_of_model_lval = function
+  | { base = Var { value_origin = Some value; _ }; rev_offset = [] } ->
+      G.E value
+  | lval -> any_of_lval lval
+
 let handle_taint_propagators env thing taints shape =
   (* We propagate taints via an auxiliary variable (the propagator id). This is
    * simple but it has limitations. It works well to propagate "forward" and,
@@ -1087,8 +1198,8 @@ let handle_taint_propagators env thing taints shape =
   let propagators =
     let any =
       match thing with
-      | `Lval lval -> any_of_lval lval
-      | `Exp exp -> any_of_orig exp.eorig
+      | `Lval lval -> any_of_model_lval lval
+      | `Exp exp -> any_of_value_orig exp.eorig
       | `Ins ins -> any_of_orig ins.iorig
     in
     env.taint_inst.preds.is_propagator any
@@ -1096,6 +1207,35 @@ let handle_taint_propagators env thing taints shape =
   let propagate_froms, propagate_tos =
     List.partition (fun p -> p.TM.spec.TRI.kind =*= `From) propagators
   in
+  let propagate_froms =
+    List.filter
+      (fun prop ->
+        match prop.TM.spec.TRI.prop.propagator_from_model_type with
+        | None -> true
+        | Some name ->
+            let holds =
+              Taint_model_types.has_type prop.spec_pm.env
+                prop.spec.prop.propagator_from_model_member name shape
+            in
+            (if
+               (not holds)
+               && Taint_model_types.from_model name (`Tainted taints)
+               && T.solve_precondition ~ignore_poly_taint:false ~taints
+                    (R.get_propagator_precondition prop.spec.prop)
+                  =*= Some true
+             then
+               match shape with
+               | S.Bot
+               | S.Arg _
+               | S.Obj _ ->
+                   Taint_model_types.unresolved (fst prop.spec_pm.range_loc)
+               | S.Instance (_, fields)
+                 when Shape_and_sig.Fields.mem T.Oany fields ->
+                   Taint_model_types.unresolved (fst prop.spec_pm.range_loc)
+               | _ -> ());
+            holds)
+      propagate_froms
+  in
   let lval_env =
     (* `thing` is the source (the "from") of propagation, we add its taints to
      * the environment. *)
@@ -1134,6 +1274,9 @@ let handle_taint_propagators env thing taints shape =
             (R.get_propagator_precondition prop.TM.spec.TRI.prop)
         with
         | Some true ->
+            if prop.spec.prop.propagator_preserves_model_type then
+              env.model_preservations :=
+                prop.range :: !(env.model_preservations);
             (* If we have an output label, change the incoming taints to be
                of the new label.
                Otherwise, keep them the same.
@@ -1159,41 +1302,78 @@ let handle_taint_propagators env thing taints shape =
      * incoming taints by looking for the propagator ids in the environment. *)
     List.fold_left
       (fun (taints_in_acc, lval_env) prop ->
-        let opt_propagated, lval_env =
-          Lval_env.propagate_from prop.TM.spec.TRI.var lval_env
-        in
-        let taints_from_prop =
-          match opt_propagated with
-          | None -> Taints.empty
-          | Some taints -> taints
-        in
-        let lval_env =
-          if prop.spec.TRI.prop.propagator_by_side_effect then
-            match thing with
-            (* If `thing` is an l-value of the form `x.a.b.c`, then taint can be
-             *  propagated by side-effect. A pattern-propagator may use this to
-             * e.g. propagate taint from `x` to `y` in `f(x,y)`, so that
-             * subsequent uses of `y` are tainted if `x` was previously tainted. *)
-            | `Lval lval ->
-                if Option.is_some opt_propagated then
-                  lval_env
-                  |> Lval_env.add_lval env.taint_inst.lang lval
-                       taints_from_prop
-                else
-                  (* If we did not find any taint to be propagated, it could
-                   * be because we have not encountered the 'from' yet, so we
-                   * add the 'lval' to a "pending" queue. *)
-                  lval_env |> Lval_env.pending_propagation prop.TM.spec.var lval
-            | `Exp _
-            | `Ins _ ->
-                lval_env
-          else lval_env
-        in
-        (Taints.union taints_in_acc taints_from_prop, lval_env))
+        match prop.TM.spec.TRI.prop.propagator_to_parameter with
+        | Some parameter ->
+            queue_callback env prop shape parameter;
+            (taints_in_acc, lval_env)
+        | None ->
+            let opt_propagated, lval_env =
+              Lval_env.propagate_from prop.TM.spec.TRI.var lval_env
+            in
+            let taints_from_prop =
+              match opt_propagated with
+              | None -> Taints.empty
+              | Some taints -> taints
+            in
+            let lval_env =
+              if prop.spec.TRI.prop.propagator_by_side_effect then
+                match thing with
+                (* If `thing` is an l-value of the form `x.a.b.c`, then taint can be
+                 *  propagated by side-effect. A pattern-propagator may use this to
+                 * e.g. propagate taint from `x` to `y` in `f(x,y)`, so that
+                 * subsequent uses of `y` are tainted if `x` was previously tainted. *)
+                | `Lval lval ->
+                    if Option.is_some opt_propagated then
+                      lval_env |> Lval_env.add_lval env.taint_inst.lang lval taints_from_prop
+                    else
+                      (* If we did not find any taint to be propagated, it could
+                       * be because we have not encountered the 'from' yet, so we
+                       * add the 'lval' to a "pending" queue. *)
+                      lval_env
+                      |> Lval_env.pending_propagation prop.TM.spec.var lval
+                | `Exp _
+                | `Ins _ ->
+                    lval_env
+              else lval_env
+            in
+            (Taints.union taints_in_acc taints_from_prop, lval_env))
       (Taints.empty, lval_env) propagate_tos
   in
   (taints_propagated, lval_env)
 
+let modeled_source_shape incoming matches original =
+  let matches =
+    List.filter
+      (fun (m : R.taint_source TM.t) ->
+        T.solve_precondition ~ignore_poly_taint:false ~taints:incoming
+          (R.get_source_precondition m.spec)
+        =*= Some true)
+      matches
+  in
+  let models =
+    matches
+    |> List.filter_map (fun (m : R.taint_source TM.t) ->
+           if TM.is_exact m && m.spec.source_exact then
+             Option.map
+               (fun name -> (name, m.spec.source_model_singleton))
+               m.spec.source_model_type
+           else None)
+    |> List.sort_uniq Stdlib.compare
+  in
+  let types =
+    matches
+    |> List.filter_map (fun (m : R.taint_source TM.t) ->
+           if TM.is_exact m && m.spec.source_exact then m.spec.source_value_type
+           else None)
+    |> List.sort_uniq R.compare_taint_value_type
+  in
+  match (models, types) with
+  | [], [] -> original
+  | [ (name, singleton) ], [] ->
+      Taint_model_types.establish name ~singleton original
+  | [], [ kind ] -> S.Scalar kind
+  | _ -> S.Bot
+
 let find_lval_taint_sources env incoming_taints lval =
   let taints_of_pms env = taints_of_matches env ~incoming:incoming_taints in
   let source_pms = lval_is_source env lval in
@@ -1271,29 +1451,30 @@ and propagate_taint_via_java_getters_and_setters_without_definition env e args
   }
   (* We check for the "get"/"set" prefix below. *)
     when env.taint_inst.lang =*= Lang.Java && String.length method_str > 3 ->
-      begin
-        let mk_prop_lval () =
-          (* e.g. getFooBar/setFooBar -> fooBar *)
-          let prop_str =
+  begin
+      let mk_prop_lval () =
+        (* e.g. getFooBar/setFooBar -> fooBar *)
+        let prop_str =
           String.uncapitalize_ascii (Str.string_after method_str 3)
-          in
-          let prop_name =
+        in
+        let prop_name =
           match
-              Hashtbl.find_opt env.taint_inst.java_props_cache (prop_str, sid)
+            Hashtbl.find_opt env.taint_inst.java_props_cache (prop_str, sid)
           with
           | Some prop_name -> prop_name
           | None -> (
               let mk_default_prop_name () =
-                  let prop_name =
+                let prop_name =
                   {
-                      ident = (prop_str, method_tok);
-                      sid = G.SId.unsafe_default;
-                      id_info = G.empty_id_info ();
+                    ident = (prop_str, method_tok);
+                    sid = G.SId.unsafe_default;
+                    id_info = G.empty_id_info ();
+                    value_origin = None;
                   }
-                  in
-                  Hashtbl.add env.taint_inst.java_props_cache (prop_str, sid)
+                in
+                Hashtbl.add env.taint_inst.java_props_cache (prop_str, sid)
                   prop_name;
-                  prop_name
+                prop_name
               in
               match (!(obj.id_info.id_type), !hook_find_attribute_in_class) with
               | Some { t = TyN class_name; _ }, Some hook -> (
@@ -1302,12 +1483,12 @@ and propagate_taint_via_java_getters_and_setters_without_definition env e args
                   | Some prop_name ->
                       let prop_name = AST_to_IL.var_of_name prop_name in
                       Hashtbl.add env.taint_inst.java_props_cache
-                          (prop_str, sid) prop_name;
+                        (prop_str, sid) prop_name;
                       prop_name)
               | __else__ -> mk_default_prop_name ())
-          in
-          { lval with rev_offset = [ { o = Dot prop_name; oorig = NoOrig } ] }
         in
+        { lval with rev_offset = [ { o = Dot prop_name; oorig = NoOrig } ] }
+      in
         match args with
         | [] when String.(starts_with ~prefix:"get" method_str) ->
             let taints, shape, _sub, lval_env =
@@ -1401,6 +1582,15 @@ and check_tainted_lval_aux env (lval : IL.lval) :
               (* THINK: Should we just use 'Sig.find_in_shape' directly here ?
                        We have the 'sub_shape' available. *)
               match Lval_env.find_lval env.taint_inst.lang lval_env lval with
+              | None when env.taint_inst.lang =*= Lang.Swift -> (
+                  match (lval.rev_offset, sub_shape) with
+                  | { o = Dot { ident = "first", _; _ }; _ } :: _, S.Obj fields
+                    when Shape_and_sig.Fields.mem (T.Oint 0) fields ->
+                      let (S.Cell (taints, shape)) =
+                        Shape_and_sig.Fields.find (T.Oint 0) fields
+                      in
+                      (taints, shape)
+                  | _ -> (`None, S.Bot))
               | None -> (`None, S.Bot)
               | Some (Cell (xtaint', shape)) -> (xtaint', shape)
             in
@@ -1423,12 +1613,41 @@ and check_tainted_lval_aux env (lval : IL.lval) :
             in
             (xtaint', shape)
       in
+      let sub_new_taints =
+        match lval_in_env with
+        | `Clean
+        | `Sanitized ->
+            Taints.empty
+        | `None
+        | `Tainted _ ->
+            sub_new_taints
+      in
       let taints_from_env = Xtaint.to_taints lval_in_env in
       (* Find taint sources matching lval. *)
       let current_taints = Taints.union sub_new_taints taints_from_env in
       let taints_from_sources, lval_env =
         find_lval_taint_sources { env with lval_env } current_taints lval
       in
+      let source_matches = lval_is_source { env with lval_env } lval in
+      let lval_shape =
+        modeled_source_shape current_taints source_matches lval_shape
+      in
+      let lval_env =
+        if
+          List.exists
+            (fun (m : R.taint_source TM.t) ->
+              (not (m.spec.source_by_side_effect =*= R.No))
+              && Option.is_some m.spec.source_model_type)
+            source_matches
+        then
+          let updated =
+            Lval_env.add_lval_shape env.taint_inst.lang lval Taints.empty lval_shape lval_env
+          in
+          if Lang.is_js env.taint_inst.lang then
+            Taint_model_types.share_value env.taint_inst.lang updated lval
+          else updated
+        else lval_env
+      in
       (* Check sub-expressions in the offset. *)
       let taints_from_offset, lval_env =
         match lval.rev_offset with
@@ -1509,6 +1728,8 @@ and check_tainted_expr ?(arity = 0) env exp : Taints.t * S.shape * Lval_env.t =
   let check env = check_tainted_expr env in
   let check_subexpr exp =
     match exp.e with
+    | Literal (G.String _) ->
+        (Taints.empty, S.Scalar R.StringOrNull, env.lval_env)
     | Fetch _
     (* TODO: 'Fetch' is handled specially, this case should not never be taken.  *)
     | Literal _
@@ -1518,17 +1739,17 @@ and check_tainted_expr ?(arity = 0) env exp : Taints.t * S.shape * Lval_env.t =
         let taints, shape, lval_env = check env e in
         let taints = taints |> add_taints_from_shape shape in
         (taints, S.Bot, lval_env)
-    | Composite ((CTuple | CArray | CList), (_, es, _)) ->
+    | Composite (((CTuple | CArray | CList) as kind), (_, es, _)) ->
         let taints_and_shapes, lval_env = map_check_expr env check es in
-        let tuple_shape = Shape.tuple_like_obj taints_and_shapes in
-        let all_taints =
-          taints_and_shapes
-          |> List.fold_left
-               (fun acc (taints, shape) ->
-                 acc |> Taints.union taints |> add_taints_from_shape shape)
-               Taints.empty
+        let array =
+          Lang.is_js env.taint_inst.lang
+          &&
+          match kind with
+          | CArray -> true
+          | _ -> false
         in
-        (all_taints, tuple_shape, lval_env)
+        let tuple_shape = Shape.tuple_like_obj ~array taints_and_shapes in
+        (Taints.empty, tuple_shape, lval_env)
     | Composite ((CSet | Constructor _ | Regexp), (_, es, _)) ->
         let taints, lval_env = union_map_taints_and_vars env check es in
         (taints, S.Bot, lval_env)
@@ -1603,7 +1824,7 @@ and check_tainted_expr ?(arity = 0) env exp : Taints.t * S.shape * Lval_env.t =
         in
         (op_taints, S.Bot, lval_env)
     | RecordOrDict fields ->
-        (* TODO: Construct a proper record/dict shape here. *)
+        (* Child taints belong to their shape cells, not every sibling field. *)
         let (lval_env, taints), taints_and_shapes =
           fields
           |> List.fold_left_map
@@ -1614,19 +1835,12 @@ and check_tainted_expr ?(arity = 0) env exp : Taints.t * S.shape * Lval_env.t =
                      let e_taints, e_shape, lval_env =
                        check { env with lval_env } e
                      in
-                     let taints_acc =
-                       taints_acc |> Taints.union e_taints
-                       |> add_taints_from_shape e_shape
-                     in
                      ((lval_env, taints_acc), `Field (id, e_taints, e_shape))
                  | Spread e ->
                      let e_taints, e_shape, lval_env =
                        check { env with lval_env } e
                      in
-                     let taints_acc =
-                       taints_acc |> Taints.union e_taints
-                       |> add_taints_from_shape e_shape
-                     in
+                     let taints_acc = taints_acc |> Taints.union e_taints in
                      ((lval_env, taints_acc), `Spread e_shape)
                  | Entry (ke, ve) ->
                      let ke_taints, ke_shape, lval_env =
@@ -1639,12 +1853,6 @@ and check_tainted_expr ?(arity = 0) env exp : Taints.t * S.shape * Lval_env.t =
                      let ve_taints, ve_shape, lval_env =
                        check { env with lval_env } ve
                      in
-                     let taints_acc =
-                       taints_acc
-                       |> Taints.union
-                            ve_taints (* ← Now includes value taints! *)
-                       |> add_taints_from_shape ve_shape
-                     in
                      ((lval_env, taints_acc), `Entry (ke, ve_taints, ve_shape)))
                (env.lval_env, Taints.empty)
         in
@@ -1683,10 +1891,11 @@ and check_tainted_expr ?(arity = 0) env exp : Taints.t * S.shape * Lval_env.t =
                * avoid incorrectly matching them to lambda signatures. *)
               match shape with
               | S.Fun _ -> shape (* Already has a Fun shape, keep it *)
-              | _ ->
+              | _ -> (
                   let is_temp_var =
                     match lval.base with
-                    | Var name -> String.starts_with ~prefix:"_tmp" (fst name.ident)
+                    | Var name ->
+                        String.starts_with ~prefix:"_tmp" (fst name.ident)
                     | _ -> false
                   in
                   if is_temp_var then shape
@@ -1696,15 +1905,31 @@ and check_tainted_expr ?(arity = 0) env exp : Taints.t * S.shape * Lval_env.t =
                         lookup_signature env exp arity
                       else None
                     in
-                    (match sign with
+                    match sign with
                     | Some fun_sig -> S.Fun fun_sig
                     | None -> shape)
             in
-            (taints, shape, lval_env)
+            let sources =
+              any_is_best_source { env with lval_env }
+                (any_of_value_orig exp.eorig)
+            in
+            queue_callback_sources { env with lval_env } taints shape sources;
+            let propagated, lval_env =
+              handle_taint_propagators { env with lval_env } (`Exp exp) taints
+                shape
+            in
+            (Taints.union taints propagated, shape, lval_env)
         | __else__ ->
             let taints_exp, shape, lval_env = check_subexpr exp in
+            let source_matches =
+              orig_is_best_source { env with lval_env } exp.eorig
+            in
+            queue_callback_sources { env with lval_env } taints_exp shape
+              source_matches;
+            let shape = modeled_source_shape taints_exp source_matches shape in
+            let shape = modeled_sanitizer_shape env exp.eorig shape in
             let taints_sources, lval_env =
-              orig_is_best_source env exp.eorig
+              source_matches
               |> taints_of_matches { env with lval_env } ~incoming:taints_exp
             in
             let taints = taints_exp |> Taints.union taints_sources in
@@ -1934,34 +2159,33 @@ let check_function_call env fun_exp args
   let arity = List.length args in
   Log.debug (fun m ->
       m "CHECK_FUNCTION_CALL: %s with arity %d, intrafile=%b"
-        (Display_IL.string_of_exp fun_exp) arity
-        env.taint_inst.options.taint_intrafile);
+        (Display_IL.string_of_exp fun_exp)
+        arity env.taint_inst.options.taint_intrafile);
   let sig_result =
     if env.taint_inst.options.taint_intrafile then
       let from_db = lookup_signature env fun_exp arity in
       match from_db with
       | Some _ -> from_db
-      | None ->
+      | None -> (
           (* lookup_signature failed - check if callee has a Fun shape in lval_env.
            * This handles two cases:
            *   callback(source())       -- direct call, lval = callback
            *   callback.run(source())   -- invoke method, lval = callback.run
            * For invoke methods (e.g. Java Runnable.run), strip the method offset
            * and look up the base variable. *)
-          (match fun_exp.e with
-          | Fetch lval ->
+          match fun_exp.e with
+          | Fetch lval -> (
               let lval_to_check =
-                let invoke_methods = (Lang_config.get env.taint_inst.lang).invoke_methods in
+                let invoke_methods =
+                  (Lang_config.get env.taint_inst.lang).invoke_methods
+                in
                 match lval.rev_offset with
-                | [{ o = Dot method_name; _ }]
+                | [ { o = Dot method_name; _ } ]
                   when List.mem (fst method_name.ident) invoke_methods ->
                     { lval with rev_offset = [] }
                 | _ -> lval
               in
-              (match
-                 Lval_env.find_lval env.taint_inst.lang env.lval_env
-                   lval_to_check
-               with
+              match Lval_env.find_lval env.taint_inst.lang env.lval_env lval_to_check with
               | Some (S.Cell (_, S.Fun fun_sig)) ->
                   Log.debug (fun m ->
                       m "SIG_FROM_SHAPE: Found Fun shape for %s"
@@ -2152,9 +2376,10 @@ let check_function_call_callee env e =
       in
       let obj_taints = sub_taints |> add_taints_from_shape sub_shape in
       Log.debug (fun m ->
-          m "METHOD_CALL_CALLEE: obj_taints=%s, sub_taints=%s, returning taints=%s"
-            (T.show_taints obj_taints)
-            (T.show_taints sub_taints)
+          m
+            "METHOD_CALL_CALLEE: obj_taints=%s, sub_taints=%s, returning \
+             taints=%s"
+            (T.show_taints obj_taints) (T.show_taints sub_taints)
             (T.show_taints taints));
       (* Return sub_shape so we can check if the base object is a function parameter *)
       (`Obj (obj_taints, sub_shape), taints, shape, lval_env)
@@ -2183,40 +2408,36 @@ let call_with_intrafile lval_opt e env args instr =
      * and the callee is a Call expression, treat it as calling the inner method
      * with the lambda as an implicit block *)
     let implicit_lambda_call =
-      (match args with
-      | [ arg ] ->
-          (match arg with
-          | IL.Unnamed ({ e = Fetch lval; _ } as lambda_exp) ->
+      match args with
+      | [ arg ] -> (
+          match arg with
+          | IL.Unnamed ({ e = Fetch lval; _ } as lambda_exp) -> (
               (* Single Fetch argument - check if it's a lambda by looking at its shape *)
-              (match
-                 Lval_env.find_lval env.taint_inst.lang env.lval_env lval
-               with
-              | Some (S.Cell (_, shape)) ->
-                  (match shape with
+              match Lval_env.find_lval env.taint_inst.lang env.lval_env lval with
+              | Some (S.Cell (_, shape)) -> (
+                  match shape with
                   | S.Fun _fun_sig ->
                       (* It's a function/lambda! *)
                       Some (e, lambda_exp)
                   | _ -> None)
               | None -> None)
           | _ -> None)
-      | _ -> None)
+      | _ -> None
     in
     (* Handle implicit lambda pattern FIRST, before trying constructor *)
     match implicit_lambda_call with
-    | Some (inner_e, lambda_exp) ->
+    | Some (inner_e, lambda_exp) -> (
         (* Trace back to find the original call expression that was assigned to inner_e.
          * For Ruby, inner_e is typically _tmp:N which was assigned from arr.map().
          * We need to use arr.map (not _tmp) for signature lookup. *)
         (* For Ruby implicit blocks, inner_e is typically Fetch(_tmp) where _tmp
          * has a Fun shape from calling arr.map(). We need to directly instantiate
          * that Fun shape instead of doing signature database lookup. *)
-        (match inner_e.e with
-        | Fetch lval ->
+        match inner_e.e with
+        | Fetch lval -> (
             (* Check the shape of this lval to see if it has a Fun signature *)
-            (match
-               Lval_env.find_lval env.taint_inst.lang env.lval_env lval
-             with
-            | Some (S.Cell (var_taints, S.Fun fun_sig)) ->
+            match Lval_env.find_lval env.taint_inst.lang env.lval_env lval with
+            | Some (S.Cell (var_taints, S.Fun fun_sig)) -> (
                 (* The variable has a Fun shape. Instantiate it directly instead of
                  * doing signature database lookup. *)
                 let lambda_arg = IL.Unnamed lambda_exp in
@@ -2225,24 +2446,26 @@ let call_with_intrafile lval_opt e env args instr =
                 let callback_arg_taints = Xtaint.to_taints var_taints in
                 (* Get the lambda's Fun shape from the lval_env *)
                 let lambda_shape =
-                  (match lambda_exp.e with
-                  | Fetch lval ->
-                      (match
+                  match lambda_exp.e with
+                  | Fetch lval -> (
+                      match
                          Lval_env.find_lval env.taint_inst.lang env.lval_env
                            lval
                        with
                       | Some (S.Cell (_, shape)) -> shape
                       | None -> S.Bot)
-                  | _ -> S.Bot)
+                  | _ -> S.Bot
+                in
+                let lambda_arg_taint =
+                  IL.Unnamed (callback_arg_taints, lambda_shape)
                 in
-                let lambda_arg_taint = IL.Unnamed (callback_arg_taints, lambda_shape) in
-                let args_taints = [lambda_arg_taint] in
+                let args_taints = [ lambda_arg_taint ] in
                 let lookup_sig_fn exp arity =
                   if env.taint_inst.options.taint_intrafile then
                     lookup_signature env exp arity
                   else None
                 in
-                (match Sig_inst.instantiate_function_signature
+                match Sig_inst.instantiate_function_signature
                          ~lang:env.taint_inst.lang
                          ~outer_params:env.func.il_params env.lval_env
                          fun_sig ~callee:inner_e
@@ -2254,10 +2477,18 @@ let call_with_intrafile lval_opt e env args instr =
                     (* Process the call effects to get taints and shape *)
                     let call_taints, shape, lval_env =
                       List.fold_left
-                        (fun (taints_acc, shape_acc, lval_env) (call_effect : Sig_inst.call_effect) ->
+                        (fun (taints_acc, shape_acc, lval_env)
+                             (call_effect : Sig_inst.call_effect) ->
                           match call_effect with
-                          | ToSink { taints_with_precondition = incoming_taints, _; sink; _ } ->
-                              let sink_effects = effects_of_tainted_sink env incoming_taints sink in
+                          | ToSink
+                              {
+                                taints_with_precondition = incoming_taints, _;
+                                sink;
+                                _;
+                              } ->
+                              let sink_effects =
+                                effects_of_tainted_sink env incoming_taints sink
+                              in
                               record_effects env sink_effects;
                               (taints_acc, shape_acc, lval_env)
                           | ToReturn { data_taints; data_shape; _ } ->
@@ -2283,28 +2514,34 @@ let call_with_intrafile lval_opt e env args instr =
                     in
                     (call_taints, shape, lval_env)
                 | None -> (all_args_taints, S.Bot, lval_env))
-            | Some (S.Cell (_, _)) ->
+            | Some (S.Cell (_, _)) -> (
                 (* Try signature lookup instead *)
-                (match check_function_call { env with lval_env } inner_e args args_taints () with
+                match
+                  check_function_call { env with lval_env } inner_e args
+                    args_taints ()
+                with
                 | Some (call_taints, shape, lval_env) ->
                     (call_taints, shape, lval_env)
-                | None ->
-                    (all_args_taints, S.Bot, lval_env))
-            | None ->
+                | None -> (all_args_taints, S.Bot, lval_env))
+            | None -> (
                 (* Try signature lookup instead *)
-                (match check_function_call { env with lval_env } inner_e args args_taints () with
+                match
+                  check_function_call { env with lval_env } inner_e args
+                    args_taints ()
+                with
                 | Some (call_taints, shape, lval_env) ->
                     (call_taints, shape, lval_env)
-                | None ->
-                    (all_args_taints, S.Bot, lval_env)))
-        | _ ->
+                | None -> (all_args_taints, S.Bot, lval_env)))
+        | _ -> (
             (* Try signature lookup instead *)
-            (match check_function_call { env with lval_env } inner_e args args_taints () with
+            match
+              check_function_call { env with lval_env } inner_e args args_taints
+                ()
+            with
             | Some (call_taints, shape, lval_env) ->
                 (call_taints, shape, lval_env)
-            | None ->
-                (all_args_taints, S.Bot, lval_env)))
-    | None ->
+            | None -> (all_args_taints, S.Bot, lval_env)))
+    | None -> (
         (* Constructor call handling for ClassName() and ClassName.new().
          *
          * When taint flows through a constructor (e.g., `obj = Foo(tainted)`),
@@ -2322,25 +2559,29 @@ let call_with_intrafile lval_opt e env args instr =
              Skip the constructor check for Dot accesses unless it's Ruby's or
              Crystal's ClassName.new() pattern. *)
           (match e.e with
-          | Fetch { rev_offset = [{ o = Dot name; _ }]; _ }
+          | Fetch { rev_offset = [ { o = Dot name; _ } ]; _ }
             when fst name.IL.ident <> "new"
                  || not Lang.(env.taint_inst.lang =*= Ruby || env.taint_inst.lang =*= Crystal) -> false
           | _ -> true) &&
           Option.is_some env.signature_db &&
           (* The constructor edge is stored at the class name token position
              (first token of the call expression). Extract it from the callee. *)
-          let call_tok = match e.e with
+          let call_tok =
+            match e.e with
             | Fetch { base = Var name; _ } -> snd name.ident
             | _ -> Tok.unsafe_fake_tok ""
           in
-          not (Tok.is_fake call_tok) &&
-          match Call_graph.lookup_callee_from_graph
-                  env.call_graph
-                  (Option.map Function_id.of_il_name env.func.name)
-                  call_tok with
+          (not (Tok.is_fake call_tok))
+          &&
+          match
+            Call_graph.lookup_callee_from_graph env.call_graph
+              (Option.map Function_id.of_il_name env.func.name)
+              call_tok
+          with
           | Some callee_node ->
               Object_initialization.is_constructor env.taint_inst.lang
-                (Function_id.show callee_node) None
+                (Function_id.show callee_node)
+                None
           | None -> false
         in
         (* Remap: ClassName() → obj.ClassName(), ClassName.new() → obj.ClassName()
@@ -2350,10 +2591,20 @@ let call_with_intrafile lval_opt e env args instr =
         let e =
           if resolves_to_constructor then
             match (lval_opt, e.e) with
-            | Some lval, Fetch { base = Var name; rev_offset = ([] | [{ o = Dot _; _ }]) } ->
-                IL.{ e = Fetch { base = lval.base;
-                                 rev_offset = [{ o = Dot name; oorig = NoOrig }] };
-                     eorig = e.eorig }
+            | ( Some lval,
+                Fetch
+                  { base = Var name; rev_offset = [] | [ { o = Dot _; _ } ] } )
+              ->
+                IL.
+                  {
+                    e =
+                      Fetch
+                        {
+                          base = lval.base;
+                          rev_offset = [ { o = Dot name; oorig = NoOrig } ];
+                        };
+                    eorig = e.eorig;
+                  }
             | _ -> e
           else e
         in
@@ -2363,8 +2614,8 @@ let call_with_intrafile lval_opt e env args instr =
          * Ruby's initialize does NOT have explicit self, so this is
          * Python-specific. *)
         let args, args_taints =
-          if resolves_to_constructor
-             && Lang.(env.taint_inst.lang =*= Python) then
+          if resolves_to_constructor && Lang.(env.taint_inst.lang =*= Python)
+          then
             match lval_opt with
             | Some lval ->
                 let self_exp = IL.{ e = Fetch lval; eorig = NoOrig } in
@@ -2511,7 +2762,7 @@ let call_with_intrafile lval_opt e env args instr =
                           call_taints |> Taints.union obj_taints
                       | `Obj (obj_taints, _) -> call_taints |> Taints.union obj_taints
                     in
-                    (call_taints, Bot, lval_env)))))
+                    (call_taints, Bot, lval_env))))))
   in
   (* We add the taint of the function itselt (i.e., 'e_taints') too. *)
   let all_call_taints =
@@ -2554,7 +2805,8 @@ let new_with_intrafile env _result_lval _ty args constructor =
     with
     | Some (call_taints, shape, lval_env) -> Some (call_taints, shape, lval_env)
     | None ->
-        check_function_call { env with lval_env } constructor args args_taints ()
+        check_function_call { env with lval_env } constructor args args_taints
+          ()
   in
   match call_result with
   | Some (call_taints, shape, lval_env) -> (call_taints, shape, lval_env)
@@ -2606,11 +2858,20 @@ let check_tainted_instr env instr : Taints.t * S.shape * Lval_env.t =
     | AssignAnon (lval, anon_entity) -> (
         match anon_entity with
         | Lambda _ -> (
+            let identity =
+              match lval.base with
+              | Var name -> S.Closure (IL.NameSet.singleton name)
+              | _ -> S.Bot
+            in
             (* For lambdas, look up their signature from the signature database *)
             match (lval.base, env.signature_db, anon_entity) with
-            | Var lambda_name, Some db, Lambda fdef ->
+            | Var lambda_name, Some db, Lambda fdef -> (
                 let arity = List.length fdef.fparams in
-                (match Shape_and_sig.lookup_signature db (Function_id.of_il_name lambda_name) arity with
+                match
+                  Shape_and_sig.lookup_signature db
+                    (Function_id.of_il_name lambda_name)
+                    arity
+                with
                 | Some sig_ ->
                     let fun_shape = S.Fun sig_ in
                     Log.debug (fun m ->
@@ -2622,8 +2883,8 @@ let check_tainted_instr env instr : Taints.t * S.shape * Lval_env.t =
                     Log.debug (fun m ->
                         m "AssignAnon: lambda %s has no signature in db"
                           (IL.str_of_name lambda_name));
-                    (Taints.empty, Bot, env.lval_env))
-            | _, _, _ -> (Taints.empty, Bot, env.lval_env))
+                    (Taints.empty, identity, env.lval_env))
+            | _, _, _ -> (Taints.empty, identity, env.lval_env))
         | AnonClass _cdef ->
             (* Anonymous class instantiations are detected by Object_initialization.ml
              * before dataflow analysis and added to object_mappings. *)
@@ -2693,7 +2954,11 @@ let check_tainted_instr env instr : Taints.t * S.shape * Lval_env.t =
                       | `Obj (_obj_taints, S.Arg _) ->
                           (* This is a method call on a function parameter (e.g., callback.apply in Java,
                            * callback.call in Ruby). Treat it as invoking the callback. *)
-                          effects_of_call_func_arg e (match e_obj with `Obj (_, shape) -> shape | `Fun -> e_shape) args_taints
+                          effects_of_call_func_arg e
+                            (match e_obj with
+                            | `Obj (_, shape) -> shape
+                            | `Fun -> e_shape)
+                            args_taints
                           |> record_effects { env with lval_env }
                       | _ ->
                           effects_of_call_func_arg e e_shape args_taints
@@ -2760,6 +3025,39 @@ let check_tainted_instr env instr : Taints.t * S.shape * Lval_env.t =
           else all_args_taints
         in
         (all_args_taints, Bot, lval_env)
+    | CallSpecial
+        ( _,
+          (((ForeachNext | ForeachKeys) as operation), _),
+          [ Unnamed iterable ] )
+      when Lang.is_js env.taint_inst.lang -> (
+        let taints, shape, lval_env = check_tainted_expr env iterable in
+        match (operation, shape) with
+        | ForeachKeys, _ -> (taints, Bot, lval_env)
+        | ForeachNext, (S.Instance (_, fields) | S.Obj fields | S.Array fields)
+          -> (
+            let element =
+              Shape_and_sig.Fields.fold
+                (fun offset cell previous ->
+                  match offset with
+                  | T.Oint _
+                  | T.Oany ->
+                      Some
+                        (match previous with
+                        | None -> cell
+                        | Some previous -> Shape.join_cell previous cell)
+                  | _ -> previous)
+                fields None
+            in
+            match element with
+            | Some (S.Cell (xtaint, shape)) ->
+                let taints =
+                  match xtaint with
+                  | `Clean -> Taints.empty
+                  | _ -> Taints.union taints (Xtaint.to_taints xtaint)
+                in
+                (taints, shape, lval_env)
+            | None -> (taints, Bot, lval_env))
+        | _ -> (taints, shape, lval_env))
     | CallSpecial (_, (op, _), args) ->
         let args_taints, all_args_taints, lval_env =
           check_function_call_arguments env args
@@ -2794,8 +3092,20 @@ let check_tainted_instr env instr : Taints.t * S.shape * Lval_env.t =
       (Taints.empty, Bot, env.lval_env)
   | [] ->
       let taints_instr, rhs_shape, lval_env = check_instr instr.i in
+      let source_matches =
+        orig_is_best_source { env with lval_env } instr.iorig
+      in
+      (match instr.i with
+      | Call (Some _, _, _) -> ()
+      | _ ->
+          queue_callback_sources { env with lval_env } taints_instr rhs_shape
+            source_matches);
+      let rhs_shape =
+        modeled_source_shape taints_instr source_matches rhs_shape
+      in
+      let rhs_shape = modeled_sanitizer_shape env instr.iorig rhs_shape in
       let taint_sources, lval_env =
-        orig_is_best_source env instr.iorig
+        source_matches
         |> taints_of_matches { env with lval_env } ~incoming:taints_instr
       in
       let taints = Taints.union taints_instr taint_sources in
@@ -2962,8 +3272,7 @@ let pattern_leaves_with_offsets ~(lang : Lang.t) (pat : AST_generic.pattern) :
     {
       ident = (s, tok);
       sid = G.SId.unsafe_default;
-      id_info = G.empty_id_info ();
-    }
+      id_info = G.empty_id_info (); value_origin = None }
   in
   let stripped (s, tok) = (String_.strip_wrapping_char ':' s, tok) in
   (* The [OtherPat((":"|"::"), …)] branches below match the Clojure
@@ -3109,7 +3418,9 @@ let mk_lambda_in_env env lcfg =
             * have attached to it previously. This can happen when a
             * lambda is called inside a loop. *)
            let lval_env =
-             Lval_env.clean env.taint_inst.lang lval_env (LV.lval_of_var var)
+             let parameter = LV.lval_of_var var in
+             let writing, aliases = Lval_env.prepare_assignment env.taint_inst.lang lval_env parameter None in
+             Lval_env.finish_assignment (Lval_env.clean env.taint_inst.lang writing parameter) aliases
            in
            (* Now check if the parameter is itself a taint source. *)
            let taints, shape, lval_env =
@@ -3214,6 +3525,46 @@ let recognised_guards (env : env) ~(negated : bool) (params : IL.param list)
                          Effect_guard.is_length_atom l.atom.node)))
   else []
 
+let refine_string_guard lang env truth expression =
+  if not (lang =*= Lang.Python) then env
+  else
+    let rec constrained truth expr =
+      match expr.IL.e with
+      | Operator ((G.Not, _), [ Unnamed operand ]) ->
+          constrained (not truth) operand
+      | Operator
+          ( (G.Eq, _),
+            [
+              Unnamed { e = Fetch value; _ };
+              Unnamed { e = Literal (G.String _); _ };
+            ] )
+        when truth ->
+          Some value
+      | Operator
+          ( (((G.In | G.NotIn) as op), _),
+            [
+              Unnamed { e = Fetch value; _ };
+              Unnamed
+                { e = Composite ((CSet | CList | CTuple), (_, values, _)); _ };
+            ] )
+        when Bool.equal truth (op =*= G.In)
+             && (not (List.is_empty values))
+             && List.for_all
+                  (fun value ->
+                    match value.IL.e with
+                    | Literal (G.String _) -> true
+                    | _ -> false)
+                  values ->
+          Some value
+      | _ -> None
+    in
+    match constrained truth expression with
+    | Some value -> (
+        match Lval_env.find_lval lang env value with
+        | Some (S.Cell (_, S.Scalar R.StringOrNull)) -> Lval_env.clean lang env value
+        | _ -> env)
+    | None -> env
+
 let rec transfer : env -> fun_cfg:F.fun_cfg -> Lval_env.t D.transfn =
  fun enter_env ~fun_cfg
      (* the transfer function to update the mapping at node index ni *)
@@ -3224,12 +3575,60 @@ let rec transfer : env -> fun_cfg:F.fun_cfg -> Lval_env.t D.transfn =
     input_env ~enter_env:enter_env.lval_env ~flow mapping ni
   in
   let node = flow.graph#nodes#assoc ni in
-  let env = { enter_env with lval_env = in' } in
+  let env =
+    {
+      enter_env with
+      lval_env = in';
+      callback_requests = ref [];
+      model_preservations = ref [];
+    }
+  in
   let out' : Lval_env.t =
     match node.F.n with
+    | NInstr { i = CallSpecial (None, (Yield, tok), [ Unnamed value ]); _ }
+      when env.taint_inst.lang =*= Lang.Powershell ->
+        let taints, shape, lval_env = check_tainted_return env tok value in
+        record_effects env (effects_of_tainted_return env taints shape tok);
+        lval_env
     | NInstr x ->
         let taints, shape, lval_env' = check_tainted_instr env x in
+        let lval_env' =
+          if Lang.is_js env.taint_inst.lang then
+            let preserved =
+              Taint_callback_models.preserved_arrays lval_env'
+                !(env.callback_requests)
+            in
+            let lval_env' =
+              Taint_js_model_lifetime.instruction
+                ~preserved:!(env.model_preservations)
+                ~lambdas:env.func.lambda_bodies lval_env' x
+            in
+            Taint_js_callback_lifetime.instruction ~preserved lval_env' x
+          else lval_env'
+        in
         let opt_lval = LV.lval_of_instr_opt x in
+        let expression_env = lval_env' in
+        let lval_env', assigned_aliases =
+          match (env.taint_inst.lang, opt_lval) with
+          | (Lang.Js | Lang.Ts), Some target ->
+              let source, source_suffix =
+                match (x.i, shape) with
+                | ( Assign (_, { e = Fetch source; _ }),
+                    (S.Instance _ | S.Obj _ | S.Array _ | S.Bot | S.Arg _) ) ->
+                    (Some source, [])
+                | ( CallSpecial
+                      (_, (ForeachNext, _), [ Unnamed { e = Fetch source; _ } ]),
+                    (S.Instance _ | S.Obj _ | S.Array _ | S.Bot | S.Arg _) ) ->
+                    (Some source, [ T.Oany ])
+                | _ -> (None, [])
+              in
+              let write_env, aliases =
+                Lval_env.prepare_assignment ~source_suffix env.taint_inst.lang lval_env' target
+                  source
+              in
+              (write_env, Some aliases)
+          | _ -> (lval_env', None)
+        in
         let lval_env' =
           match opt_lval with
           | Some lval ->
@@ -3256,15 +3655,19 @@ let rec transfer : env -> fun_cfg:F.fun_cfg -> Lval_env.t D.transfn =
           in
           Taints.conjoin_guard write_guard taints
         in
-        let out_lval_env =
+        let assigned_env =
           match opt_lval with
           | Some lval ->
               if Shape.taints_and_shape_are_relevant taints shape then
                 (* Instruction returns tainted data, add taints to lval.
                  * See [Taint_lval_env] for details. *)
-                lval_env'
-                |> Lval_env.add_lval_shape env.taint_inst.lang lval taints
-                     shape
+                let destination =
+                  match Lval_env.normalize_lval env.taint_inst.lang lval with
+                  | Some (_, offsets) when not (List.mem T.Oany offsets) ->
+                      Lval_env.clean env.taint_inst.lang lval_env' lval
+                  | _ -> lval_env'
+                in
+                destination |> Lval_env.add_lval_shape env.taint_inst.lang lval taints shape
               else
                 (* The RHS returns no taint, but taint could propagate by
                  * side-effect too. So, we check whether the taint assigned
@@ -3289,6 +3692,24 @@ let rec transfer : env -> fun_cfg:F.fun_cfg -> Lval_env.t D.transfn =
               (* Instruction returns 'void' or its return value is ignored. *)
               lval_env'
         in
+        let assigned_env =
+          match assigned_aliases with
+          | None -> assigned_env
+          | Some aliases -> Lval_env.finish_assignment assigned_env aliases
+        in
+        let assigned_env =
+          match (env.taint_inst.lang, x.i) with
+          | (Lang.Js | Lang.Ts), Assign (target, expression) ->
+              Taint_js_references.assignment ~before:expression_env assigned_env
+                target expression
+          | _ -> assigned_env
+        in
+        let out_lval_env = if Lang.is_js env.taint_inst.lang then
+          Option.fold ~none:assigned_env
+            ~some:(Taint_model_types.share_value env.taint_inst.lang assigned_env)
+            opt_lval
+        else assigned_env
+        in
         (* Record the assigned variable as reassigned, so a guard that reads
          * it is dropped at stamp time: the IL is non-SSA, so a later read of
          * this name may observe a different value than a guard established
@@ -3310,6 +3731,7 @@ let rec transfer : env -> fun_cfg:F.fun_cfg -> Lval_env.t D.transfn =
     | TrueNode cond ->
         let pruned =
           prune_branch_if_unreachable env.taint_inst.lang cond true in'
+          |> fun env' -> refine_string_guard env.taint_inst.lang env' true cond
         in
         recognised_guards env ~negated:false fun_cfg.params cond
         |> List.fold_left
@@ -3321,6 +3743,7 @@ let rec transfer : env -> fun_cfg:F.fun_cfg -> Lval_env.t D.transfn =
     | FalseNode cond ->
         let pruned =
           prune_branch_if_unreachable env.taint_inst.lang cond false in'
+          |> fun env' -> refine_string_guard env.taint_inst.lang env' false cond
         in
         recognised_guards env ~negated:true fun_cfg.params cond
         |> List.fold_left
@@ -3380,7 +3803,67 @@ and do_lambdas env (lambdas : IL.lambdas_cfgs) node =
   (* We visit lambdas at their "use" site (where they are fetched), so we can e.g.
    * propagate taint from an object receiving a method call, to a lambda being
    * passed to that method. *)
-  let lambdas_to_analyze = lambdas_to_analyze_in_node env lambdas node in
+  let directly_called =
+    match node.F.n with
+    | NInstr { i = Call (_, { e = Fetch callee; _ }, []); _ } -> (
+        match Lval_env.find_lval env.taint_inst.lang env.lval_env callee with
+        | Some (S.Cell (_, S.Fun signature)) -> signature.closures
+        | Some (S.Cell (_, S.Closure names)) -> names
+        | _ -> IL.NameSet.empty)
+    | _ -> IL.NameSet.empty
+  in
+  let available_bodies =
+    if Lang.is_js env.taint_inst.lang then env.func.lambda_bodies else lambdas
+  in
+  let callback_inputs, lval_env =
+    Taint_callback_models.resolve env.lval_env available_bodies
+      !(env.callback_requests)
+  in
+  let env = { env with lval_env } in
+  let modeled_closures =
+    List.fold_left
+      (fun names (name, _, _, _) -> IL.NameSet.add name names)
+      directly_called callback_inputs
+  in
+  let direct_lambdas =
+    modeled_closures |> IL.NameSet.elements
+    |> List.filter_map (fun name ->
+           Option.map
+             (fun cfg -> (name, cfg))
+             (IL.NameMap.find_opt name available_bodies))
+  in
+  let lambdas_to_analyze =
+    List.sort_uniq
+      (fun (left, _) (right, _) -> IL.compare_name left right)
+      (direct_lambdas @ lambdas_to_analyze_in_node env lambdas node)
+    |> List.filter (fun (name, _) ->
+           if not (Lang.is_js env.taint_inst.lang) then true
+           else if
+             IL.NameSet.mem name env.func.active_lambdas
+             || IL.NameSet.cardinal env.func.active_lambdas >= 16
+           then (
+             (match node.F.n with
+             | NInstr instruction ->
+                 Taint_model_types.call_limit instruction.iorig
+             | _ -> ());
+             false)
+           else true)
+  in
+
+  let guaranteed_call =
+    Lang.is_js env.taint_inst.lang
+    && Int.equal (IL.NameSet.cardinal directly_called) 1
+    &&
+    match lambdas_to_analyze with
+    | [ (name, body) ] ->
+        IL.NameSet.mem name directly_called && List.is_empty body.IL.params
+    | _ -> false
+  in
+  let needed_vars =
+    if guaranteed_call then
+      IL.NameSet.union env.needed_vars (Lval_env.names env.lval_env)
+    else env.needed_vars
+  in
   let num_lambdas = List.length lambdas_to_analyze in
   if num_lambdas > 0 then
     Log.debug (fun m ->
@@ -3389,14 +3872,19 @@ and do_lambdas env (lambdas : IL.lambdas_cfgs) node =
   let effects_lambdas, out_envs_lambdas =
     lambdas_to_analyze
     |> List_.map (fun (lambda_name, lambda_cfg) ->
-           let lambda_in_env = mk_lambda_in_env env lambda_cfg in
-           fixpoint_lambda env.taint_inst env.func env.needed_vars lambda_name
+           let lambda_in_env =
+             mk_lambda_in_env env lambda_cfg
+             |> Taint_callback_models.seed env.taint_inst.lang lambda_name
+                  lambda_cfg.params callback_inputs
+           in
+           fixpoint_lambda env.taint_inst env.func needed_vars lambda_name
              lambda_cfg lambda_in_env ?signature_db:env.signature_db
              ?builtin_signature_db:env.builtin_signature_db
              ?call_graph:env.call_graph ())
     |> List_.split
   in
-  let effects = Effects.union_list effects_lambdas in
+  let effects = Effects.union_list effects_lambdas
+    |> Effects.filter (function ToReturn _ -> false | _ -> true) in
   (* Restamp the lambda's upflowed effects with the enclosing function's
    * currently-active guards — the guards in scope at the node where the
    * lambda is being evaluated. Paired with [mk_lambda_in_env]'s
@@ -3425,7 +3913,9 @@ and do_lambdas env (lambdas : IL.lambdas_cfgs) node =
        * We assume that these lambdas are being evaluated and that their side-effects
        * should affect the subsequent statements.
        *)
-      Lval_env.union_list ~default:env.lval_env out_envs_lambdas
+      match (guaranteed_call, out_envs_lambdas) with
+      | true, [ out ] -> out
+      | _ -> Lval_env.union_list ~default:env.lval_env out_envs_lambdas
     else
       (* If lambdas are not part of a call, we don't make their side-effects visible.
        * E.g.
@@ -3441,11 +3931,44 @@ and do_lambdas env (lambdas : IL.lambdas_cfgs) node =
        *)
       env.lval_env
   in
+  let out_env =
+    match node.F.n with
+    | NInstr { i = Call (Some result, _, []); iorig } ->
+        List.fold_left2
+          (fun current (candidate, cfg) effects ->
+            if
+              IL.NameSet.mem candidate directly_called
+              && List.is_empty cfg.params
+            then
+              Effects.fold
+                (fun eff current ->
+                  match eff with
+                  | ToReturn { data_taints; data_shape; guards; _ } ->
+                      let taints =
+                        data_taints
+                        |> Taints.conjoin_guard guards
+                        |> Taints.map_taint
+                             (fun t -> { t with T.tokens = List.rev t.T.tokens })
+                      in
+                      check_orig_if_sink env iorig taints data_shape;
+                      Lval_env.add_lval_shape env.taint_inst.lang result taints data_shape current
+                  | _ -> current)
+                effects current
+            else current)
+          out_env lambdas_to_analyze effects_lambdas
+    | _ -> out_env
+  in
   (effects, out_env)
 
 and fixpoint_lambda taint_inst func needed_vars lambda_name lambda_cfg in_env
-    ?signature_db ?builtin_signature_db ?call_graph () :
-    Effects.t * Lval_env.t =
+    ?signature_db ?builtin_signature_db ?call_graph () : Effects.t * Lval_env.t
+    =
+  let func =
+    {
+      func with
+      active_lambdas = IL.NameSet.add lambda_name func.active_lambdas;
+    }
+  in
   Log.debug (fun m ->
       m "Analyzing lambda %s (%s)"
         (IL.str_of_name lambda_name)
@@ -3455,15 +3978,6 @@ and fixpoint_lambda taint_inst func needed_vars lambda_name lambda_cfg in_env
       ~in_lambda:(Some lambda_name) ~class_name:None ?signature_db
       ?builtin_signature_db ?call_graph lambda_cfg
   in
-  let effects =
-    effects
-    |> Effects.filter (function
-         | ToSink _
-         | ToLval _
-         | ToSinkInCall _ ->
-             true
-         | ToReturn _ -> false)
-  in
   let out_env = mapping.(lambda_cfg.cfg.exit).Dataflow_core.out_env in
   let out_env' =
     out_env
@@ -3471,7 +3985,8 @@ and fixpoint_lambda taint_inst func needed_vars lambda_name lambda_cfg in_env
            (* Always preserve instance variables by checking if they were created from VarSpecial *)
            (* We need access to the original lval structure, not just the normalized name *)
            (* For now, keep the original logic but we'll need to fix the normalization *)
-           IL.NameSet.mem var needed_vars)
+           IL.NameSet.mem var needed_vars
+           || Taint_model_types.is_shared_name var)
   in
   Log.debug (fun m ->
       m ~tags:transfer_tag "Lambda out_env %s --FILTER(%s)--> %s"
@@ -3481,7 +3996,8 @@ and fixpoint_lambda taint_inst func needed_vars lambda_name lambda_cfg in_env
   (effects, out_env')
 
 and fixpoint_aux taint_inst func ?(needed_vars = IL.NameSet.empty)
-    ~enter_lval_env ~in_lambda ?class_name ?signature_db ?builtin_signature_db ?call_graph fun_cfg =
+    ~enter_lval_env ~in_lambda ?class_name ?signature_db ?builtin_signature_db
+    ?call_graph fun_cfg =
   let flow = fun_cfg.cfg in
   let init_mapping = DataflowX.new_node_array flow Lval_env.empty_inout in
   let needed_vars =
@@ -3498,6 +4014,8 @@ and fixpoint_aux taint_inst func ?(needed_vars = IL.NameSet.empty)
       needed_vars;
       effects_acc = ref Effects.empty;
       did_self_recurse = ref false;
+      callback_requests = ref [];
+      model_preservations = ref [];
       signature_db;
       builtin_signature_db;
       call_graph;
@@ -3603,6 +4121,8 @@ and fixpoint_aux taint_inst func ?(needed_vars = IL.NameSet.empty)
 and (fixpoint :
       Taint_rule_inst.t ->
       ?in_env:Lval_env.t ->
+      ?deferred_functions:IL.NameSet.t ->
+      ?lambda_bodies:IL.lambdas_cfgs ->
       ?name:IL.name ->
       ?class_name:string ->
       ?signature_db:Shape_and_sig.signature_database ->
@@ -3610,8 +4130,10 @@ and (fixpoint :
       ?builtin_signature_db:Shape_and_sig.builtin_signature_database ->
       F.fun_cfg ->
       Effects.t * mapping) =
- fun taint_inst ?(in_env = Lval_env.empty) ?name ?class_name
-     ?signature_db ?call_graph ?builtin_signature_db fun_cfg ->
+ fun taint_inst ?(in_env = Lval_env.empty)
+     ?(deferred_functions = IL.NameSet.empty)
+     ?(lambda_bodies = IL.NameMap.empty) ?name ?class_name ?signature_db
+     ?call_graph ?builtin_signature_db fun_cfg ->
   (* Check if this is a constructor and get class-level instance variable taint *)
   let enhanced_in_env =
     if taint_inst.options.taint_intrafile then
@@ -3793,6 +4315,9 @@ and (fixpoint :
                        param_sids = mk_param_sids lambda_cfg.params;
                        best_matches = lambda_best_matches;
                        used_lambdas = IL.NameSet.empty;
+                       deferred_functions = IL.NameSet.empty;
+                       lambda_bodies;
+                       active_lambdas = IL.NameSet.singleton lambda_name;
                      }
                    in
                    let lambda_effects, _lambda_mapping =
@@ -3805,6 +4330,7 @@ and (fixpoint :
                    let signature =
                      {
                        Signature.params;
+                       closures = IL.NameSet.singleton lambda_name;
                        params_il = lambda_cfg.params;
                        effects = lambda_effects;
                      }
@@ -3857,6 +4383,15 @@ and (fixpoint :
            sources |> Seq.append sanitizers |> Seq.append sinks)
   in
   let used_lambdas = lambdas_used_in_cfg fun_cfg in
+  let lambda_bodies =
+    List.fold_left
+      (fun bodies (name, cfg) -> IL.NameMap.add name cfg bodies)
+      lambda_bodies
+      (collect_all_lambdas_innermost_first fun_cfg)
+  in
+  let active_lambdas =
+    Option.fold ~none:IL.NameSet.empty ~some:IL.NameSet.singleton name
+  in
   let func =
     {
       name;
@@ -3865,11 +4400,15 @@ and (fixpoint :
       param_sids = mk_param_sids fun_cfg.params;
       best_matches;
       used_lambdas;
+      deferred_functions;
+      lambda_bodies;
+      active_lambdas;
     }
   in
   let effects, mapping =
     fixpoint_aux taint_inst func ~enter_lval_env:enhanced_in_env ~in_lambda:None
-      ~class_name ?signature_db:signature_db_with_lambdas ?builtin_signature_db ?call_graph fun_cfg
+      ~class_name ?signature_db:signature_db_with_lambdas ?builtin_signature_db
+      ?call_graph fun_cfg
   in
   (* If this was a constructor, store the instance variable taint for other methods *)
   (if taint_inst.options.taint_intrafile then
@@ -3894,6 +4433,8 @@ and (fixpoint :
   (effects, mapping)
 [@@profiling]
 
-let fixpoint taint_inst ?in_env ?name ?class_name ?signature_db ?builtin_signature_db ?call_graph fun_cfg =
-  fixpoint taint_inst ?in_env ?name ?class_name ?signature_db ?builtin_signature_db ?call_graph fun_cfg
+let fixpoint taint_inst ?in_env ?deferred_functions ?lambda_bodies ?name
+    ?class_name ?signature_db ?builtin_signature_db ?call_graph fun_cfg =
+  fixpoint taint_inst ?in_env ?deferred_functions ?lambda_bodies ?name
+    ?class_name ?signature_db ?builtin_signature_db ?call_graph fun_cfg
 [@@profiling]
diff --git a/src/tainting/Dataflow_tainting.mli b/src/tainting/Dataflow_tainting.mli
index ab57bce..dcfa2b0 100644
--- a/src/tainting/Dataflow_tainting.mli
+++ b/src/tainting/Dataflow_tainting.mli
@@ -1,10 +1,10 @@
 type mapping = Taint_lval_env.t Dataflow_core.mapping
-(** Mapping from variables to taint sources (if the variable is tainted).
-  * If a variable is not in the map, then it's not tainted. *)
+(** Mapping from variables to taint sources (if the variable is tainted). * If a
+    variable is not in the map, then it's not tainted. *)
 
 type java_props_cache
-(** When we encounter getters/setters without a definition, we need to resolve them
-  * to their corresponding property, we cache the results here. *)
+(** When we encounter getters/setters without a definition, we need to resolve
+    them * to their corresponding property, we cache the results here. *)
 
 val mk_empty_java_props_cache : unit -> java_props_cache
 
@@ -36,6 +36,8 @@ val pattern_leaves_with_offsets :
 val fixpoint :
   Taint_rule_inst.t ->
   ?in_env:Taint_lval_env.t ->
+  ?deferred_functions:IL.NameSet.t ->
+  ?lambda_bodies:IL.lambdas_cfgs ->
   ?name:IL.name ->
   ?class_name:string ->
   ?signature_db:Shape_and_sig.signature_database ->
@@ -55,4 +57,6 @@ val fixpoint :
 (* TODO: Move to module 'Taint' maybe. *)
 val drop_taints_if_bool_or_number :
   Rule_options.t -> Taint.Taint_set.t -> 'a Type.t -> Taint.Taint_set.t
-val reset_constructor: unit -> unit
+
+val reset_constructor : unit -> unit
+val available_lambdas : IL.fun_cfg -> IL.lambdas_cfgs
diff --git a/src/tainting/Graph_from_AST.ml b/src/tainting/Graph_from_AST.ml
index 596d9b9..f7bd98a 100644
--- a/src/tainting/Graph_from_AST.ml
+++ b/src/tainting/Graph_from_AST.ml
@@ -67,7 +67,7 @@ let fn_id_of_entity ~(lang : Lang.t) (opt_ent : G.entity option)
                         {
                           ident = (recv_name, fake_tok);
                           sid = AST_generic.SId.unsafe_default;
-                          id_info = AST_generic.empty_id_info ();
+                          id_info = AST_generic.empty_id_info (); value_origin = None;
                         }
                 | None -> None)
             | _ -> None
@@ -256,7 +256,7 @@ let build_call_graph ~(lang : Lang.t) ?(object_mappings = []) (ast : G.program)
   (* Create a special top_level node to represent code outside functions *)
   let top_level_node : node =
     let fake_tok = Tok.unsafe_fake_tok "<top_level>" in
-    let il_name = IL.{ ident = ("<top_level>", fake_tok); sid = G.SId.unsafe_default; id_info = AST_generic.empty_id_info () } in
+    let il_name = IL.{ ident = ("<top_level>", fake_tok); sid = G.SId.unsafe_default; id_info = AST_generic.empty_id_info (); value_origin = None } in
     Function_id.of_il_name il_name
   in
   Call_graph.G.add_vertex graph top_level_node;
@@ -435,7 +435,7 @@ let build_call_graph ~(lang : Lang.t) ?(object_mappings = []) (ast : G.program)
     (* Create Class:* node *)
     let class_init_node : node =
       let fake_tok = Tok.unsafe_fake_tok ("Class:" ^ class_str) in
-      let il_name = IL.{ ident = ("Class:" ^ class_str, fake_tok); sid = G.SId.unsafe_default; id_info = G.empty_id_info () } in
+      let il_name = IL.{ ident = ("Class:" ^ class_str, fake_tok); sid = G.SId.unsafe_default; id_info = G.empty_id_info (); value_origin = None } in
       Function_id.of_il_name il_name
     in
     Call_graph.G.add_vertex graph class_init_node;
@@ -495,7 +495,7 @@ let find_functions_containing_ranges ~(lang : Lang.t) (ast : G.program)
       match g_name with
       | G.Id ((str, tok), id_info) ->
           let id_info = { id_info with G.id_resolved = ref None } in
-          Some IL.{ ident = (str, tok); sid = G.SId.unsafe_default; id_info }
+          Some IL.{ ident = (str, tok); sid = G.SId.unsafe_default; id_info; value_origin = None }
       | _ -> None
 
     (* Helper to get IL.name from entity *)
@@ -533,7 +533,7 @@ let find_functions_containing_ranges ~(lang : Lang.t) (ast : G.program)
                       let class_str = fst class_il_name.IL.ident in
                       let class_node_name =
                         let fake_tok = Tok.unsafe_fake_tok ("Class:" ^ class_str) in
-                        Some IL.{ ident = ("Class:" ^ class_str, fake_tok); sid = G.SId.unsafe_default; id_info = AST_generic.empty_id_info () }
+                        Some IL.{ ident = ("Class:" ^ class_str, fake_tok); sid = G.SId.unsafe_default; id_info = AST_generic.empty_id_info (); value_origin = None }
                       in
                       let class_fn_id = [None; class_node_name] in
                       let existing = Hashtbl.find range_to_funcs range in
@@ -614,7 +614,7 @@ let find_functions_containing_ranges ~(lang : Lang.t) (ast : G.program)
         let fake_tok = Tok.unsafe_fake_tok "<top_level>" in
         Some IL.{ ident = ("<top_level>", fake_tok);
                   sid = G.SId.unsafe_default;
-                  id_info = AST_generic.empty_id_info () }
+                  id_info = AST_generic.empty_id_info (); value_origin = None }
       in
       let top_level_fn_id = [None; top_level_name] in
       if List.mem top_level_fn_id matching_funcs then
diff --git a/src/tainting/Shape_and_sig.ml b/src/tainting/Shape_and_sig.ml
index fc3746a..dd518d4 100644
--- a/src/tainting/Shape_and_sig.ml
+++ b/src/tainting/Shape_and_sig.ml
@@ -24,7 +24,7 @@ module Log = Log_tainting.Log
 (* Taint shapes *)
 (*****************************************************************************)
 
-module Fields = Map.Make (struct
+module Field = struct
   type t = T.offset
 
   (* In taint shapes we consider 'Ofld' and 'Ostr' to be the same, given that
@@ -45,7 +45,9 @@ module Fields = Map.Make (struct
     | Oany, (Ofld _ | Ostr _ | Oint _ | Oslice _) -> 1
     | Oslice _, (Ofld _ | Ostr _ | Oint _) -> 1
     | Oint _, (Ofld _ | Ostr _) -> 1
-end)
+end
+
+module Fields = Map.Make (Field)
 
 (** A shape approximates an object or data structure, and tracks the taint
  * associated with its fields and indexes.
@@ -83,7 +85,12 @@ end)
  *)
 module rec Shape : sig
   type shape =
+    | Scalar of Rule.taint_value_type
     | Bot  (** _|_, don't know or don't care *)
+    | Instance of (string * bool) * obj
+        (** Model name, shared-instance identity, and tracked members. *)
+    | Array of obj
+        (** JavaScript arrays retain their runtime kind and unknown elements. *)
     | Obj of obj
         (** An "object" or struct-like thing.
 
@@ -100,6 +107,7 @@ module rec Shape : sig
             [Arg (arg, [[]])]: one path, empty. At HOF dispatch the engine
             enumerates each path; at call-site instantiation each path is
             resolved against the caller's actual argument. *)
+    | Closure of IL.NameSet.t
     | Fun of Signature.t
         (** Function shapes. These enable Semgrep to handle HOFs. *)
 
@@ -134,9 +142,9 @@ module rec Shape : sig
             of `x.a` such as `x.a.v` will inherit the same taint as `x.a`.
 
             INVARIANT(cell): To keep shapes minimal:
-              1. If the xtaint is '`None', then the shape is not 'Bot' and we can reach
+              1. Except for array element slots, if the xtaint is '`None', then the shape is not 'Bot' and we can reach
                  another 'cell' whose xtaint is either '`Tainted' or '`Clean'.
-              2. If the xtaint is '`Clean', then the shape is 'Bot'.
+              2. If the xtaint is '`Clean', the shape is 'Bot' or a scalar type.
                  (If we add aliasing we may need to revisit this, and instead just mark
                   every reachable 'cell' as clean too.)
 
@@ -183,10 +191,16 @@ module rec Shape : sig
   val show_obj : obj -> string
 end = struct
   type shape =
+    | Scalar of Rule.taint_value_type
     | Bot
+    | Instance of (string * bool) * obj
+        (** Model name, shared-instance identity, and tracked members. *)
+    | Array of obj
     | Obj of obj
     | Arg of T.arg * T.offset list list
+    | Closure of IL.NameSet.t
     | Fun of Signature.t
+
   and cell = Cell of Xtaint.t * shape
   and obj = cell Fields.t
 
@@ -210,8 +224,16 @@ end = struct
     if depth > Limits_semgrep.taint_MAX_SHAPE_DEPTH then true
     else
       match (shape1, shape2) with
+      | Closure a, Closure b -> IL.NameSet.equal a b
+      | Closure _, _ -> false
+      | Scalar a, Scalar b -> Rule.equal_taint_value_type a b
       | Bot, Bot -> true
-      | Obj obj1, Obj obj2 -> equal_obj_depth (depth + 1) obj1 obj2
+      | Instance (name1, obj1), Instance (name2, obj2) ->
+          Stdlib.(name1 = name2) && equal_obj_depth (depth + 1) obj1 obj2
+      | Instance _, _ -> false
+      | Array obj1, Array obj2
+      | Obj obj1, Obj obj2 ->
+          equal_obj_depth (depth + 1) obj1 obj2
       | Arg (arg1, offsets1), Arg (arg2, offsets2) ->
           T.equal_arg arg1 arg2
           && Int.equal
@@ -219,7 +241,9 @@ end = struct
                   offsets1 offsets2)
                0
       | Fun sig1, Fun sig2 -> Signature.equal sig1 sig2
+      | Scalar _, _
       | Bot, _
+      | Array _, _
       | Obj _, _
       | Arg _, _
       | Fun _, _ ->
@@ -246,8 +270,13 @@ end = struct
     if depth > Limits_semgrep.taint_MAX_SHAPE_DEPTH then true
     else
       match (shape1, shape2) with
-      | Bot, Bot -> true
+      | Closure a, Closure b -> IL.NameSet.equal a b
+      | Scalar a, Scalar b -> Rule.equal_taint_value_type a b
+      | Instance (name1, obj1), Instance (name2, obj2) ->
+          Stdlib.(name1 = name2) && equal_obj_with_guards_depth (depth + 1) obj1 obj2
+      | Array obj1, Array obj2
       | Obj obj1, Obj obj2 -> equal_obj_with_guards_depth (depth + 1) obj1 obj2
+      | Bot, Bot -> true
       | Arg (arg1, offsets1), Arg (arg2, offsets2) ->
           T.equal_arg arg1 arg2
           && Int.equal
@@ -255,6 +284,7 @@ end = struct
                   offsets1 offsets2)
                0
       | Fun sig1, Fun sig2 -> Signature.equal_with_guards sig1 sig2
+      | Closure _, _ | Scalar _, _ | Instance _, _ | Array _, _
       | Bot, _
       | Obj _, _
       | Arg _, _
@@ -280,6 +310,21 @@ end = struct
 
   and compare_shape shape1 shape2 =
     match (shape1, shape2) with
+    | Closure a, Closure b -> IL.NameSet.compare a b
+    | Closure _, _ -> -1
+    | _, Closure _ -> 1
+    | Scalar a, Scalar b -> Rule.compare_taint_value_type a b
+    | Scalar _, _ -> -1
+    | _, Scalar _ -> 1
+    | Instance (name1, obj1), Instance (name2, obj2) -> (
+        match Stdlib.compare name1 name2 with
+        | 0 -> compare_obj obj1 obj2
+        | order -> order)
+    | Instance _, _ -> -1
+    | _, Instance _ -> 1
+    | Array a, Array b -> compare_obj a b
+    | Array _, _ -> -1
+    | _, Array _ -> 1
     | Bot, Bot -> 0
     | Obj obj1, Obj obj2 -> compare_obj obj1 obj2
     | Arg (arg1, offsets1), Arg (arg2, offsets2) -> (
@@ -307,7 +352,12 @@ end = struct
     spf "cell<%s>(%s)" (Xtaint.show xtaint) (show_shape shape)
 
   and show_shape = function
+    | Closure names -> "closure " ^ IL.NameSet.show names
+    | Scalar kind -> Rule.show_taint_value_type kind
     | Bot -> "_|_"
+    | Instance ((name, shared), obj) ->
+        spf "instance %s shared=%b {|%s|}" name shared (show_obj obj)
+    | Array obj -> spf "array [%s]" (show_obj obj)
     | Obj obj -> spf "obj {|%s|}" (show_obj obj)
     | Arg (arg, []) ->
         (* No offsets recorded — should not arise from normal
@@ -952,10 +1002,7 @@ and Signature : sig
       represent parameter kinds that we do not support yet. We don't want to
       just remove those unsupported parameters because we rely on the position
       of a parameter to represent taint variables, see 'Taint.arg'. *)
-  type param =
-    | P of string
-    | PRest of string
-    | Other
+  type param = P of string | PRest of string | Other
   [@@deriving eq, ord, show]
 
   type params = param list [@@deriving eq, ord]
@@ -977,6 +1024,7 @@ and Signature : sig
             consumers that read it. Consider deriving [params] lazily
             from [params_il] and dropping the field. *)
     effects : Effects.t;
+    closures : IL.NameSet.t;
   }
   (** * The 'params' act like an universal quantifier, we need them to later *
       instantiate the accompanying signature. *)
@@ -998,10 +1046,9 @@ end = struct
   (*************************************)
 
   (* TODO: Now with HOFs we run the risk of shadowing... *)
-  type param =
-    | P of string
-    | PRest of string
-    | Other [@@deriving eq, ord, show]
+  type param = P of string | PRest of string | Other
+  [@@deriving eq, ord, show]
+
   type params = param list
 
   let show_param = function
@@ -1035,26 +1082,31 @@ end = struct
     params : params;
     params_il : IL.param list;
     effects : Effects.t;
+    closures : IL.NameSet.t;
   }
 
   (* [params_il] is instantiation metadata; identity is determined by
      [params] and [effects] alone. *)
-  let equal { params = params1; params_il = _; effects = effects1 }
-      { params = params2; params_il = _; effects = effects2 } =
+  let equal { params = params1; params_il = _; effects = effects1; closures = closures1 }
+      { params = params2; params_il = _; effects = effects2; closures = closures2 } =
     equal_params params1 params2 && Effects.equal effects1 effects2
+    && IL.NameSet.equal closures1 closures2
 
-  let equal_with_guards { params = params1; params_il = _; effects = effects1 }
-      { params = params2; params_il = _; effects = effects2 } =
+  let equal_with_guards { params = params1; params_il = _; effects = effects1; closures = closures1 }
+      { params = params2; params_il = _; effects = effects2; closures = closures2 } =
     equal_params params1 params2
     && Effects.equal_with_guards effects1 effects2
+    && IL.NameSet.equal closures1 closures2
 
-  let compare { params = params1; params_il = _; effects = effects1 }
-      { params = params2; params_il = _; effects = effects2 } =
+  let compare { params = params1; params_il = _; effects = effects1; closures = closures1 }
+      { params = params2; params_il = _; effects = effects2; closures = closures2 } =
     match compare_params params1 params2 with
-    | 0 -> Effects.compare effects1 effects2
+    | 0 -> (match Effects.compare effects1 effects2 with
+        | 0 -> IL.NameSet.compare closures1 closures2
+        | other -> other)
     | other -> other
 
-  let show ?(truncate_guards = true) { params; params_il = _; effects } =
+  let show ?(truncate_guards = true) { params; params_il = _; effects; closures = _ } =
     spf "%s => {%s}" (show_params params)
       (Effects.show ~truncate_guards effects)
 end
@@ -1071,7 +1123,8 @@ module FunctionMap = Map.Make (Function_id)
 
 (** Arity tag for disambiguating multi-arity function signatures.
     [Arity_exact n] matches call sites with exactly [n] arguments.
-    [Arity_at_least n] matches call sites with >= [n] arguments (rest params). *)
+    [Arity_at_least n] matches call sites with >= [n] arguments (rest params).
+*)
 type sig_arity = Arity_exact of int | Arity_at_least of int
 [@@deriving show, eq, ord]
 
@@ -1087,10 +1140,10 @@ type extended_sig = {
 module SignatureSet = Set.Make (struct
   type t = extended_sig
 
-  let compare = fun x y ->
+  let compare =
+   fun x y ->
     let sig_cmp = Signature.compare x.sig_ y.sig_ in
-    if sig_cmp <> 0 then sig_cmp
-    else compare_sig_arity x.arity y.arity
+    if sig_cmp <> 0 then sig_cmp else compare_sig_arity x.arity y.arity
 end)
 
 type signature_database = {
@@ -1098,10 +1151,11 @@ type signature_database = {
   object_mappings : (AST_generic.name * AST_generic.name) list;
 }
 
-(** Separate database for builtin function signatures.
-    This is for builtin stdlib functions that aren't in the call graph. *)
-module BuiltinMap = Map.Make(struct
+(** Separate database for builtin function signatures. This is for builtin
+    stdlib functions that aren't in the call graph. *)
+module BuiltinMap = Map.Make (struct
   type t = string
+
   let compare = String.compare
 end)
 
@@ -1121,11 +1175,14 @@ let add_builtin_signature (db : builtin_signature_database) (func_name : string)
 
 (** Extract the concrete arity from a [sig_arity] for comparison purposes. *)
 let int_of_sig_arity : sig_arity -> int = function
-  | Arity_exact n | Arity_at_least n -> n
+  | Arity_exact n
+  | Arity_at_least n ->
+      n
 
 (** Given a non-empty set of signatures, find the best match for [arity].
     Returns the unique sig if only one exists, then tries [Arity_exact arity],
-    then falls back to the most specific [Arity_at_least n] where [n <= arity]. *)
+    then falls back to the most specific [Arity_at_least n] where [n <= arity].
+*)
 let find_by_arity (sigs : SignatureSet.t) (arity : int) : Signature.t option =
   if Int.equal (SignatureSet.cardinal sigs) 1 then
     Some (SignatureSet.choose sigs).sig_
@@ -1149,7 +1206,9 @@ let find_by_arity (sigs : SignatureSet.t) (arity : int) : Signature.t option =
               | None -> Some x
               | Some prev ->
                   if n > int_of_sig_arity prev.arity then Some x else acc)
-          | Arity_at_least _ | Arity_exact _ -> acc)
+          | Arity_at_least _
+          | Arity_exact _ ->
+              acc)
         sigs None
       |> Option.map (fun (x : extended_sig) -> x.sig_)
 
@@ -1157,10 +1216,12 @@ let lookup_builtin_signature (db : builtin_signature_database)
     (func_name : string) (arity : int) : Signature.t option =
   match BuiltinMap.find_opt func_name db with
   | Some sigs when not (SignatureSet.is_empty sigs) ->
-    (* NOTE: We do not use [find_by_arity sigs arity] because for built-ins we require an exact
+      (* NOTE: We do not use [find_by_arity sigs arity] because for built-ins we require an exact
        arity match. *)
       let filtered_sigs =
-        SignatureSet.filter (fun x -> equal_sig_arity x.arity (Arity_exact arity)) sigs
+        SignatureSet.filter
+          (fun x -> equal_sig_arity x.arity (Arity_exact arity))
+          sigs
       in
       let signatures_card = SignatureSet.cardinal filtered_sigs in
       if Int.equal signatures_card 1 then
@@ -1179,8 +1240,7 @@ let empty_signature_database () : signature_database =
 let lookup_signature (db : signature_database) (name : Function_id.t)
     (arity : int) : Signature.t option =
   match FunctionMap.find_opt name db.signatures with
-  | Some sigs when not (SignatureSet.is_empty sigs) ->
-      find_by_arity sigs arity
+  | Some sigs when not (SignatureSet.is_empty sigs) -> find_by_arity sigs arity
   | _ -> None
 
 let add_signature (db : signature_database) (name : Function_id.t)
@@ -1204,8 +1264,7 @@ let get_object_mappings (db : signature_database) :
     (AST_generic.name * AST_generic.name) list =
   db.object_mappings
 
-let show_func_key (key : func_key) : string =
-  Function_id.show_debug key
+let show_func_key (key : func_key) : string = Function_id.show_debug key
 
 let show_signature_database (db : signature_database) : string =
   FunctionMap.fold
diff --git a/src/tainting/Sig_inst.ml b/src/tainting/Sig_inst.ml
index 78562d0..e5be0ba 100644
--- a/src/tainting/Sig_inst.ml
+++ b/src/tainting/Sig_inst.ml
@@ -931,6 +931,13 @@ let rec substitute_in_sig (inst_var : inst_var) (inst_trace : inst_trace)
   let rec walk_shape (sh : shape) : shape =
     match sh with
     | Bot -> Bot
+    | (Scalar _ | Closure _) as shape -> shape
+    | Instance (name, fields) ->
+        Instance (name, Fields.filter_map (fun _ cell ->
+          Shape.update_offset_in_cell ~f:walk_xtaint [] cell) fields)
+    | Array fields ->
+        Array (Fields.filter_map (fun _ cell ->
+          Shape.update_offset_in_cell ~f:walk_xtaint [] cell) fields)
     | Obj obj ->
         let obj =
           obj
@@ -1069,6 +1076,13 @@ let instantiate_shape inst_var inst_trace shape =
   let inst_taints = instantiate_taints inst_var inst_trace in
   let rec inst_shape = function
     | Bot -> Bot
+    | (Scalar _ | Closure _) as shape -> shape
+    | Instance (name, fields) ->
+        Instance (name, Fields.filter_map (fun _ cell ->
+          Shape.update_offset_in_cell ~f:inst_xtaint [] cell) fields)
+    | Array fields ->
+        Array (Fields.filter_map (fun _ cell ->
+          Shape.update_offset_in_cell ~f:inst_xtaint [] cell) fields)
     | Obj obj ->
         let obj =
           obj
diff --git a/src/tainting/Taint_input_env.ml b/src/tainting/Taint_input_env.ml
index e9f06b7..414ea2e 100644
--- a/src/tainting/Taint_input_env.ml
+++ b/src/tainting/Taint_input_env.ml
@@ -20,7 +20,6 @@ module Effects = Shape_and_sig.Effects
 module Log = Log_tainting.Log
 
 let check_var_def (taint_inst : Taint_rule_inst.t) env id ii expr =
-  let name = AST_to_IL.var_of_id_info id ii in
   let assign =
     G.Assign (G.N (G.Id (id, ii)) |> G.e, Tok.fake_tok (snd id) "=", expr)
     |> G.e |> G.exprstmt
@@ -39,10 +38,7 @@ let check_var_def (taint_inst : Taint_rule_inst.t) env id ii expr =
     Dataflow_tainting.fixpoint taint_inst ~in_env:env
       IL.{ params = []; cfg; lambdas }
   in
-  let out_env = end_mapping.(cfg.exit).Dataflow_core.out_env in
-  let lval : IL.lval = { base = Var name; rev_offset = [] } in
-  let xtaint = Taint_lval_env.find_lval_xtaint taint_inst.lang out_env lval in
-  (xtaint, effects)
+  (end_mapping.(cfg.exit).Dataflow_core.out_env, effects)
 
 let add_to_env_aux (taint_inst : Taint_rule_inst.t) env id ii opt_expr =
   let var = AST_to_IL.var_of_id_info id ii in
@@ -59,7 +55,10 @@ let add_to_env_aux (taint_inst : Taint_rule_inst.t) env id ii opt_expr =
   let expr_taints, expr_effects =
     match opt_expr with
     | Some e ->
-        let xtaint, effects = check_var_def taint_inst env id ii e in
+        let result, effects = check_var_def taint_inst env id ii e in
+        let xtaint =
+          Taint_lval_env.find_lval_xtaint taint_inst.lang result (IL_helpers.lval_of_var var)
+        in
         (Xtaint.to_taints xtaint, effects)
     | None -> (T.Taint_set.empty, Effects.empty)
   in
@@ -84,16 +83,41 @@ let mk_fun_input_env taint_inst ?(glob_env = Taint_lval_env.empty)
   let add_to_env = add_to_env taint_inst in
   fparams
   (* For each argument, check if it's a source and, if so, add it to the input
-     * environment. *)
+   * environment. *)
   |> Fold_IL_params.fold add_to_env (glob_env, Effects.empty)
 
 let is_global (id_info : G.id_info) =
   let* kind, _sid = !(id_info.id_resolved) in
   Some (H.name_is_global kind)
 
-let mk_file_env taint_inst ast =
+let mk_import_env (taint_inst : Taint_rule_inst.t) ast =
+  let result = ref (Taint_lval_env.empty, Effects.empty) in
+  (if Lang.is_js taint_inst.lang then
+     let add (id, info) =
+       let env, effects = !result in
+       let value = G.N (G.Id (id, info)) |> G.e in
+       let env, imported_effects = check_var_def taint_inst env id info value in
+       result := (env, Effects.union effects imported_effects)
+     in
+     let visitor =
+       object
+         inherit [_] G.iter_no_id_info as super
+
+         method! visit_directive () directive =
+           (match directive.G.d with
+           | G.ImportAs (_, _, Some binding) -> add binding
+           | G.ImportFrom (_, _, imports) ->
+               List.iter (fun (_, binding) -> Option.iter add binding) imports
+           | _ -> ());
+           super#visit_directive () directive
+       end
+     in
+     visitor#visit_program () ast);
+  !result
+
+let mk_file_env taint_inst ~in_env ast =
   let add_to_env = add_to_env taint_inst in
-  let env = ref (Taint_lval_env.empty, Effects.empty) in
+  let env = ref (in_env, Effects.empty) in
   let visitor =
     object (_self : 'self)
       inherit [_] G.iter_no_id_info as super
diff --git a/src/tainting/Taint_input_env.mli b/src/tainting/Taint_input_env.mli
index e708a01..deeda25 100644
--- a/src/tainting/Taint_input_env.mli
+++ b/src/tainting/Taint_input_env.mli
@@ -7,12 +7,21 @@ val mk_fun_input_env :
   Taint_lval_env.t * Shape_and_sig.Effects.t
 (** Constructs the initial taint environment for a given function definition.
     Essentially, it records the parameters that are taint sources, or whose
-    default value is a taint source.
-    It is exposed to be used by inter-file taint analysis in Pro.  *)
+    default value is a taint source. It is exposed to be used by inter-file
+    taint analysis in Pro. *)
+
+val mk_import_env :
+  Taint_rule_inst.t ->
+  AST_generic.program ->
+  Taint_lval_env.t * Shape_and_sig.Effects.t
+(** Establish declaration-qualified JavaScript import values before module
+    execution. *)
 
 val mk_file_env :
   Taint_rule_inst.t ->
+  in_env:Taint_lval_env.t ->
   AST_generic.program ->
   Taint_lval_env.t * Shape_and_sig.Effects.t
 (** Constructs the global taint environment for a given file: it finds global-
-  and class- variable definitions that are final, and checks if they are tainted. *)
+    and class- variable definitions that are final, and checks if they are
+    tainted. *)
diff --git a/src/tainting/Taint_lval_env.ml b/src/tainting/Taint_lval_env.ml
index c630f13..9158c3b 100644
--- a/src/tainting/Taint_lval_env.ml
+++ b/src/tainting/Taint_lval_env.ml
@@ -34,13 +34,17 @@ type taints_to_propagate = T.taints VarMap.t
 type pending_propagation_dests = IL.lval VarMap.t
 
 type t = {
+  aliases : Taint_aliases.t;
   tainted : cell NameMap.t;
-      (** Lvalues that are tainted, it is only meant to track l-values of the form x.a_1. ... . a_N. *)
+      (** Lvalues that are tainted, it is only meant to track l-values of the
+          form x.a_1. ... . a_N. *)
   control : T.taints;
-      (** Taints propagated via the flow of control (rather than the flow of data). *)
+      (** Taints propagated via the flow of control (rather than the flow of
+          data). *)
   taints_to_propagate : taints_to_propagate;
-      (** Taint that is propagated via taint propagators (internally represented by
-    unique propagator variables), this is the taint going into the 'from's.. *)
+      (** Taint that is propagated via taint propagators (internally represented
+          by unique propagator variables), this is the taint going into the
+          'from's.. *)
   pending_propagation_dests : pending_propagation_dests;
       (** By-side-effect propagators waiting for taint to be propagated through
     an l-value. Because arguments are checked left-to-right, we use this trick to
@@ -101,6 +105,7 @@ let hook_propagate_to :
 let empty =
   {
     tainted = NameMap.empty;
+    aliases = Taint_aliases.empty;
     control = Taints.empty;
     taints_to_propagate = VarMap.empty;
     pending_propagation_dests = VarMap.empty;
@@ -123,11 +128,12 @@ let union le1 le2 =
   | false, false ->
       let tainted =
         NameMap.union
-          (fun _ x y -> Some (Shape.unify_cell x y))
+          (fun _ x y -> Some (Shape.join_cell x y))
           le1.tainted le2.tainted
       in
       {
         tainted;
+        aliases = Taint_aliases.join le1.aliases le2.aliases;
         control = Taints.union le1.control le2.control;
         taints_to_propagate =
           Var_env.varmap_union Taints.union le1.taints_to_propagate
@@ -151,6 +157,11 @@ let union le1 le2 =
 
 let union_list ?(default = empty) les = List.fold_left union default les
 
+let names env =
+  NameMap.fold
+    (fun name _ names -> IL.NameSet.add name names)
+    env.tainted IL.NameSet.empty
+
 (* Reduces an l-value into the form x.a_1. ... . a_N, the resulting l-value may
  * not represent the exact same object as the original l-value, but an
  * overapproximation. For example, the normalized l-value of `x[i]` will be `x`,
@@ -169,7 +180,8 @@ let normalize_lval lang lval =
            a unique global.
            TODO: C.x.y ? *)
         | [ { o = IL.Dot var; _ } ]
-          when H.is_class_name name || IdFlags.is_static !(var.id_info.id_flags) ->
+          when H.is_class_name name || IdFlags.is_static !(var.id_info.id_flags)
+          ->
             Some (var, [])
         | __else__ -> Some (name, rev_offset))
     (* explicit dereference of `this` e.g. `this->x` *)
@@ -202,7 +214,7 @@ let remove_some_lval_from_tainted_set tainted =
   tainted
   (* auxiliary _tmp variables get fake tokens *)
   |> NameMap.find_first_opt (fun var -> Tok.is_fake (snd var.ident))
-  |> Option.map (function var, _ -> var, NameMap.remove var tainted)
+  |> Option.map (function var, _ -> (var, NameMap.remove var tainted))
 
 let check_tainted_lvals_limit tainted new_var =
   if
@@ -227,27 +239,44 @@ let check_tainted_lvals_limit tainted new_var =
         None)
   else Some tainted
 
-let add_shape var offset new_taints new_shape lval_env =
-  match check_tainted_lvals_limit lval_env.tainted var with
-  | None -> lval_env
+let add_shape_direct ~weak var offset new_taints new_shape
+    ({ tainted; _ } as env) =
+  match check_tainted_lvals_limit tainted var with
+  | None -> env
   | Some tainted ->
+      let var_tok = snd var.IL.ident in
       let new_taints =
-        let var_tok = snd var.ident in
         if Tok.is_fake var_tok then new_taints
         else
-          new_taints
-          |> Taints.map_taint (fun (t : T.taint) ->
-                 { t with tokens = var_tok :: t.tokens })
+          Taints.map_taint
+            (fun t -> { t with tokens = var_tok :: t.tokens })
+            new_taints
       in
-      {
-        lval_env with
-        tainted =
-          NameMap.update var
-            (fun opt_var_ref ->
-              Shape.update_offset_and_unify new_taints new_shape offset
-                opt_var_ref)
-            tainted;
-      }
+      let tainted =
+        NameMap.update var
+          (fun original ->
+            let updated =
+              Shape.update_offset_and_unify new_taints new_shape offset original
+            in
+            match (weak, original, updated) with
+            | true, Some original, Some updated ->
+                Some (Shape.join_cell original updated)
+            | _ -> updated)
+          tainted
+      in
+      { env with tainted }
+
+let add_shape var offset new_taints new_shape env =
+  let path = (var, offset) in
+  let certain, aliases = Taint_aliases.related ~certain:true env.aliases path in
+  let possible, aliases = Taint_aliases.related ~certain:false aliases path in
+  List.fold_left
+    (fun env (var, offset) ->
+      let weak =
+        not (List.exists (Taint_aliases.equal_path (var, offset)) certain)
+      in
+      add_shape_direct ~weak var offset new_taints new_shape env)
+    { env with aliases } possible
 
 let add_lval_shape lang lval new_taints new_shape lval_env =
   match normalize_lval lang lval with
@@ -286,13 +315,27 @@ let propagate_to lang prop_var taints env =
 
 let find_var { tainted; _ } var = NameMap.find_opt var tainted
 
+let find_value { tainted; _ } expression =
+  let range = AST_generic_helpers.range_of_any_opt (AST_generic.E expression) in
+  NameMap.fold
+    (fun name cell found ->
+      match (range, name.IL.value_origin) with
+      | Some range, Some origin
+        when Some range
+             =*= AST_generic_helpers.range_of_any_opt (AST_generic.E origin) ->
+          Some
+            (Option.fold ~none:cell
+               ~some:(fun previous -> Shape.join_cell previous cell)
+               found)
+      | _ -> found)
+    tainted None
+
 let find_lval lang { tainted; _ } lval =
   let* var, offsets = normalize_lval lang lval in
   let* var_ref = NameMap.find_opt var tainted in
   match Shape.find_in_cell offsets var_ref with
-  | `Clean
-  | `Not_found _ ->
-      None
+  | `Clean -> Some (Cell (`Clean, Bot))
+  | `Not_found _ -> None
   | `Found cell -> Some cell
 
 let find_poly { tainted; _ } var offsets =
@@ -327,29 +370,57 @@ let pending_propagation prop_var lval env =
       VarMap.add prop_var lval env.pending_propagation_dests;
   }
 
-let clean lang lval_env lval =
+let clean lang env lval =
   match normalize_lval lang lval with
-  | None ->
-      (* Cannot track taint for this l-value; e.g. because the base is not a simple
-         variable. We just return the same environment untouched. *)
-      lval_env
-  | Some (var, offsets) ->
-      (* THINK: Should we clean propagations before they are executed?
-         (taints_to_propagate / pending_propagation_dests flow through
-         [with] unchanged below.) *)
+  | None -> env
+  | Some (_, offsets) when List.mem T.Oany offsets -> env
+  | Some path ->
+      let targets, aliases =
+        Taint_aliases.related ~certain:true env.aliases path
+      in
+      let tainted =
+        List.fold_left
+          (fun tainted (var, offsets) ->
+            NameMap.update var (Option.map (Shape.clean_cell offsets)) tainted)
+          env.tainted targets
+      in
+      { env with aliases; tainted }
+
+let prepare_assignment ?(source_suffix = []) lang env target source =
+  match normalize_lval lang target with
+  | None -> (env, env.aliases)
+  | Some target ->
+      let source =
+        Option.bind source (normalize_lval lang)
+        |> Option.map (fun (base, offsets) -> (base, offsets @ source_suffix))
+      in
+      let writing, after =
+        Taint_aliases.assignment env.aliases ~target ~source
+      in
+      ({ env with aliases = writing }, after)
+
+let finish_assignment env aliases = { env with aliases }
+
+let copy_reference ~before ~replaced lang env target source =
+  match
+    (normalize_lval lang replaced, normalize_lval lang target, normalize_lval lang source)
+  with
+  | Some replaced, Some target, Some source ->
       {
-        lval_env with
-        tainted =
-          NameMap.update var
-            (function
-              | None -> None
-              | Some var_ref -> Some (Shape.clean_cell offsets var_ref))
-            lval_env.tainted;
+        env with
+        aliases =
+          Taint_aliases.copy_reference ~before:before.aliases ~replaced
+            env.aliases ~target ~source;
       }
+  | _ -> env
 
 let filter_tainted pred ({ tainted; _ } as lval_env) =
   let tainted = tainted |> NameMap.filter (fun var _cell -> pred var) in
-  { lval_env with tainted }
+  {
+    lval_env with
+    tainted;
+    aliases = Taint_aliases.filter pred lval_env.aliases;
+  }
 
 let add_control_taints lval_env taints =
   if Taints.is_empty taints then lval_env
@@ -389,6 +460,7 @@ let mark_dead env = { env with dead = true }
 let equal
     {
       tainted = tainted1;
+      aliases = aliases1;
       control = control1;
       taints_to_propagate = _;
       pending_propagation_dests = _;
@@ -398,6 +470,7 @@ let equal
     }
     {
       tainted = tainted2;
+      aliases = aliases2;
       control = control2;
       taints_to_propagate = _;
       pending_propagation_dests = _;
@@ -412,6 +485,7 @@ let equal
    * guard reach the sink recording (a lost finding at any call site the
    * wider guard would have kept). *)
   && NameMap.equal equal_cell_with_guards tainted1 tainted2
+  && Taint_aliases.equal aliases1 aliases2
   (* NOTE: We ignore 'taints_to_propagate' and 'pending_propagation_dests',
    * we just care how they affect 'tainted'. *)
   && Taints.equal_with_guards control1 control2
@@ -426,16 +500,25 @@ let equal_by_lval lang { tainted = tainted1; _ } { tainted = tainted2; _ } lval
          variable. We just return the same environment untouched. *)
       false
   | Some (var, _offsets) ->
-      Option.equal equal_cell (NameMap.find_opt var tainted1) (NameMap.find_opt var tainted2)
+      Option.equal equal_cell
+        (NameMap.find_opt var tainted1)
+        (NameMap.find_opt var tainted2)
 
 let to_string
-    { tainted; control; taints_to_propagate; pending_propagation_dests; _ } =
-  (* FIXME: lval_to_str *)
-  (if NameMap.is_empty tainted then ""
-   else
-     NameMap.fold
-       (fun dn v s -> s ^ IL.str_of_name dn ^ ":" ^ show_cell v ^ " ")
-       tainted "[TAINTED]")
+    {
+      tainted;
+      control;
+      taints_to_propagate;
+      pending_propagation_dests;
+      aliases;
+      _;
+    } =
+  Taint_aliases.to_string aliases
+  ^ (if NameMap.is_empty tainted then ""
+     else
+       NameMap.fold
+         (fun dn v s -> s ^ IL.str_of_name dn ^ ":" ^ show_cell v ^ " ")
+         tainted "[TAINTED]")
   ^ (if Taints.is_empty control then ""
      else "[CONTROL] " ^ T.show_taints control)
   ^ (if VarMap.is_empty taints_to_propagate then ""
diff --git a/src/tainting/Taint_lval_env.mli b/src/tainting/Taint_lval_env.mli
index 80e14e5..84d2ab0 100644
--- a/src/tainting/Taint_lval_env.mli
+++ b/src/tainting/Taint_lval_env.mli
@@ -1,21 +1,14 @@
-(** Lval-to-taints environments used by taint-mode.
- *
- * This environment is field-sensitive, but only for l-values of the form
- * x.a_1. ... . a_N (i.e. a variable followed by field accesses). The main
- * purpose of tracking fields is to remove FPs.
- *
- * L-values of the form this.x.a_1. ... . a_N are normalized as
- * x.a_1. ... . a_N. The `this` base is not important as different variables
- * `x` should have different 'sid's. Same applies to `self`, `super`, etc.
- * We rely on Naming_AST to resolve the variables correctly.
- *
- * L-values of the form x.a_1. ... . a_N [i] o_1...o_M are normalized as
- * x.a_1. ... . a_N. That is, we obtain the longest prefix of dot-offsets
- * possible. See docs of `add` and `clean` below for more details.
- *
- * We track taints per variable, but not per object in memory. There is
- * no alias analysis involved!
- *)
+(** Lval-to-taints environments used by taint-mode. * * This environment is
+    field-sensitive, but only for l-values of the form * x.a_1. ... . a_N (i.e.
+    a variable followed by field accesses). The main * purpose of tracking
+    fields is to remove FPs. * * L-values of the form this.x.a_1. ... . a_N are
+    normalized as * x.a_1. ... . a_N. The `this` base is not important as
+    different variables * `x` should have different 'sid's. Same applies to
+    `self`, `super`, etc. * We rely on Naming_AST to resolve the variables
+    correctly. * * Constant fields and indexes retain their own shapes. Unknown
+    indexes are * weak updates and cannot prove that another field is clean. * *
+    JavaScript and TypeScript assignments also retain bounded definite and *
+    possible reference aliases. Other languages use per-variable shapes. *)
 
 open Shape_and_sig.Shape
 
@@ -55,9 +48,8 @@ val add_shape :
 val add_lval_shape : Lang.t -> IL.lval -> Taint.taints -> shape -> env -> env
 (** Add taints & shape to an l-value.
 
-    Adding taints to x.a_1. ... .a_N will NOT taint the prefixes
-    x.a_1. ... .a_i (i < N) (unless they become tainted separately).
- *)
+    Adding taints to x.a_1. ... .a_N will NOT taint the prefixes x.a_1. ... .a_i
+    (i < N) (unless they become tainted separately). *)
 
 val add : IL.name -> Taint.offset list -> Taint.taints -> env -> env
 
@@ -71,6 +63,8 @@ val propagate_to : Lang.t -> Dataflow_var_env.var -> Taint.taints -> env -> env
 val find_var : env -> IL.name -> cell option
 (** Find the 'cell' of a variable. *)
 
+val find_value : env -> AST_generic.expr -> cell option
+
 val find_lval : Lang.t -> env -> IL.lval -> cell option
 (** Find the 'cell' of an l-value. *)
 
@@ -124,10 +118,8 @@ val clean : Lang.t -> env -> IL.lval -> env
     Cleaning x.a_1. ... .a_N will clean that l-value as well as all its
     extensions x.a_1. ... .a_N. ... .a_M.
 
-    Crucially, cleaning x.a_1. ... . a_N [i] o_1...o_M  is the same as cleaning
-    x.a_1. ... . a_N. So, cleaning an element of an array such as x[1] would
-    clean the entire array! This seems drastic but it should help reducing FPs.
- *)
+    Constant field and index writes clear only that field and its descendants. A
+    dynamic index does not establish which element was overwritten. *)
 
 val filter_tainted : (IL.name -> bool) -> env -> env
 val add_control_taints : env -> Taint.taints -> env
@@ -170,11 +162,8 @@ val mark_dead : env -> env
 val union : env -> env -> env
 (** Compute the environment for the join of two branches.
 
-     If an lvalue x.a_1. ... .a_N was clean in one branch, we still consider it
-     clean in the union unless it is explicitly tainted in the other branch.
-     Note that if e.g. x.a_1. ... .a_i (with i < N) were tainted in the other
-     branch, then x.a_1. ... . a_N may no longer be clean, but we assume the
-     best case scenario to reduce FPs. *)
+    Missing fields inherit the corresponding branch's parent taint. A clean
+    write on one branch does not establish a clean value on another branch. *)
 
 val union_list : ?default:env -> env list -> env
 val equal : env -> env -> bool
@@ -185,3 +174,18 @@ val equal_by_lval : Lang.t -> env -> env -> IL.lval -> bool
 
 val to_string : env -> string
 val seq_of_tainted : env -> (IL.name * cell) Seq.t
+
+val prepare_assignment :
+  ?source_suffix:Taint.offset list ->
+  Lang.t ->
+  env ->
+  IL.lval ->
+  IL.lval option ->
+  env * Taint_aliases.t
+
+val finish_assignment : env -> Taint_aliases.t -> env
+
+val copy_reference :
+  before:env -> replaced:IL.lval -> Lang.t -> env -> IL.lval -> IL.lval -> env
+
+val names : env -> IL.NameSet.t
diff --git a/src/tainting/Taint_shape.ml b/src/tainting/Taint_shape.ml
index bfd41b3..b959908 100644
--- a/src/tainting/Taint_shape.ml
+++ b/src/tainting/Taint_shape.ml
@@ -70,7 +70,11 @@ let debug_offset offset =
 let rec shape_has_relevant_content = function
   | Bot -> false
   | Arg _
-  | Fun _ ->
+  | Fun _
+  | Closure _
+  | Scalar _
+  | Instance _
+  | Array _ ->
       true
   | Obj obj ->
       Fields.exists (fun _ cell -> cell_has_relevant_content cell) obj
@@ -112,18 +116,19 @@ let fix_poly_taint_with_offset offset taints =
            while (true) { x = x.getX(); }
       *)
       (not (List.mem o offset))
-      && (* For perf reasons we don't allow offsets to get too long.
-          * Otherwise in a long chain of function calls where each
-          * function adds some offset, we could end up a very large
-          * amount of polymorphic taint.
-          * This actually happened with rule
-          * semgrep.perf.rules.express-fs-filename from the Pro
-          * benchmarks, and file
-          * WebGoat/src/main/resources/webgoat/static/js/libs/ace.js.
-          *
-          * TODO: This is way less likely to happen if we had better
-          *   type info and we used it to remove taint, e.g. if Boolean
-          *   and integer expressions didn't propagate taint. *)
+      &&
+      (* For perf reasons we don't allow offsets to get too long.
+       * Otherwise in a long chain of function calls where each
+       * function adds some offset, we could end up a very large
+       * amount of polymorphic taint.
+       * This actually happened with rule
+       * semgrep.perf.rules.express-fs-filename from the Pro
+       * benchmarks, and file
+       * WebGoat/src/main/resources/webgoat/static/js/libs/ace.js.
+       *
+       * TODO: This is way less likely to happen if we had better
+       *   type info and we used it to remove taint, e.g. if Boolean
+       *   and integer expressions didn't propagate taint. *)
       List.length offset < Limits_semgrep.taint_MAX_POLY_OFFSET
     then extended_lval
     else (
@@ -137,14 +142,14 @@ let fix_poly_taint_with_offset offset taints =
        (fun taints o ->
          match (type_of_offset o, o) with
          | Some { t = TyFun _; _ }, _ ->
-            (* We have an l-value like `o.f` where `f` has a function type,
-             * so it's a method call, we return nothing here. We cannot just
-             * return `xtaint`, which is the taint of `o` in the environment;
-             * whether that taint propagates or not is determined in
-             * 'check_tainted_instr'/'Call'. Otherwise, if `o` had taint var
-             * 'o@i', the call `o.getX()` would have taints '{o@i, o@i.x}'
-             * when it should only have taints '{o@i.x}'. *)
-            Taints.empty
+             (* We have an l-value like `o.f` where `f` has a function type,
+              * so it's a method call, we return nothing here. We cannot just
+              * return `xtaint`, which is the taint of `o` in the environment;
+              * whether that taint propagates or not is determined in
+              * 'check_tainted_instr'/'Call'. Otherwise, if `o` had taint var
+              * 'o@i', the call `o.getX()` would have taints '{o@i, o@i.x}'
+              * when it should only have taints '{o@i.x}'. *)
+             Taints.empty
          | _, Oany ->
             (* Cannot handle this offset. *)
             taints
@@ -179,13 +184,41 @@ let rec unify_cell cell1 cell2 =
 
 and unify_shape shape1 shape2 =
   match (shape1, shape2) with
+  | Bot, (Instance _ as instance)
+  | (Instance _ as instance), Bot ->
+      instance
+  | Instance ((left, left_shared), a), Instance ((right, right_shared), b)
+    when String.equal left right ->
+      Instance ((left, left_shared && right_shared), unify_obj a b)
+  | Instance (_, a), (Instance (_, b) | Obj b | Array b)
+  | (Obj a | Array a), Instance (_, b) ->
+      Obj (unify_obj a b)
+  | Instance (_, fields), _
+  | _, Instance (_, fields) ->
+      Obj fields
+  | Scalar a, Scalar b ->
+      if Rule.equal_taint_value_type a b then Scalar a else Bot
+  | Scalar _, shape
+  | shape, Scalar _ ->
+      shape
   | Bot, shape
   | shape, Bot ->
       (* 'Bot' acts like a do-not-care. *)
       shape
-  | Obj obj1, Obj obj2 -> Obj (unify_obj obj1 obj2)
-  | ( Fun { params = params1; params_il = params_il1; effects = effects1 },
-      Fun { params = params2; params_il = params_il2; effects = effects2 } )
+  | Closure a, Closure b -> Closure (IL.NameSet.union a b)
+  | Closure names, Fun signature
+  | Fun signature, Closure names ->
+      Closure (IL.NameSet.union names signature.closures)
+  | Closure _, _
+  | _, Closure _ ->
+      Bot
+  | Array obj1, Array obj2 -> Array (unify_obj obj1 obj2)
+  | Array obj1, Obj obj2
+  | Obj obj1, Array obj2
+  | Obj obj1, Obj obj2 ->
+      Obj (unify_obj obj1 obj2)
+  | ( Fun { params = params1; params_il = params_il1; effects = effects1; closures = closures1 },
+      Fun { params = params2; params_il = params_il2; effects = effects2; closures = closures2 } )
     ->
       let equal_il_param p1 p2 =
         match
@@ -202,6 +235,7 @@ and unify_shape shape1 shape2 =
           params = params1;
           params_il = params_il1;
           effects = Effects.union effects1 effects2;
+          closures = IL.NameSet.union closures1 closures2;
         }
       else (
         (* Two Fun shapes from different lambdas. Their effects'
@@ -240,14 +274,14 @@ and unify_shape shape1 shape2 =
             (T.show_arg arg1) (T.show_arg arg2));
       shape1
   (* 'Arg' acts like a shape variable. *)
-  | Arg _, (Obj _ as obj)
-  | (Obj _ as obj), Arg _ ->
+  | Arg _, ((Obj _ | Array _) as obj)
+  | ((Obj _ | Array _) as obj), Arg _ ->
       obj
   | Arg _, (Fun _ as func)
   | (Fun _ as func), Arg _ ->
       func
-  | Obj _, Fun _
-  | Fun _, Obj _ ->
+  | (Obj _ | Array _), Fun _
+  | Fun _, (Obj _ | Array _) ->
       (* This could be caused by bugs in Semgrep, or by an if-then-else in a
        * dynamic language like Python where the same variable has different types
        * in each branch, or by unsafe casts in C/C++ perhaps. *)
@@ -261,6 +295,84 @@ and unify_obj obj1 obj2 =
   (* THINK: Apply taint_MAX_OBJ_FIELDS limit ? *)
   Fields.union (fun _ x y -> Some (unify_cell x y)) obj1 obj2
 
+let join_cell left right =
+  let inherit_taint inherited = function
+    | `None -> inherited
+    | value -> value
+  in
+  let inherited_field offset = function
+    | `Tainted taints -> `Tainted (fix_poly_taint_with_offset [ offset ] taints)
+    | (`None | `Clean) as value -> value
+  in
+  let rec join inherited_left inherited_right (Cell (left, left_shape))
+      (Cell (right, right_shape)) =
+    let left = inherit_taint inherited_left left in
+    let right = inherit_taint inherited_right right in
+    let taint =
+      match (left, right) with
+      | `Clean, `None
+      | `None, `Clean ->
+          `None
+      | _ -> Xtaint.union left right
+    in
+    let shape =
+      match (left_shape, right_shape) with
+      | ( Instance ((left_name, left_shared), left_fields),
+          Instance ((right_name, right_shared), right_fields) )
+        when String.equal left_name right_name ->
+          Instance
+            ( (left_name, left_shared && right_shared),
+              join_model_fields left right left_fields right_fields )
+      | ( Instance (_, left_fields),
+          (Instance (_, right_fields) | Obj right_fields | Array right_fields) )
+      | (Obj left_fields | Array left_fields), Instance (_, right_fields) ->
+          Obj (join_fields left right left_fields right_fields)
+      | Instance (_, fields), _ ->
+          Obj (join_fields left right fields Fields.empty)
+      | _, Instance (_, fields) ->
+          Obj (join_fields left right Fields.empty fields)
+      | Array left_fields, Array right_fields ->
+          Array (join_fields left right left_fields right_fields)
+      | Array left_fields, Obj right_fields
+      | Obj left_fields, Array right_fields
+      | Obj left_fields, Obj right_fields ->
+          Obj (join_fields left right left_fields right_fields)
+      | Array fields, Bot -> Array (join_fields left right fields Fields.empty)
+      | Bot, Array fields -> Array (join_fields left right Fields.empty fields)
+      | (Obj fields | Array fields), _ ->
+          Obj (join_fields left right fields Fields.empty)
+      | _, (Obj fields | Array fields) ->
+          Obj (join_fields left right Fields.empty fields)
+      | Bot, Scalar _
+      | Scalar _, Bot ->
+          Bot
+      | _ -> unify_shape left_shape right_shape
+    in
+    Cell (taint, shape)
+  and join_model_fields left right left_fields right_fields =
+    let fields = join_fields left right left_fields right_fields in
+    Fields.mapi
+      (fun offset (Cell (taints, shape)) ->
+        if Fields.mem offset left_fields <> Fields.mem offset right_fields then
+          Cell (taints, Bot)
+        else Cell (taints, shape))
+      fields
+  and join_fields left right left_fields right_fields =
+    Fields.merge
+      (fun offset left_cell right_cell ->
+        let left = inherited_field offset left in
+        let right = inherited_field offset right in
+        match (left_cell, right_cell) with
+        | None, None -> None
+        | _ ->
+            Some
+              (join left right
+                 (Option.value ~default:cell_none_bot left_cell)
+                 (Option.value ~default:cell_none_bot right_cell)))
+      left_fields right_fields
+  in
+  join `None `None left right
+
 (*********************************************************)
 (* Object shapes *)
 (*********************************************************)
@@ -282,19 +394,21 @@ let add_field_to_obj_check_invariant obj offset taints shape =
       Fields.add offset (Cell (`Clean, Bot)) obj
   | xtaint, shape -> Fields.add offset (Cell (xtaint, shape)) obj
 
-let tuple_like_obj taints_and_shapes : shape =
+let tuple_like_obj ?(array = false) taints_and_shapes : shape =
   let _index, obj =
     taints_and_shapes
     |> List.fold_left
          (fun (i, obj) (taints, shape) ->
            let obj =
-             add_field_to_obj_check_invariant obj (T.Oint i) taints shape
+             if array then
+               Fields.add (T.Oint i) (Cell (Xtaint.of_taints taints, shape)) obj
+             else add_field_to_obj_check_invariant obj (T.Oint i) taints shape
            in
            (i + 1, obj))
          (0, Fields.empty)
   in
   (* See INVARIANT(cell) *)
-  if Fields.is_empty obj then Bot else Obj obj
+  if array then Array obj else if Fields.is_empty obj then Bot else Obj obj
 
 let record_or_dict_like_obj taints_and_shapes : shape =
   let obj =
@@ -318,9 +432,14 @@ let record_or_dict_like_obj taints_and_shapes : shape =
                add_field_to_obj_check_invariant obj offset taints shape
            | `Spread shape -> (
                match shape with
-               | Obj obj' -> unify_obj obj obj'
+               | Instance (_, obj')
+               | Obj obj'
+               | Array obj' ->
+                   unify_obj obj obj'
+               | Scalar _
                | Bot
                | Arg _
+               | Closure _
                | Fun _ ->
                    Log.err (fun m ->
                        m
@@ -349,8 +468,8 @@ let rec gather_all_taints_in_cell_acc acc cell =
       gather_all_taints_in_shape_acc (Taints.union taints acc) shape
 
 and gather_all_taints_in_shape_acc acc = function
-  | Bot -> acc
-  | Obj obj -> gather_all_taints_in_obj_acc acc obj
+  | Bot | Scalar _ -> acc
+  | Instance (_, obj) | Array obj | Obj obj -> gather_all_taints_in_obj_acc acc obj
   | Arg (arg, offsets) ->
       (* One [Shape_var] per alternative offset. *)
       List.fold_left
@@ -359,6 +478,7 @@ and gather_all_taints_in_shape_acc acc = function
           let taint = { T.orig = T.Shape_var lval; tokens = [] } in
           Taints.add_taint taint acc)
         acc offsets
+  | Closure _
   | Fun _ ->
       (* Consider a third-party/opaque function to which we pass a record that
        * contains a function object. Should be gather the taints in the function
@@ -388,7 +508,13 @@ let rec find_in_cell_w_carry ~taints offset cell =
   | _ :: _ -> (
       match xtaint with
       | `Clean ->
-          if shape <> Bot then
+          if
+            match shape with
+            | Bot
+            | Scalar _ ->
+                false
+            | _ -> true
+          then
             Log.err (fun m ->
                 m "BUG: Taint_shape.find_in_cell: INVARIANT(cell).2 is broken");
           `Clean
@@ -399,8 +525,8 @@ and find_in_shape_w_carry ~taints offset shape =
   let not_found = `Not_found (taints, shape, offset) in
   match shape with
   (* offset <> [] *)
-  | Bot -> not_found
-  | Obj obj -> find_in_obj_w_carry ~taints offset obj
+  | Bot | Scalar _ -> not_found
+  | Instance (_, obj) | Array obj | Obj obj -> find_in_obj_w_carry ~taints offset obj
   | Arg (arg, base_offsets) ->
       (* Mirror the method-vs-field discriminator from
        * [fix_poly_taint_with_offset]: when any offset segment has a
@@ -442,6 +568,7 @@ and find_in_shape_w_carry ~taints offset shape =
             m "Could not find offset %s in polymorphic shape %s"
               (debug_offset offset) (show_shape shape));
         not_found)
+  | Closure _
   | Fun _ ->
       (* This is an error, we just don't want to crash here. *)
       Log.err (fun m ->
@@ -568,25 +695,38 @@ let rec update_offset_in_cell ~f offset cell =
   | `None, Bot -> None
   | `Tainted taints, Bot when Taints.is_empty taints -> None
   (* Restore INVARIANT(cell).2 *)
-  | `Clean, (Obj _ | Arg _ | Fun _) ->
+  | `Clean, (Instance _ | Obj _ | Array _ | Arg _ | Closure _ | Fun _) ->
       (* If we are tainting an offset of this cell, the cell cannot be
          considered clean anymore. *)
       Some (Cell (`None, shape))
-  | `Clean, Bot
-  | `None, (Obj _ | Arg _ | Fun _)
-  | `Tainted _, (Bot | Obj _ | Arg _ | Fun _) ->
+  | `Clean, (Bot | Scalar _)
+  | `None, (Instance _ | Obj _ | Array _ | Arg _ | Closure _ | Fun _ | Scalar _)
+  | ( `Tainted _,
+      (Bot | Instance _ | Obj _ | Array _ | Arg _ | Closure _ | Fun _ | Scalar _)
+    ) ->
       Some (Cell (xtaint, shape))
 
 and update_offset_in_shape ~f offset shape =
   match shape with
   | Bot
+  | Scalar _
   | Arg _ ->
       let shape = Obj Fields.empty in
       update_offset_in_shape ~f offset shape
+  | Instance (name, obj) ->
+      Instance
+        ( name,
+          Option.value ~default:Fields.empty
+            (update_offset_in_obj ~f offset obj) )
+  | Array obj ->
+      Array
+        (Option.value ~default:Fields.empty
+           (update_offset_in_obj ~f offset obj))
   | Obj obj -> (
       match update_offset_in_obj ~f offset obj with
       | None -> Bot
       | Some obj -> Obj obj)
+  | Closure _
   | Fun _ ->
       (* This is an error, we just don't want to crash here. *)
       Log.err (fun m ->
@@ -605,8 +745,15 @@ and update_offset_in_obj ~f offset obj =
         let o, obj = internal_UNSAFE_find_offset_in_obj o obj in
         match o with
         | Oany (* arbitrary index [*] *) ->
-            (* consider all fields/indexes *)
-            Fields.filter_map (fun _o' -> update_offset_in_cell ~f offset) obj
+            Fields.filter_map
+              (fun _ original ->
+                match update_offset_in_cell ~f offset original with
+                | None -> Some original
+                | Some updated -> (
+                    match join_cell original updated with
+                    | Cell (`None, Bot) -> None
+                    | joined -> Some joined))
+              obj
         | Oslice n ->
             (* Update the trailing-rest from index [n]. For each entry
              * intersecting [n, infinity), apply the update with the
@@ -644,11 +791,17 @@ let update_offset_and_unify new_taints new_shape offset opt_cell =
   if taints_and_shape_are_relevant new_taints new_shape then
     let new_xtaint =
       (* THINK: Maybe Dataflow_tainting 'check_xyz' should be returning 'Xtaint.t'? *)
-      Xtaint.of_taints new_taints
+      match (Taints.is_empty new_taints, new_shape) with
+      | true, Scalar _ -> `Clean
+      | _ -> Xtaint.of_taints new_taints
     in
     let cell = opt_cell ||| cell_none_bot in
     let add_new_taints xtaint shape =
-      let shape = unify_shape new_shape shape in
+      let shape =
+        match (xtaint, shape) with
+        | (`None | `Clean), Bot -> new_shape
+        | _ -> unify_shape new_shape shape
+      in
       match xtaint with
       | `None
       | `Clean ->
@@ -701,10 +854,14 @@ let rec clean_cell (offset : T.offset list) cell =
 and clean_shape offset shape =
   match shape with
   | Bot
+  | Scalar _
   | Arg _ ->
       let shape = Obj Fields.empty in
       clean_shape offset shape
+  | Instance (name, obj) -> Instance (name, clean_obj offset obj)
+  | Array obj -> Array (clean_obj offset obj)
   | Obj obj -> Obj (clean_obj offset obj)
+  | Closure _
   | Fun _ ->
       (* This is an error, we just don't want to crash here. *)
       Log.err (fun m ->
@@ -740,16 +897,22 @@ let rec enum_in_cell cell : (T.offset list * Taints.t) Seq.t =
   Seq.append x (enum_in_shape shape)
 
 and enum_in_shape = function
-  | Bot -> Seq.empty
-  | Obj obj -> enum_in_obj obj
+  | Bot
+  | Scalar _ ->
+      Seq.empty
+  | Instance (_, obj)
+  | Obj obj
+  | Array obj ->
+      enum_in_obj obj
   | Arg _ ->
       (* TODO: First need to record taint shapes in 'ToLval'.  *)
       Seq.empty
-  | Fun _ -> Seq.empty
+  | Closure _
+  | Fun _ ->
+      Seq.empty
 
 and enum_in_obj obj =
-  obj
-  |> Fields.to_seq
+  obj |> Fields.to_seq
   |> Seq.map (fun (o, cell) ->
          enum_in_cell cell
          |> Seq.map (fun (offset, taints) -> (o :: offset, taints)))
diff --git a/src/tainting/Taint_shape.mli b/src/tainting/Taint_shape.mli
index 535cc52..267f13b 100644
--- a/src/tainting/Taint_shape.mli
+++ b/src/tainting/Taint_shape.mli
@@ -3,8 +3,8 @@
 open Shape_and_sig.Shape
 
 val taints_and_shape_are_relevant : Taint.taints -> shape -> bool
-(** [true] iff the union of [taints] and [gather_all_taints_in_shape shape]
- * is non-empty, or if [shape] contains a cleaned offset. *)
+(** [true] iff the union of [taints] and [gather_all_taints_in_shape shape] * is
+    non-empty, or if [shape] contains a cleaned offset. *)
 
 val fix_poly_taint_with_offset :
   Taint.offset list -> Taint.taints -> Taint.taints
@@ -13,9 +13,9 @@ val fix_poly_taint_with_offset :
 
     FEATURE(field-sensitivity) *)
 
-val tuple_like_obj : (Taint.taints * shape) list -> shape
-(** Constructs a 0-indexed tuple-like 'obj' from a list of pairs, taints and shape,
- * for each element in the tuple.  *)
+val tuple_like_obj : ?array:bool -> (Taint.taints * shape) list -> shape
+(** Constructs a 0-indexed tuple-like 'obj' from a list of pairs, taints and
+    shape, * for each element in the tuple. *)
 
 (* THINK: Replace polymorphic variant with a parameterized IL.field_or_entry ? *)
 val record_or_dict_like_obj :
@@ -25,11 +25,15 @@ val record_or_dict_like_obj :
   list
   (** see 'IL.field_or_entry' *) ->
   shape
-(** Constructs an 'Obj' shape from a list of taints and shapes associated with
-    a record/dict expression. *)
+(** Constructs an 'Obj' shape from a list of taints and shapes associated with a
+    record/dict expression. *)
 
 val unify_cell : cell -> cell -> cell
-(** Unify two 'cell's into one. *)
+(** Unify two cells. *)
+
+val join_cell : cell -> cell -> cell
+(** Join control-flow alternatives, retaining inherited taint for absent fields.
+*)
 
 val unify_shape : shape -> shape -> shape
 (** Unify two 'shapes's into one. *)
@@ -77,13 +81,13 @@ val find_in_cell :
 
 val find_in_cell_poly :
   Taint.offset list -> cell -> (Taint.taints * shape) option
-(** Find an offset in a cell, BUT if the full offset cannot be found, then
-    it returns the taints of the offset prefix that was found; and if those
-    taints are polymorphic, then it adds to them the remaining offset.
+(** Find an offset in a cell, BUT if the full offset cannot be found, then it
+    returns the taints of the offset prefix that was found; and if those taints
+    are polymorphic, then it adds to them the remaining offset.
 
     For example, if `x` is tainted but `x.a` is not being tracked, it just
-    assigns to `x.a` the same taints as `x`. If `x` had polymorphic taint
-    (see 'Taint.Var'), then it would attach the offset `.a` to it.
+    assigns to `x.a` the same taints as `x`. If `x` had polymorphic taint (see
+    'Taint.Var'), then it would attach the offset `.a` to it.
 
     TODO: We need to fix polymorphic shapes too instad of just returning 'Bot'.
 
@@ -94,8 +98,8 @@ val find_in_shape_poly :
   Taint.offset list ->
   shape ->
   (Taint.taints * shape) option
-(** Like 'find_in_cell_poly' but to find an offset in a shape, the 'taints'
-    are the "base taints" in case the offset cannot be found. *)
+(** Like 'find_in_cell_poly' but to find an offset in a shape, the 'taints' are
+    the "base taints" in case the offset cannot be found. *)
 
 val update_offset_in_cell :
   f:(Xtaint.t -> shape -> Xtaint.t * shape) ->
@@ -105,13 +109,13 @@ val update_offset_in_cell :
 
 val update_offset_and_unify :
   Taint.taints -> shape -> Taint.offset list -> cell option -> cell option
-(** Given a 'cell' and an 'offset', it finds the corresponding sub-'cell'
- * for that 'offset', and it updates its 'taints' and 'shape'. If no 'cell'
- * is given (i.e. 'None'), it creates a fresh one. If 'taints' are empty
- * and 'shape' is 'Bot', it just returns the given 'cell' (or 'None'). *)
+(** Given a 'cell' and an 'offset', it finds the corresponding sub-'cell' * for
+    that 'offset', and it updates its 'taints' and 'shape'. If no 'cell' * is
+    given (i.e. 'None'), it creates a fresh one. If 'taints' are empty * and
+    'shape' is 'Bot', it just returns the given 'cell' (or 'None'). *)
 
 val clean_cell : Taint.offset list -> cell -> cell
-(** [clean_cell offset cell] marks the 'offset' in 'cell' as clean.  *)
+(** [clean_cell offset cell] marks the 'offset' in 'cell' as clean. *)
 
 val enum_in_cell : cell -> (Taint.offset list * Taint.taints) Seq.t
 (**
diff --git a/src/tainting/Taint_signature_extractor.ml b/src/tainting/Taint_signature_extractor.ml
index 635b94e..553f78a 100644
--- a/src/tainting/Taint_signature_extractor.ml
+++ b/src/tainting/Taint_signature_extractor.ml
@@ -367,6 +367,7 @@ let extract_signature (taint_inst : TRI.t) ?(in_env : Taint_lval_env.t option)
   let signature =
     {
       Signature.params;
+      closures = IL.NameSet.empty;
       params_il = func_cfg.params;
       effects = effects_with_preconditions;
     }
@@ -389,7 +390,7 @@ let mk_global_assumptions_with_sids (lang : Lang.t)
              {
                ident = (var_name, fake_tok);
                sid;
-               id_info = G.empty_id_info ();
+               id_info = G.empty_id_info (); value_origin = None;
              }
          in
          let il_lval : IL.lval = { base = Var var_id; rev_offset = [] } in
@@ -410,7 +411,7 @@ let mk_global_tracking_without_taint (lang : Lang.t)
              {
                ident = (var_name, fake_tok);
                sid;
-               id_info = G.empty_id_info ();
+               id_info = G.empty_id_info (); value_origin = None;
              }
          in
          let il_lval : IL.lval = { base = Var var_id; rev_offset = [] } in
