diff --git a/languages/cairo/generic/Parse_cairo_tree_sitter.ml b/languages/cairo/generic/Parse_cairo_tree_sitter.ml
index 992ddb9..56918f6 100644
--- a/languages/cairo/generic/Parse_cairo_tree_sitter.ml
+++ b/languages/cairo/generic/Parse_cairo_tree_sitter.ml
@@ -129,6 +129,12 @@ let rec map_qualified_name (env : env) (x : CST.qualified_name) :
     name_info = G.empty_id_info ();
   }
 
+and map_expression_name env x =
+  let qualified = map_qualified_name env x in
+  match qualified.G.name_middle, qualified.G.name_last with
+  | Some (G.QDots []), (id, None) -> G.Id (id, qualified.G.name_info)
+  | _ -> G.IdQualified qualified
+
 and map_type_arguments (env : env) (x : CST.type_argument_list) :
     G.type_arguments =
   let map_type_argument (x : CST.literal_expression) : G.type_argument =
@@ -238,7 +244,7 @@ and map_expression (env : env) (x : CST.expression) =
   | `Match_exp x -> map_match_expression env x
   | `Sele_exp x -> map_selector_expression env x
   | `Call_exp x -> map_call_expression env x
-  | `Qual_name x -> G.N (G.IdQualified (map_qualified_name env x)) |> G.e
+  | `Qual_name x -> G.N (map_expression_name env x) |> G.e
   | `Choice_true x -> G.L (map_literal env x) |> G.e
   | `Ellips x -> G.Ellipsis (token env x) |> G.e
   | `Deep_ellips x -> map_deep_ellipsis env x |> G.e
@@ -253,7 +259,7 @@ and map_simple_expression (env : env) (x : CST.simple_expression) : G.expr =
   | `Match_exp x -> map_match_expression env x
   | `Sele_exp x -> map_selector_expression env x
   | `Call_exp x -> map_call_expression env x
-  | `Qual_name x -> G.N (G.IdQualified (map_qualified_name env x)) |> G.e
+  | `Qual_name x -> G.N (map_expression_name env x) |> G.e
   | `Choice_true x -> G.L (map_literal env x) |> G.e
   | `Ellips x -> G.Ellipsis (token env x) |> G.e
   | `Deep_ellips x -> map_deep_ellipsis env x |> G.e
diff --git a/languages/javascript/ast/ast_js.ml b/languages/javascript/ast/ast_js.ml
index 5f96952..4643b28 100644
--- a/languages/javascript/ast/ast_js.ml
+++ b/languages/javascript/ast/ast_js.ml
@@ -698,7 +698,9 @@ let parameter_to_pattern tok (param : parameter) : a_pattern =
       let pat =
         match p_dots with
         | None -> Id p_name
-        | Some tok -> IdSpecial (Spread, tok)
+        | Some tok ->
+            Apply
+              (IdSpecial (Spread, tok), Tok.unsafe_fake_bracket [ Id p_name ])
       in
       let pat =
         match p_type with
diff --git a/languages/javascript/generic/js_to_generic.ml b/languages/javascript/generic/js_to_generic.ml
index 3e26280..0899205 100644
--- a/languages/javascript/generic/js_to_generic.ml
+++ b/languages/javascript/generic/js_to_generic.ml
@@ -33,6 +33,16 @@ module Log = Log_parser_javascript.Log
 let id x = x
 let option = Option.map
 let list = List_.map
+
+let named_expression name expression =
+  match name with
+  | None -> expression
+  | Some id ->
+      G.OtherExpr
+        ( ("NamedExpression", snd id),
+          [ G.Name (G.Id (id, G.empty_id_info ())); G.E expression ] )
+      |> G.e
+
 let bool = id
 let string = id
 let error = AST_generic.error
@@ -57,6 +67,34 @@ let ident x = name x
 let filename v = wrap string v
 let label v = wrap string v
 
+let rec binding_expression = function
+  | IdSpecial
+      ((Require | Eval | Undefined | Exports | Module | Define | Arguments), tok)
+    ->
+      Id (Tok.content_of_tok tok, tok)
+  | Assign (left, tok, right) -> Assign (binding_expression left, tok, right)
+  | Arr (left, values, right) ->
+      Arr (left, List_.map binding_expression values, right)
+  | Obj (left, fields, right) ->
+      Obj (left, List_.map binding_property fields, right)
+  | Apply ((IdSpecial (Spread, _) as spread), (left, values, right)) ->
+      Apply (spread, (left, List_.map binding_expression values, right))
+  | ParenExpr (left, value, right) ->
+      ParenExpr (left, binding_expression value, right)
+  | Cast (value, tok, type_) -> Cast (binding_expression value, tok, type_)
+  | TypeAssert (value, tok, type_) ->
+      TypeAssert (binding_expression value, tok, type_)
+  | expression -> expression
+
+and binding_property = function
+  | FieldColon field ->
+      FieldColon
+        { field with fld_body = Option.map binding_expression field.fld_body }
+  | FieldSpread (tok, value) -> FieldSpread (tok, binding_expression value)
+  | FieldPatDefault (left, tok, right) ->
+      FieldPatDefault (binding_expression left, tok, right)
+  | field -> field
+
 type special_result =
   | SR_Special of G.special wrap
   | SR_Other of G.todo_kind
@@ -264,7 +302,7 @@ and expr (x : expr) =
        | SR_Expr e -> e)
       |> G.e
   | Assign (v1, tok, v2) ->
-      let v1 = expr v1 and v2 = expr v2 in
+      let v1 = expr (binding_expression v1) and v2 = expr v2 in
       let tok = info tok in
       G.Assign (v1, tok, v2) |> G.e
   | ArrAccess (v1, v2) ->
@@ -283,9 +321,9 @@ and expr (x : expr) =
       let v1 = name v1 and v2 = type_ v2 in
       let tok = info tok in
       G.TypedMetavar (v1, tok, v2) |> G.e
-  | Class (v1, _v2TODO) ->
+  | Class (v1, name) ->
       let def, _more_attrsTODOEMPTY = class_ v1 in
-      G.AnonClass def |> G.e
+      G.AnonClass def |> G.e |> named_expression name
   | ObjAccess (v1, t, v2) ->
       let e = expr v1 in
       let t, v1 =
@@ -302,7 +340,7 @@ and expr (x : expr) =
       | Left n -> G.DotAccess (v1, t, G.FN (G.Id (n, G.empty_id_info ())))
       | Right e -> G.DotAccess (v1, t, G.FDynamic e))
       |> G.e
-  | Fun (v1, _v2TODO) ->
+  | Fun (v1, name) ->
       let def, more_attrs = fun_ v1 in
       (* TODO: Include attrs in generic AST? Where? *)
       let e = G.Lambda def |> G.e in
@@ -311,7 +349,7 @@ and expr (x : expr) =
        * https://github.com/semgrep/semgrep/issues/7353 *)
       let attrs_any = List_.map (fun attr -> G.At attr) more_attrs in
       H.set_e_range_with_anys (G.Dk (G.FuncDef def) :: attrs_any) e;
-      e
+      named_expression name e
   | Apply (IdSpecial v1, v2) ->
       let x = special v1 in
       let v2 = bracket (list expr) v2 in
@@ -375,9 +413,15 @@ and stmt x =
   | While (t, v1, v2) ->
       let v1 = expr v1 and v2 = stmt v2 in
       G.While (t, G.Cond v1, v2) |> G.s
-  | For (t, v1, v2) ->
-      let v1 = for_header v1 and v2 = stmt v2 in
-      G.For (t, v1, v2) |> G.s
+  | For (t, header, body) -> (
+      let loop = G.For (t, for_header header, stmt body) |> G.s in
+      match header with
+      | ForIn (Left ((_, { v_kind = Var, _; _ }) as variable), _, _)
+      | ForOf (Left ((_, { v_kind = Var, _; _ }) as variable), _, _) ->
+          let entity, variable = var_of_var variable in
+          G.Block (fb [ G.DefStmt (entity, G.VarDef variable) |> G.s; loop ])
+          |> G.s
+      | _ -> loop)
   | Switch (v0, v1, v2) ->
       let v0 = info v0 in
       let v1 = expr v1 and v2 = list case v2 in
@@ -409,7 +453,7 @@ and stmt x =
 
 and catch_block = function
   | BoundCatch (t, v1, v2) ->
-      let v1 = H.expr_to_pattern (expr v1) and v2 = stmt v2 in
+      let v1 = pattern v1 and v2 = stmt v2 in
       (t, G.CatchPattern v1, v2)
   | UnboundCatch (t, v1) ->
       let v1 =
@@ -609,11 +653,22 @@ and parameter_binding = function
       in
       let tk = AST_generic_helpers.first_info_of_any (G.P pat) in
       let pdefault = Option.map expr pp_default in
-      G.ParamPattern (pat, G.implicit_param_classic ?pdefault tk)
+      let rec rest_binding = function
+        | G.PatTyped (inner, _) -> rest_binding inner
+        | G.PatConstructor (G.Id (("...", tok), _), [ G.PatId (id, info) ]) ->
+            Some (tok, id, info)
+        | _ -> None
+      in
+      (match rest_binding pat with
+      | Some (tok, id, info) ->
+          G.ParamRest
+            (tok, { G.pname = Some id; pinfo = info; pdefault;
+                    ptype = Option.map type_ pp_type; pattrs = [] })
+      | None -> G.ParamPattern (pat, G.implicit_param_classic ?pdefault tk))
   | ParamEllipsis x -> G.ParamEllipsis x
 
 and pattern x =
-  let x = expr x in
+  let x = expr (binding_expression x) in
   H.expr_to_pattern x
 
 and parameter x =
@@ -720,8 +775,7 @@ and field_classic
       let g_def =
         match fkind with
         | G.Arrow ->
-            G.VarDef
-              { G.vinit = option expr v3; vtype = vt; vtok = G.no_sc }
+            G.VarDef { G.vinit = option expr v3; vtype = vt; vtok = G.no_sc }
         | _ -> G.FuncDef { def with G.fkind = (fkind, tok) }
       in
       ({ ent with G.attrs = ent.G.attrs @ more_attrs }, g_def)
diff --git a/languages/lisp/tree-sitter/Parse_clojure_tree_sitter.ml b/languages/lisp/tree-sitter/Parse_clojure_tree_sitter.ml
index 5b31c2d..5ad686a 100644
--- a/languages/lisp/tree-sitter/Parse_clojure_tree_sitter.ml
+++ b/languages/lisp/tree-sitter/Parse_clojure_tree_sitter.ml
@@ -66,7 +66,10 @@ type syntax_mode =
   | Quoted
   | Syntax_quoted
 
+type dialect = Clojure | CommonLisp | Scheme
+
 type extra = {
+  dialect: dialect;
   kind: parse_kind;
   mode: syntax_mode;
   ns: string option; (* alt: or in split form? [clojure; core] etc; or G.name? *)
@@ -375,6 +378,14 @@ let name_from_ident ?(is_auto_resolved = false) ?(is_kwd = false)
           (* NOTE: We don't assume well formed, 'clojure..core'
            * won't throw but will contain empty idents in the list. *)
       in
+      let ids_before =
+        if is_kwd || env.extra.mode <> Normal then ids_before
+        else match List.assoc_opt before env.extra.ns_aliases with
+          | None -> ids_before
+          | Some namespace ->
+              List.map (fun part -> (part, t_before))
+                (String.split_on_char '.' namespace)
+      in
       let should_add_colon =
         is_kwd && not (in_pattern env && G.is_metavar_name after)
       in
@@ -594,6 +605,7 @@ and map_list_form
   (* TODO:
    * - if not env says quoted;
    * - if not under defmacro. *)
+  if not (env.extra.dialect =*= Clojure) then map_lisp_form env forms else
   match discriminant with
     (* TODO: Check if in Quoted state! In that case just symbols list. *)
     | Some ("def" | "defonce") -> map_def_form env forms
@@ -639,6 +651,96 @@ and map_list_form
     | Some "quote" -> map_quote_form env forms
     | _ -> map_call_form env forms
 
+and lisp_list_forms env = function
+  | `List_lit (_, (_, source, _)) -> forms_in_source env source
+  | _ -> raise_parse_error "Expected a parenthesized binding or parameter list"
+
+and lisp_body env forms =
+  match forms with
+  | [] -> G.L (G.Null (G.fake "nil")) |> G.e
+  | _ -> G.Seq (List_.map (map_form env) forms) |> G.e
+
+and lisp_parameters env forms =
+  forms |> List_.map (function
+    | `Sym_lit (_, ((_, "...") as symbol)) when in_pattern env -> G.ParamEllipsis (token env symbol)
+    | `Sym_lit (_, ((_, name) as symbol)) when not (String.starts_with ~prefix:"&" name) -> G.Param (G.param_of_id (H.str env symbol))
+    | _ -> raise_parse_error "Unsupported Lisp parameter binding")
+
+and lisp_function env tok params body =
+  { G.fkind = (G.Function, tok);
+    fparams = Tok.unsafe_fake_bracket (lisp_parameters env params);
+    frettype = None;
+    fbody = G.FBExpr (lisp_body env body) }
+
+and lisp_definition env tok symbol params body =
+  let entity = G.basic_entity (H.str env symbol) in
+  G.DefStmt (entity, G.FuncDef (lisp_function env tok params body))
+  |> G.s |> G.stmt_to_expr
+
+and lisp_bindings env tok sequential bindings body =
+  let pairs = bindings |> List_.map (function
+    | `List_lit (_, (_, source, _)) ->
+        (match forms_in_source env source with
+         | [`Sym_lit (_, symbol); value] ->
+             G.PatId (H.str env symbol, G.empty_id_info ()), map_form env value
+         | [`Sym_lit (_, symbol)] when env.extra.dialect =*= CommonLisp ->
+             G.PatId (H.str env symbol, G.empty_id_info ()), (G.L (G.Null tok) |> G.e)
+         | _ -> raise_parse_error "Unsupported Lisp binding")
+    | `Sym_lit (_, symbol) when env.extra.dialect =*= CommonLisp ->
+        G.PatId (H.str env symbol, G.empty_id_info ()), (G.L (G.Null tok) |> G.e)
+    | _ -> raise_parse_error "Unsupported Lisp binding") in
+  let declarations =
+    if sequential then pairs |> List_.map (fun (pat, value) -> G.LetPattern (pat, value) |> G.e |> G.exprstmt)
+    else
+      let patterns, values = List.split pairs in
+      [G.LetPattern (G.PatTuple (Tok.unsafe_fake_bracket patterns),
+                     G.Container (G.Tuple, Tok.unsafe_fake_bracket values) |> G.e)
+       |> G.e |> G.exprstmt] in
+  G.Block (Tok.unsafe_fake_bracket (declarations @ [G.exprstmt (lisp_body env body)]))
+  |> G.s |> G.stmt_to_expr
+
+and map_lisp_form env forms =
+  match forms with
+  | [] -> G.L (G.Null (G.fake "nil")) |> G.e
+  | `Sym_lit (_, ((_, "defun") as head)) :: `Sym_lit (_, name) :: params :: body
+      when env.extra.dialect =*= CommonLisp ->
+      lisp_definition env (token env head) name (lisp_list_forms env params) body
+  | `Sym_lit (_, ((_, "define") as head)) :: signature :: body
+      when env.extra.dialect =*= Scheme ->
+      (match signature with
+       | `List_lit _ ->
+           (match lisp_list_forms env signature with
+            | `Sym_lit (_, name) :: params -> lisp_definition env (token env head) name params body
+            | _ -> raise_parse_error "Unsupported Scheme definition")
+       | `Sym_lit (_, name) ->
+           (match body with
+            | [value] -> G.DefStmt (G.basic_entity (H.str env name),
+                G.VarDef {G.vinit = Some (map_form env value); vtype = None; vtok = G.no_sc}) |> G.s |> G.stmt_to_expr
+            | _ -> raise_parse_error "Unsupported Scheme definition")
+       | _ -> raise_parse_error "Unsupported Scheme definition")
+  | `Sym_lit (_, ((_, "lambda") as head)) :: params :: body ->
+      G.Lambda (lisp_function env (token env head) (lisp_list_forms env params) body) |> G.e
+  | `Sym_lit (_, ((_, ("let" | "let*" as form)) as head)) :: bindings :: body ->
+      lisp_bindings env (token env head) (form = "let*") (lisp_list_forms env bindings) body
+  | `Sym_lit (_, (_, "quote")) :: _ -> map_quote_form env forms
+  | `Sym_lit (_, (_, "begin")) :: body when env.extra.dialect =*= Scheme -> lisp_body env body
+  | `Sym_lit (_, (_, "progn")) :: body when env.extra.dialect =*= CommonLisp -> lisp_body env body
+  | `Sym_lit (_, ((_, "if") as head)) :: condition :: yes :: rest ->
+      let no = match rest with
+        | [] -> None
+        | [value] -> Some (G.exprstmt (map_form env value))
+        | _ -> raise_parse_error "Unsupported Lisp conditional" in
+      G.If (token env head, G.Cond (map_form env condition), G.exprstmt (map_form env yes), no)
+      |> G.s |> G.stmt_to_expr
+  | `Sym_lit (_, ((_, ("set!" | "setq" as form)) as head)) :: `Sym_lit (_, name) :: [value]
+      when (form = "set!" && env.extra.dialect =*= Scheme) || (form = "setq" && env.extra.dialect =*= CommonLisp) ->
+      G.Assign (G.N (G.Id (H.str env name, G.empty_id_info ())) |> G.e, token env head, map_form env value) |> G.e
+  | `Sym_lit (_, (_, ("flet" | "labels" | "macrolet" | "symbol-macrolet" | "defmacro"
+      | "defmethod" | "defgeneric" | "destructuring-bind" | "multiple-value-bind"
+      | "letrec" | "letrec*" | "define-syntax" | "syntax-rules" | "quasiquote" | "unquote"))) :: _ ->
+      raise_parse_error "Unsupported Lisp binding or macro semantics"
+  | _ -> map_call_form env forms
+
 and map_form (env : env) (x : CST.form) : G.expr =
   match x with
   | `Semg_deep_exp (lt, _v2_gap, expr_form, _v4_gap, gt) ->
@@ -2800,30 +2902,58 @@ and forms_in_source (env : env) (xs : CST.source) =
   map_forms_in_source (fun _env x -> x) env xs
 
 (* This is for top-level stuff, used below in [parse]. *)
+and namespace_environment (env : env) (form : CST.form) : env =
+  match env.extra.dialect, env.extra.kind, form with
+  | Clojure, Program, `List_lit (_, (_, source, _)) -> (
+      match forms_in_source env source with
+      | `Sym_lit (_, (_, "ns")) :: `Sym_lit (_, (_, namespace)) :: clauses ->
+          let aliases = List.concat_map
+            (function
+              | `List_lit (_, (_, clause_source, _)) -> (
+                  match forms_in_source env clause_source with
+                  | `Kwd_lit (_, ":require") :: libraries ->
+                      List.filter_map
+                        (function
+                          | `Vec_lit (_, (_, library_source, _)) -> (
+                              match forms_in_source env library_source with
+                              | `Sym_lit (_, (_, library)) :: options ->
+                                  let rec alias = function
+                                    | `Kwd_lit (_, ":as") :: `Sym_lit (_, (_, name)) :: _ ->
+                                        Some (name, library)
+                                    | _ :: rest -> alias rest
+                                    | [] -> None
+                                  in
+                                  alias options
+                              | _ -> None)
+                          | _ -> None)
+                        libraries
+                  | _ -> [])
+              | _ -> []) clauses
+          in
+          {env with extra = {env.extra with ns = Some namespace; ns_aliases = aliases}}
+      | `Sym_lit (_, (_, ("in-ns" | "alias" | "ns-unalias" | "require"))) :: _ ->
+          {env with extra = {env.extra with ns_aliases = []}}
+      | _ -> env)
+  | _ -> env
+
 and map_program (env : env) (xs : CST.source) : G.program =
-  map_forms_in_source
-    (fun env x ->
-       let x = map_form env x in
-       (* In reality stuff like `def`, `defn` etc will already be
-        * statements. So these can return ExprStmt and se can unpack it.
-        * Else we wrap into Stmt at this top level only. That is, a file
-        * is a list of statements, but inside we work with expressions
-        * (modulo If etc which exist as statements only). *)
-       expr_to_stmt x)
-    env
-    xs
+  snd (fold_forms_in_source
+    (fun env form ->
+       let env = namespace_environment env form in
+       env, expr_to_stmt (map_form env form))
+    env xs)
 
 (*****************************************************************************)
 (* Entry point *)
 (*****************************************************************************)
 
-let parse file =
+let parse ~dialect file =
   H.wrap_parser
     (fun () -> Tree_sitter_clojure.Parse.file !!file)
     (fun cst _extras ->
       let env = { H.file;
                   conv = H.line_col_to_pos file;
-                  extra = {kind = Program; mode = Normal;
+                  extra = {dialect; kind = Program; mode = Normal;
                            ns = None; ns_aliases = []} }
       in
       map_program env cst)
@@ -2849,7 +2979,7 @@ let with_expr_stmt_seq_to_block = function
     |> G.e |> expr_to_stmt |> (fun e -> [e])
   | ss -> ss
 
-let parse_pattern str =
+let parse_pattern ~dialect str =
   H.wrap_parser
     (fun () -> Tree_sitter_clojure.Parse.string str)
     (fun cst _extras ->
@@ -2863,7 +2993,7 @@ let parse_pattern str =
                    * maybe in patterns the implied ns should be a
                    * metavar, perhaps anonymous, so it can be matched.
                    * *)
-                  extra = {kind = Pattern; mode = Normal;
+                  extra = {dialect; kind = Pattern; mode = Normal;
                            ns = None; ns_aliases = []} }
       in
       (* XXX: Need sequence of ExprStmt to one ExprStmt with ExprBlock inside.
diff --git a/languages/lisp/tree-sitter/Parse_clojure_tree_sitter.mli b/languages/lisp/tree-sitter/Parse_clojure_tree_sitter.mli
index b6d8e93..4f9979e 100644
--- a/languages/lisp/tree-sitter/Parse_clojure_tree_sitter.mli
+++ b/languages/lisp/tree-sitter/Parse_clojure_tree_sitter.mli
@@ -1,5 +1,7 @@
+type dialect = Clojure | CommonLisp | Scheme
+
 val parse :
-  Fpath.t -> (AST_generic.program, unit) Tree_sitter_run.Parsing_result.t
+  dialect:dialect -> Fpath.t -> (AST_generic.program, unit) Tree_sitter_run.Parsing_result.t
 
 val parse_pattern :
-  string -> (AST_generic.any, unit) Tree_sitter_run.Parsing_result.t
+  dialect:dialect -> string -> (AST_generic.any, unit) Tree_sitter_run.Parsing_result.t
diff --git a/languages/typescript/tree-sitter/Parse_typescript_tree_sitter.ml b/languages/typescript/tree-sitter/Parse_typescript_tree_sitter.ml
index 92c0ae0..6864e4e 100644
--- a/languages/typescript/tree-sitter/Parse_typescript_tree_sitter.ml
+++ b/languages/typescript/tree-sitter/Parse_typescript_tree_sitter.ml
@@ -1,9 +1,7 @@
-(**
-   Derive a JS/TS AST from a tree-sitter TS (or TSX) CST.
+(** Derive a JS/TS AST from a tree-sitter TS (or TSX) CST.
 
-   This is derived from generated code 'semgrep-typescript/lib/Boilerplate.ml'
-   in tree-sitter-lang.
-*)
+    This is derived from generated code 'semgrep-typescript/lib/Boilerplate.ml'
+    in tree-sitter-lang. *)
 
 open Common
 open Fpath_.Operators
@@ -125,7 +123,10 @@ let this env tok = IdSpecial (This, token env tok)
 let super env tok = IdSpecial (Super, token env tok)
 
 let number (env : env) (tok : CST.number) =
-  let s, t = str env tok (* number *) in
+  let s, t =
+    str env tok
+    (* number *)
+  in
   (* TODO? float_of_string_opt_also_from_hexoctbin *)
   (Common2.float_of_string_opt s, t)
 
@@ -146,7 +147,10 @@ let number_as_string (env : env) (tok : CST.number) =
 let string_ (env : env) (x : CST.string_) : string wrap =
   match x with
   | `DQUOT_rep_choice_unes_double_str_frag_DQUOT (v1, v2, v3) ->
-      let open_ = token env v1 (* "\"" *) in
+      let open_ =
+        token env v1
+        (* "\"" *)
+      in
       let contents =
         List_.map
           (fun x ->
@@ -156,12 +160,18 @@ let string_ (env : env) (x : CST.string_) : string wrap =
             | `Esc_seq tok -> (* escape_sequence *) str env tok)
           v2
       in
-      let close = token env v3 (* "\"" *) in
+      let close =
+        token env v3
+        (* "\"" *)
+      in
       let str = contents |> List_.map fst |> String.concat "" in
       let toks = (contents |> List_.map snd) @ [ close ] in
       (str, Tok.combine_toks open_ toks)
   | `SQUOT_rep_choice_unes_single_str_frag_SQUOT (v1, v2, v3) ->
-      let open_ = token env v1 (* "'" *) in
+      let open_ =
+        token env v1
+        (* "'" *)
+      in
       let v2 =
         List_.map
           (fun x ->
@@ -171,15 +181,27 @@ let string_ (env : env) (x : CST.string_) : string wrap =
             (* escape_sequence *))
           v2
       in
-      let close = token env v3 (* "'" *) in
+      let close =
+        token env v3
+        (* "'" *)
+      in
       let str = v2 |> List_.map fst |> String.concat "" in
       let toks = (v2 |> List_.map snd) @ [ close ] in
       (str, Tok.combine_toks open_ toks)
 
 let namespace_import (env : env) ((v1, v2, v3) : CST.namespace_import_export) =
-  let star = token env v1 (* "*" *) in
-  let _as = token env v2 (* "as" *) in
-  let id = identifier env v3 (* identifier *) in
+  let star =
+    token env v1
+    (* "*" *)
+  in
+  let _as =
+    token env v2
+    (* "as" *)
+  in
+  let id =
+    identifier env v3
+    (* identifier *)
+  in
   (star, id)
 
 let jsx_identifier_ (env : env) (x : CST.jsx_identifier_) =
@@ -190,7 +212,10 @@ let jsx_identifier_ (env : env) (x : CST.jsx_identifier_) =
 
 let jsx_namespace_name (env : env) ((v1, v2, v3) : CST.jsx_namespace_name) =
   let v1 = jsx_identifier_ env v1 in
-  let _v2 = token env v2 (* ":" *) in
+  let _v2 =
+    token env v2
+    (* ":" *)
+  in
   let v3 = jsx_identifier_ env v3 in
   (v1, v3)
 
@@ -210,8 +235,14 @@ let rec id_or_nested_id (env : env) (x : CST.anon_choice_type_id_42c0412) :
 
 and nested_identifier (env : env) ((v1, v2, v3) : CST.nested_identifier) =
   let v1 = id_or_nested_id env v1 in
-  let _v2 = token env v2 (* "." *) in
-  let v3 = identifier env v3 (* identifier *) in
+  let _v2 =
+    token env v2
+    (* "." *)
+  in
+  let v3 =
+    identifier env v3
+    (* identifier *)
+  in
   v1 @ [ v3 ]
 
 let jsx_element_name (env : env) (x : CST.jsx_element_name) : a_ident =
@@ -233,15 +264,27 @@ let jsx_element_name (env : env) (x : CST.jsx_element_name) : a_ident =
 
 let jsx_closing_element (env : env) ((v1, v2, v3, v4) : CST.jsx_closing_element)
     =
-  let v1 = token env v1 (* "<" *) in
-  let v2 = token env v2 (* "/" *) in
+  let v1 =
+    token env v1
+    (* "<" *)
+  in
+  let v2 =
+    token env v2
+    (* "/" *)
+  in
   let str, v3 = jsx_element_name env v3 in
-  let v4 = token env v4 (* ">" *) in
+  let v4 =
+    token env v4
+    (* ">" *)
+  in
   let t = Tok.combine_toks v1 [ v2; v3; v4 ] in
   (str, t)
 
 let from_clause (env : env) ((v1, v2) : CST.from_clause) : tok * string wrap =
-  let v1 = token env v1 (* "from" *) in
+  let v1 =
+    token env v1
+    (* "from" *)
+  in
   let v2 = string_ env v2 in
   (v1, v2)
 
@@ -345,7 +388,6 @@ let anon_choice_COMMA_5194cb4 (env : env) (x : CST.anon_choice_COMMA_5194cb4) =
 let import_export_specifier (env : env)
     ((_v1, v2, v3) : CST.import_export_specifier) :
     (a_ident * a_ident option) option =
-  
   let opt_as_id =
     match v3 with
     | None -> None
@@ -378,12 +420,27 @@ let concat_nested_identifier (idents : a_ident list) : a_ident =
  *)
 let import_require_clause tk (env : env)
     ((v1, v2, v3, v4, v5, v6) : CST.import_require_clause) =
-  let v1 = identifier env v1 (* identifier *) in
-  let _v2 = token env v2 (* "=" *) in
-  let _v3 = identifier env v3 (* "require" *) in
-  let _v4 = token env v4 (* "(" *) in
+  let v1 =
+    identifier env v1
+    (* identifier *)
+  in
+  let _v2 =
+    token env v2
+    (* "=" *)
+  in
+  let _v3 =
+    identifier env v3
+    (* "require" *)
+  in
+  let _v4 =
+    token env v4
+    (* "(" *)
+  in
   let v5 = string_ env v5 in
-  let _v6 = token env v6 (* ")" *) in
+  let _v6 =
+    token env v6
+    (* ")" *)
+  in
   ModuleAlias (tk, v1, v5)
 
 let literal_type (env : env) (x : CST.literal_type) : expr =
@@ -394,7 +451,10 @@ let literal_type (env : env) (x : CST.literal_type) : expr =
         | `DASH tok -> str env tok (* "-" *)
         | `PLUS tok -> str env tok (* "+" *)
       in
-      let s2, t2 = str env v2 (* number *) in
+      let s2, t2 =
+        str env v2
+        (* number *)
+      in
       (* TODO: float_of_string_opt_also_from_hexoctbin *)
       L (Num (float_of_string_opt (s ^ s2), Tok.combine_toks t1 [ t2 ]))
   | `Num tok ->
@@ -409,8 +469,14 @@ let literal_type (env : env) (x : CST.literal_type) : expr =
 let nested_type_identifier (env : env)
     ((v1, v2, v3) : CST.nested_type_identifier) : a_ident list =
   let v1 = id_or_nested_id env v1 in
-  let _v2 = token env v2 (* "." *) in
-  let v3 = str env v3 (* identifier *) in
+  let _v2 =
+    token env v2
+    (* "." *)
+  in
+  let v3 =
+    str env v3
+    (* identifier *)
+  in
   v1 @ [ v3 ]
 
 let id_or_reserved_id (env : env)
@@ -429,7 +495,10 @@ let import_export_specifiers (env : env)
   |> List_.filter_map (fun opt -> opt)
 
 let export_clause (env : env) ((v1, v2, v3, v4) : CST.export_clause) =
-  let _open = token env v1 (* "{" *) in
+  let _open =
+    token env v1
+    (* "{" *)
+  in
   let xs =
     match v2 with
     | Some x -> import_export_specifiers env x
@@ -440,11 +509,17 @@ let export_clause (env : env) ((v1, v2, v3, v4) : CST.export_clause) =
     | Some _tok -> (* "," *) ()
     | None -> ()
   in
-  let _close = token env v4 (* "}" *) in
+  let _close =
+    token env v4
+    (* "}" *)
+  in
   xs
 
 let named_imports (env : env) ((v1, v2, v3, v4) : CST.named_imports) =
-  let _open = token env v1 (* "{" *) in
+  let _open =
+    token env v1
+    (* "{" *)
+  in
   let imports =
     match v2 with
     | Some x -> import_export_specifiers env x
@@ -455,7 +530,10 @@ let named_imports (env : env) ((v1, v2, v3, v4) : CST.named_imports) =
     | Some tok -> Some (token env tok) (* "," *)
     | None -> None
   in
-  let _close = token env v4 (* "}" *) in
+  let _close =
+    token env v4
+    (* "}" *)
+  in
   fun (import_tok : tok) (from_path : a_filename) ->
     [ Import (import_tok, imports, from_path) ]
 
@@ -466,11 +544,17 @@ let import_clause (env : env) (x : CST.import_clause) =
       fun tok path -> [ ModuleAlias (tok, id, path) ]
   | `Named_imports x -> named_imports env x
   | `Id_opt_COMMA_choice_name_import_export (v1, v2) ->
-      let v1 = identifier env v1 (* identifier *) in
+      let v1 =
+        identifier env v1
+        (* identifier *)
+      in
       let v2 =
         match v2 with
         | Some (v1, v2) ->
-            let _v1 = token env v1 (* "," *) in
+            let _v1 =
+              token env v1
+              (* "," *)
+            in
             let v2 =
               match v2 with
               | `Name_import_export x ->
@@ -490,8 +574,14 @@ let import_clause (env : env) (x : CST.import_clause) =
 let rec decorator_member_expression (env : env)
     ((v1, v2, v3) : CST.decorator_member_expression) : a_ident list =
   let v1 = anon_choice_type_id_b8f8ced env v1 in
-  let _v2 = token env v2 (* "." *) in
-  let v3 = identifier env v3 (* identifier *) in
+  let _v2 =
+    token env v2
+    (* "." *)
+  in
+  let v3 =
+    identifier env v3
+    (* identifier *)
+  in
   v1 @ [ v3 ]
 
 and anon_choice_type_id_b8f8ced (env : env)
@@ -502,7 +592,10 @@ and anon_choice_type_id_b8f8ced (env : env)
 
 let rec parenthesized_expression (env : env)
     ((v1, v2, v3) : CST.parenthesized_expression) =
-  let v1 = token env v1 (* "(" *) in
+  let v1 =
+    token env v1
+    (* "(" *)
+  in
   let v2 =
     match v2 with
     | `Exp_opt_type_anno (v1, v2) -> (
@@ -514,12 +607,18 @@ let rec parenthesized_expression (env : env)
         | None -> v1)
     | `Seq_exp x -> sequence_expression env x
   in
-  let v3 = token env v3 (* ")" *) in
+  let v3 =
+    token env v3
+    (* ")" *)
+  in
   ParenExpr (v1, v2, v3)
 
 and jsx_opening_element (env : env) ((v1, v2, v3, v4) : CST.jsx_opening_element)
     =
-  let v1 = token env v1 (* "<" *) in
+  let v1 =
+    token env v1
+    (* "<" *)
+  in
   let v2 =
     match v2 with
     | `Choice_choice_jsx_id x -> jsx_attribute_name env x
@@ -539,12 +638,18 @@ and jsx_opening_element (env : env) ((v1, v2, v3, v4) : CST.jsx_opening_element)
         id
   in
   let v3 = List_.map (jsx_attribute_ env) v3 in
-  let v4 = token env v4 (* ">" *) in
+  let v4 =
+    token env v4
+    (* ">" *)
+  in
   (v1, v2, v3, v4)
 
 and jsx_self_clos_elem (env : env)
     ((v1, v2, v3, v4, v5) : CST.jsx_self_closing_element) =
-  let v1 = token env v1 (* "<" *) in
+  let v1 =
+    token env v1
+    (* "<" *)
+  in
   let v2 =
     match v2 with
     | `Choice_choice_jsx_id x -> jsx_attribute_name env x
@@ -559,26 +664,50 @@ and jsx_self_clos_elem (env : env)
         id
   in
   let v3 = List_.map (jsx_attribute_ env) v3 in
-  let v4 = token env v4 (* "/" *) in
-  let v5 = token env v5 (* ">" *) in
+  let v4 =
+    token env v4
+    (* "/" *)
+  in
+  let v5 =
+    token env v5
+    (* ">" *)
+  in
   let t2 = Tok.combine_toks v4 [ v5 ] in
   (v1, v2, v3, t2)
 
 and jsx_fragment (env : env) ((v1, v2, v3, v4, v5, v6) : CST.jsx_fragment) : xml
     =
-  let v1 = token env v1 (* "<" *) in
-  let v2 = token env v2 (* ">" *) in
+  let v1 =
+    token env v1
+    (* "<" *)
+  in
+  let v2 =
+    token env v2
+    (* ">" *)
+  in
   let v3 = List_.map (jsx_child env) v3 in
-  let v4 = token env v4 (* "<" *) in
-  let v5 = token env v5 (* "/" *) in
-  let v6 = token env v6 (* ">" *) in
+  let v4 =
+    token env v4
+    (* "<" *)
+  in
+  let v5 =
+    token env v5
+    (* "/" *)
+  in
+  let v6 =
+    token env v6
+    (* ">" *)
+  in
   let t1 = Tok.combine_toks v1 [ v2 ] in
   let t2 = Tok.combine_toks v4 [ v5; v6 ] in
   { xml_kind = XmlFragment (t1, t2); xml_attrs = []; xml_body = v3 }
 
 and jsx_expression (env : env) ((v1, v2, v3) : CST.jsx_expression) :
     expr option bracket =
-  let v1 = token env v1 (* "{" *) in
+  let v1 =
+    token env v1
+    (* "{" *)
+  in
   let v2 =
     match v2 with
     | Some x ->
@@ -592,7 +721,10 @@ and jsx_expression (env : env) ((v1, v2, v3) : CST.jsx_expression) :
     (* abusing { } in XML to just add comments, e.g. { /* lint-ignore */ } *)
     | None -> None
   in
-  let v3 = token env v3 (* "}" *) in
+  let v3 =
+    token env v3
+    (* "}" *)
+  in
   (v1, v2, v3)
 
 and jsx_attribute_ (env : env) (x : CST.jsx_attribute_) : xml_attribute =
@@ -602,7 +734,10 @@ and jsx_attribute_ (env : env) (x : CST.jsx_attribute_) : xml_attribute =
       let teq, v2 =
         match v2 with
         | Some (v1, v2) ->
-            let v1bis = token env v1 (* "=" *) in
+            let v1bis =
+              token env v1
+              (* "=" *)
+            in
             let v2 = jsx_attribute_value env v2 in
             (v1bis, v2)
         (* see https://www.reactenlightenment.com/react-jsx/5.7.html *)
@@ -642,7 +777,10 @@ and jsx_attribute_value (env : env) (x : CST.jsx_attribute_value) =
 and jsx_child (env : env) (x : CST.jsx_child) : xml_body =
   match x with
   | `Jsx_text tok ->
-      let s = str env tok (* pattern [^{}<>]+ *) in
+      let s =
+        str env tok
+        (* pattern [^{}<>]+ *)
+      in
       XmlText s
   | `Choice_jsx_elem x ->
       let xml = jsx_element_ env x in
@@ -698,7 +836,10 @@ and object_property_pattern (env : env) (x : CST.anon_choice_pair_pat_3ff9cbe) :
   match x with
   | `Pair_pat (v1, v2, v3) ->
       let v1 = property_name env v1 in
-      let _v2 = token env v2 (* ":" *) in
+      let _v2 =
+        token env v2
+        (* ":" *)
+      in
       let body = pat_or_assign_pat env v3 |> sub_pattern in
       let ty = None in
       FieldColon
@@ -717,7 +858,10 @@ and object_property_pattern (env : env) (x : CST.anon_choice_pair_pat_3ff9cbe) :
         | `Choice_choice_decl x -> id_or_reserved_id env x |> idexp
         | `Dest_pat x -> destructuring_pattern env x
       in
-      let tok = token env v2 (* "=" *) in
+      let tok =
+        token env v2
+        (* "=" *)
+      in
       (* default value for the property *)
       let e = expression env v3 in
       FieldPatDefault (pat, tok, e)
@@ -736,7 +880,10 @@ and destructuring_pattern (env : env) (x : CST.destructuring_pattern) :
   match x with
   | `Obj_pat (v1, v2, v3) ->
       (* similar to 'object_' *)
-      let v1 = token env v1 (* "{" *) in
+      let v1 =
+        token env v1
+        (* "{" *)
+      in
       let v2 =
         match v2 with
         | Some (v1, v2) ->
@@ -747,10 +894,16 @@ and destructuring_pattern (env : env) (x : CST.destructuring_pattern) :
             |> List_.flatten
         | None -> []
       in
-      let v3 = token env v3 (* "}" *) in
+      let v3 =
+        token env v3
+        (* "}" *)
+      in
       Obj (v1, v2, v3)
   | `Array_pat (v1, v2, v3) ->
-      let open_ = token env v1 (* "[" *) in
+      let open_ =
+        token env v1
+        (* "[" *)
+      in
       let elements =
         match v2 with
         | Some (v1, v2) ->
@@ -761,12 +914,18 @@ and destructuring_pattern (env : env) (x : CST.destructuring_pattern) :
             |> List_.flatten
         | None -> []
       in
-      let close = token env v3 (* "]" *) in
+      let close =
+        token env v3
+        (* "]" *)
+      in
       Arr (open_, elements, close)
 
 and variable_declaration (env : env)
     ((v1, v2, v3, v4) : CST.variable_declaration) : var list =
-  let v1 = (Var, token env v1) (* "var" *) in
+  let v1 =
+    (Var, token env v1)
+    (* "var" *)
+  in
   let vars = map_sep_list env v2 v3 variable_declarator in
   let _v4 = semicolon env v4 in
   build_vars v1 vars
@@ -778,7 +937,10 @@ and function_ (env : env) ((v1, v2, v3, v4, v5) : CST.function_) :
     | Some tok -> [ attr (Async, token env tok) ] (* "async" *)
     | None -> []
   in
-  let v2 = token env v2 (* "function" *) in
+  let v2 =
+    token env v2
+    (* "function" *)
+  in
   let v3 =
     match v3 with
     | Some tok -> Some (identifier env tok) (* identifier *)
@@ -799,7 +961,10 @@ and generic_type (env : env) ((v1, _v2) : CST.generic_type) : a_dotted_ident =
 
 and implements_clause (env : env) ((v1, v2, v3) : CST.implements_clause) :
     type_ list =
-  let _v1 = token env v1 (* "implements" *) in
+  let _v1 =
+    token env v1
+    (* "implements" *)
+  in
   let types = map_sep_list env v2 v3 type_ in
   types
 
@@ -811,8 +976,14 @@ and anon_choice_exp_9818c1b (env : env) (x : CST.anon_choice_exp_9818c1b) =
       Apply (IdSpecial (Spread, t), fb [ e ])
 
 and switch_default (env : env) ((v1, v2, v3) : CST.switch_default) =
-  let v1 = token env v1 (* "default" *) in
-  let v2 = token env v2 (* ":" *) in
+  let v1 =
+    token env v1
+    (* "default" *)
+  in
+  let v2 =
+    token env v2
+    (* ":" *)
+  in
   let v3 = List.concat_map (statement env) v3 in
   Default (v1, stmt1 v2 v3)
 
@@ -820,134 +991,215 @@ and binary_expression (env : env) (x : CST.binary_expression) : expr =
   match x with
   | `Exp_AMPAMP_exp (v1, v2, v3) ->
       let v1 = expression env v1 in
-      let v2 = token env v2 (* "&&" *) in
+      let v2 =
+        token env v2
+        (* "&&" *)
+      in
       let v3 = expression env v3 in
       Apply (IdSpecial (ArithOp G.And, v2), fb [ v1; v3 ])
   | `Exp_BARBAR_exp (v1, v2, v3) ->
       let v1 = expression env v1 in
-      let v2 = token env v2 (* "||" *) in
+      let v2 =
+        token env v2
+        (* "||" *)
+      in
       let v3 = expression env v3 in
       Apply (IdSpecial (ArithOp G.Or, v2), fb [ v1; v3 ])
   | `Exp_GTGT_exp (v1, v2, v3) ->
       let v1 = expression env v1 in
-      let v2 = token env v2 (* ">>" *) in
+      let v2 =
+        token env v2
+        (* ">>" *)
+      in
       let v3 = expression env v3 in
       Apply (IdSpecial (ArithOp G.LSR, v2), fb [ v1; v3 ])
   | `Exp_GTGTGT_exp (v1, v2, v3) ->
       let v1 = expression env v1 in
-      let v2 = token env v2 (* ">>>" *) in
+      let v2 =
+        token env v2
+        (* ">>>" *)
+      in
       let v3 = expression env v3 in
       Apply (IdSpecial (ArithOp G.ASR, v2), fb [ v1; v3 ])
   | `Exp_LTLT_exp (v1, v2, v3) ->
       let v1 = expression env v1 in
-      let v2 = token env v2 (* "<<" *) in
+      let v2 =
+        token env v2
+        (* "<<" *)
+      in
       let v3 = expression env v3 in
       Apply (IdSpecial (ArithOp G.LSL, v2), fb [ v1; v3 ])
   | `Exp_AMP_exp (v1, v2, v3) ->
       let v1 = expression env v1 in
-      let v2 = token env v2 (* "&" *) in
+      let v2 =
+        token env v2
+        (* "&" *)
+      in
       let v3 = expression env v3 in
       Apply (IdSpecial (ArithOp G.BitAnd, v2), fb [ v1; v3 ])
   | `Exp_HAT_exp (v1, v2, v3) ->
       let v1 = expression env v1 in
-      let v2 = token env v2 (* "^" *) in
+      let v2 =
+        token env v2
+        (* "^" *)
+      in
       let v3 = expression env v3 in
       Apply (IdSpecial (ArithOp G.BitXor, v2), fb [ v1; v3 ])
   | `Exp_BAR_exp (v1, v2, v3) ->
       let v1 = expression env v1 in
-      let v2 = token env v2 (* "|" *) in
+      let v2 =
+        token env v2
+        (* "|" *)
+      in
       let v3 = expression env v3 in
       Apply (IdSpecial (ArithOp G.BitOr, v2), fb [ v1; v3 ])
   | `Exp_PLUS_exp (v1, v2, v3) ->
       let v1 = expression env v1 in
-      let v2 = token env v2 (* "+" *) in
+      let v2 =
+        token env v2
+        (* "+" *)
+      in
       let v3 = expression env v3 in
       Apply (IdSpecial (ArithOp G.Plus, v2), fb [ v1; v3 ])
   | `Exp_DASH_exp (v1, v2, v3) ->
       let v1 = expression env v1 in
-      let v2 = token env v2 (* "-" *) in
+      let v2 =
+        token env v2
+        (* "-" *)
+      in
       let v3 = expression env v3 in
       Apply (IdSpecial (ArithOp G.Minus, v2), fb [ v1; v3 ])
   | `Exp_STAR_exp (v1, v2, v3) ->
       let v1 = expression env v1 in
-      let v2 = token env v2 (* "*" *) in
+      let v2 =
+        token env v2
+        (* "*" *)
+      in
       let v3 = expression env v3 in
       Apply (IdSpecial (ArithOp G.Mult, v2), fb [ v1; v3 ])
   | `Exp_SLASH_exp (v1, v2, v3) ->
       let v1 = expression env v1 in
-      let v2 = token env v2 (* "/" *) in
+      let v2 =
+        token env v2
+        (* "/" *)
+      in
       let v3 = expression env v3 in
       Apply (IdSpecial (ArithOp G.Div, v2), fb [ v1; v3 ])
   | `Exp_PERC_exp (v1, v2, v3) ->
       let v1 = expression env v1 in
-      let v2 = token env v2 (* "%" *) in
+      let v2 =
+        token env v2
+        (* "%" *)
+      in
       let v3 = expression env v3 in
       Apply (IdSpecial (ArithOp G.Mod, v2), fb [ v1; v3 ])
   | `Exp_STARSTAR_exp (v1, v2, v3) ->
       let v1 = expression env v1 in
-      let v2 = token env v2 (* "**" *) in
+      let v2 =
+        token env v2
+        (* "**" *)
+      in
       let v3 = expression env v3 in
       Apply (IdSpecial (ArithOp G.Pow, v2), fb [ v1; v3 ])
   | `Exp_LT_exp (v1, v2, v3) ->
       let v1 = expression env v1 in
-      let v2 = token env v2 (* "<" *) in
+      let v2 =
+        token env v2
+        (* "<" *)
+      in
       let v3 = expression env v3 in
       Apply (IdSpecial (ArithOp G.Lt, v2), fb [ v1; v3 ])
   | `Exp_LTEQ_exp (v1, v2, v3) ->
       let v1 = expression env v1 in
-      let v2 = token env v2 (* "<=" *) in
+      let v2 =
+        token env v2
+        (* "<=" *)
+      in
       let v3 = expression env v3 in
       Apply (IdSpecial (ArithOp G.LtE, v2), fb [ v1; v3 ])
   | `Exp_EQEQ_exp (v1, v2, v3) ->
       let v1 = expression env v1 in
-      let v2 = token env v2 (* "==" *) in
+      let v2 =
+        token env v2
+        (* "==" *)
+      in
       let v3 = expression env v3 in
       Apply (IdSpecial (ArithOp G.Eq, v2), fb [ v1; v3 ])
   | `Exp_EQEQEQ_exp (v1, v2, v3) ->
       let v1 = expression env v1 in
-      let v2 = token env v2 (* "===" *) in
+      let v2 =
+        token env v2
+        (* "===" *)
+      in
       let v3 = expression env v3 in
       Apply (IdSpecial (ArithOp G.PhysEq, v2), fb [ v1; v3 ])
   | `Exp_BANGEQ_exp (v1, v2, v3) ->
       let v1 = expression env v1 in
-      let v2 = token env v2 (* "!=" *) in
+      let v2 =
+        token env v2
+        (* "!=" *)
+      in
       let v3 = expression env v3 in
       Apply (IdSpecial (ArithOp G.NotEq, v2), fb [ v1; v3 ])
   | `Exp_BANGEQEQ_exp (v1, v2, v3) ->
       let v1 = expression env v1 in
-      let v2 = token env v2 (* "!==" *) in
+      let v2 =
+        token env v2
+        (* "!==" *)
+      in
       let v3 = expression env v3 in
       Apply (IdSpecial (ArithOp G.NotPhysEq, v2), fb [ v1; v3 ])
   | `Exp_GTEQ_exp (v1, v2, v3) ->
       let v1 = expression env v1 in
-      let v2 = token env v2 (* ">=" *) in
+      let v2 =
+        token env v2
+        (* ">=" *)
+      in
       let v3 = expression env v3 in
       Apply (IdSpecial (ArithOp G.GtE, v2), fb [ v1; v3 ])
   | `Exp_GT_exp (v1, v2, v3) ->
       let v1 = expression env v1 in
-      let v2 = token env v2 (* ">" *) in
+      let v2 =
+        token env v2
+        (* ">" *)
+      in
       let v3 = expression env v3 in
       Apply (IdSpecial (ArithOp G.Gt, v2), fb [ v1; v3 ])
   | `Exp_QMARKQMARK_exp (v1, v2, v3) ->
       let v1 = expression env v1 in
-      let v2 = token env v2 (* "??" *) in
+      let v2 =
+        token env v2
+        (* "??" *)
+      in
       let v3 = expression env v3 in
       Apply (IdSpecial (ArithOp G.Nullish, v2), fb [ v1; v3 ])
   | `Exp_inst_exp (v1, v2, v3) ->
       let v1 = expression env v1 in
-      let v2 = token env v2 (* "instanceof" *) in
+      let v2 =
+        token env v2
+        (* "instanceof" *)
+      in
       let v3 = expression env v3 in
       Apply (IdSpecial (Instanceof, v2), fb [ v1; v3 ])
   | `Exp_in_exp (v1, v2, v3) ->
       let v1 = expression env v1 in
-      let v2 = token env v2 (* "in" *) in
+      let v2 =
+        token env v2
+        (* "in" *)
+      in
       let v3 = expression env v3 in
       Apply (IdSpecial (In, v2), fb [ v1; v3 ])
 
 and arguments (env : env) ((v1, v2, v3) : CST.arguments) : a_arguments =
-  let v1 = token env v1 (* "(" *) in
+  let v1 =
+    token env v1
+    (* "(" *)
+  in
   let v2 = anon_opt_opt_choice_exp_rep_COMMA_opt_choice_exp_208ebb4 env v2 in
-  let v3 = token env v3 (* ")" *) in
+  let v3 =
+    token env v3
+    (* ")" *)
+  in
   (v1, v2, v3)
 
 and generator_function_declaration (env : env)
@@ -958,9 +1210,18 @@ and generator_function_declaration (env : env)
     | Some tok -> [ attr (Async, token env tok) ] (* "async" *)
     | None -> []
   in
-  let v2 = token env v2 (* "function" *) in
-  let v3 = [ attr (Generator, token env v3) ] (* "*" *) in
-  let v4 = identifier env v4 (* identifier *) in
+  let v2 =
+    token env v2
+    (* "function" *)
+  in
+  let v3 =
+    [ attr (Generator, token env v3) ]
+    (* "*" *)
+  in
+  let v4 =
+    identifier env v4
+    (* identifier *)
+  in
   let _tparams, (v5, tret) = call_signature env v5 in
   let v6 = statement_block env v6 in
   let _v7 = automatic_semicolon_opt env v7 in
@@ -985,13 +1246,19 @@ and variable_declarator (env : env) (x : CST.variable_declarator) =
       let id_or_pat = Left (identifier env v1 (* identifier *)) in
       (* definite assignment assertion
          TODO: add to AST? *)
-      let _v2 = token env v2 (* "!" *) in
+      let _v2 =
+        token env v2
+        (* "!" *)
+      in
       let type_ = type_annotation env v3 |> snd in
       (id_or_pat, Some type_, None)
 
 and sequence_expression (env : env) ((v1, v2, v3) : CST.sequence_expression) =
   let v1 = expression env v1 in
-  let v2 = token env v2 (* "," *) in
+  let v2 =
+    token env v2
+    (* "," *)
+  in
   let v3 =
     match v3 with
     | `Seq_exp x -> sequence_expression env x
@@ -1001,14 +1268,20 @@ and sequence_expression (env : env) ((v1, v2, v3) : CST.sequence_expression) =
 
 and type_arguments (env : env) ((v1, v2, v3, v4, v5) : CST.type_arguments) :
     type_ list bracket =
-  let v1 = token env v1 (* "<" *) in
+  let v1 =
+    token env v1
+    (* "<" *)
+  in
   let types = map_sep_list env v2 v3 type_ in
   let _v4 =
     match v4 with
     | Some tok -> Some (token env tok) (* "," *)
     | None -> None
   in
-  let v5 = token env v5 (* ">" *) in
+  let v5 =
+    token env v5
+    (* ">" *)
+  in
   (v1, types, v5)
 
 and add_decorators xs property =
@@ -1025,7 +1298,10 @@ and add_decorators xs property =
 (* TODO: types - class body can be just a signature. *)
 and class_body (env : env) ((v1, v2, v3) : CST.class_body) :
     property list bracket =
-  let v1 = token env v1 (* "{" *) in
+  let v1 =
+    token env v1
+    (* "{" *)
+  in
   let rec aux acc_decorators xs =
     match xs with
     | [] -> []
@@ -1085,12 +1361,18 @@ and class_body (env : env) ((v1, v2, v3) : CST.class_body) :
         )
   in
   let v2 = aux [] v2 in
-  let v3 = token env v3 (* "}" *) in
+  let v3 =
+    token env v3
+    (* "}" *)
+  in
   (v1, v2, v3)
 
 and type_parameter (env : env) ((v1, v2, v3) : CST.type_parameter) :
     a_type_parameter =
-  let v1 = str env v1 (* identifier *) in
+  let v1 =
+    str env v1
+    (* identifier *)
+  in
   let _v2 =
     match v2 with
     | Some x -> Some (constraint_ env x)
@@ -1120,14 +1402,20 @@ and member_expression (env : env) ((v1, v2, v3) : CST.member_expression) : expr
            all private properties in semgrep with e.g. #$VAR ? *)
         x
   in
-  let id = identifier env id_tok (* identifier *) in
+  let id =
+    identifier env id_tok
+    (* identifier *)
+  in
   ObjAccess (expr, dot, PN id)
 
 and object_property (env : env) (x : CST.anon_choice_pair_20c9acd) : property =
   match x with
   | `Pair (v1, v2, v3) ->
       let v1 = property_name env v1 in
-      let _v2 = token env v2 (* ":" *) in
+      let _v2 =
+        token env v2
+        (* ":" *)
+      in
       let v3 = expression env v3 in
       FieldColon
         { fld_name = v1; fld_attrs = []; fld_type = None; fld_body = Some v3 }
@@ -1154,14 +1442,23 @@ and subscript_expression (env : env)
     | None -> None
     | Some tok -> (* "?." *) Some (token env tok)
   in
-  let v3 = token env v3 (* "[" *) in
+  let v3 =
+    token env v3
+    (* "[" *)
+  in
   let v4 = expressions env v4 in
-  let v5 = token env v5 (* "]" *) in
+  let v5 =
+    token env v5
+    (* "]" *)
+  in
   (* TODO: distinguish optional chaining "?." from a simple access "." *)
   ArrAccess (expr, (v3, v4, v5))
 
 and initializer_ (env : env) ((v1, v2) : CST.initializer_) =
-  let _v1 = token env v1 (* "=" *) in
+  let _v1 =
+    token env v1
+    (* "=" *)
+  in
   let v2 = expression env v2 in
   v2
 
@@ -1187,7 +1484,10 @@ and primary_expression (env : env) (x : CST.primary_expression) : expr =
           | `This tok -> this env tok (* "this" *)
           | `Super tok -> super env tok (* "super" *)
           | `Num tok ->
-              let n = number env tok (* number *) in
+              let n =
+                number env tok
+                (* number *)
+              in
               L (Num n)
           | `Str x ->
               let s = string_ env x in
@@ -1196,9 +1496,18 @@ and primary_expression (env : env) (x : CST.primary_expression) : expr =
               let t1, xs, t2 = template_string env x in
               Apply (IdSpecial (Encaps false, t1), (t1, xs, t2))
           | `Regex (v1, v2, v3, v4) ->
-              let v1 = token env v1 (* "/" *) in
-              let v2 = str env v2 (* regex_pattern *) in
-              let v3 = token env v3 (* "/" *) in
+              let v1 =
+                token env v1
+                (* "/" *)
+              in
+              let v2 =
+                str env v2
+                (* regex_pattern *)
+              in
+              let v3 =
+                token env v3
+                (* "/" *)
+              in
               let v4 =
                 match v4 with
                 | Some tok -> Some (str env tok) (* pattern [a-z]+ *)
@@ -1231,7 +1540,10 @@ and primary_expression (env : env) (x : CST.primary_expression) : expr =
                     let _tparams, (params, tret) = call_signature env x in
                     (params, tret)
               in
-              let v3 = token env v3 (* "=>" *) in
+              let v3 =
+                token env v3
+                (* "=>" *)
+              in
               let v4 =
                 match v4 with
                 | `Exp x ->
@@ -1256,8 +1568,14 @@ and primary_expression (env : env) (x : CST.primary_expression) : expr =
                 | Some tok -> [ (Async, token env tok) ] (* "async" *)
                 | None -> []
               in
-              let v2 = token env v2 (* "function" *) in
-              let v3 = [ (Generator, token env v3) ] (* "*" *) in
+              let v2 =
+                token env v2
+                (* "function" *)
+              in
+              let v3 =
+                [ (Generator, token env v3) ]
+                (* "*" *)
+              in
               let v4 =
                 match v4 with
                 | Some tok -> Some (identifier env tok) (* identifier *)
@@ -1279,7 +1597,10 @@ and primary_expression (env : env) (x : CST.primary_expression) : expr =
               Fun (f, v4)
           | `Class (v1, v2, v3, v4, v5, v6) ->
               let v1 = List_.map (decorator env) v1 in
-              let v2 = token env v2 (* "class" *) in
+              let v2 =
+                token env v2
+                (* "class" *)
+              in
               let v3 =
                 match v3 with
                 | Some tok -> Some (identifier env tok) (* identifier *)
@@ -1308,9 +1629,18 @@ and primary_expression (env : env) (x : CST.primary_expression) : expr =
               in
               Class (class_, v3)
           | `Meta_prop (v1, v2, v3) ->
-              let v1 = token env v1 (* "new" *) in
-              let v2 = token env v2 (* "." *) in
-              let v3 = token env v3 (* "target" *) in
+              let v1 =
+                token env v1
+                (* "new" *)
+              in
+              let v2 =
+                token env v2
+                (* "." *)
+              in
+              let v3 =
+                token env v3
+                (* "target" *)
+              in
               let t = Tok.combine_toks v1 [ v2; v3 ] in
               IdSpecial (NewTarget, t)
           | `Call_exp x -> call_expression env x)
@@ -1338,7 +1668,10 @@ and call_expression (env : env) (x : CST.call_expression) =
       v3
   | `Prim_exp_QMARKDOT_opt_type_args_args (v1, v2, v3, v4) ->
       let v1 = primary_expression env v1 in
-      let _v2 = token env v2 (* "?." *) in
+      let _v2 =
+        token env v2
+        (* "?." *)
+      in
       (* TODO: types *)
       let _v3TODO =
         match v3 with
@@ -1376,7 +1709,10 @@ and module__ (env : env) ((v1, v2) : CST.module__) =
 
 and non_null_expression (env : env) ((v1, v2) : CST.non_null_expression) =
   let v1 = expression env v1 in
-  let v2 = token env v2 (* "!" *) in
+  let v2 =
+    token env v2
+    (* "!" *)
+  in
   let special = (ArithOp G.NotNullPostfix, v2) in
   Apply (IdSpecial special, fb [ v1 ])
 
@@ -1386,19 +1722,28 @@ and expression_statement (env : env) ((v1, v2) : CST.expression_statement) =
   (v1, v2)
 
 and catch_clause (env : env) ((v1, v2, v3) : CST.catch_clause) : catch =
-  let catch_tok = token env v1 (* "catch" *) in
+  let catch_tok =
+    token env v1
+    (* "catch" *)
+  in
   let stmts = statement_block env v3 in
   let catch =
     match v2 with
     | Some (v1, v2, v3, v4) ->
-        let _open = token env v1 (* "(" *) in
+        let _open =
+          token env v1
+          (* "(" *)
+        in
         let id_or_pat = id_or_destructuring_pattern env v2 in
         let type_ =
           match v3 with
           | Some x -> Some (type_annotation env x)
           | None -> None
         in
-        let _close = token env v4 (* ")" *) in
+        let _close =
+          token env v4
+          (* ")" *)
+        in
         let pat =
           match id_or_pat with
           | Left id -> idexp id
@@ -1460,32 +1805,39 @@ and object_type (env : env) ((v1, v2, v3) : CST.object_type) =
 
 and template_string (env : env) ((v1, v2, v3) : CST.template_string) :
     expr list bracket =
-  let v1 = token env v1 (* "`" *) in
+  let v1 =
+    token env v1
+    (* "`" *)
+  in
   (* Combine [`Temp_chars] and [`Esc_seq] sequences. *)
   let merge_str = function
     | [] -> assert false
     | `Temp_subs _ :: _ -> assert false
-    | [`Temp_chars tok | `Esc_seq tok] -> str env tok
+    | [ (`Temp_chars tok | `Esc_seq tok) ] -> str env tok
     | vs ->
-      let strs_toks =
-        List.map
-          (function
-            | (`Temp_chars tok | `Esc_seq tok) -> str env tok
-            | _ -> assert false)
-          vs in
-      let toks = List.map snd strs_toks in
-      let combined_toks = Tok.combine_toks (List.hd toks) (List.tl toks) in
-      let combined_str = String.concat "" (List.map fst strs_toks) in
-      combined_str, combined_toks
+        let strs_toks =
+          List.map
+            (function
+              | `Temp_chars tok
+              | `Esc_seq tok ->
+                  str env tok
+              | _ -> assert false)
+            vs
+        in
+        let toks = List.map snd strs_toks in
+        let combined_toks = Tok.combine_toks (List.hd toks) (List.tl toks) in
+        let combined_str = String.concat "" (List.map fst strs_toks) in
+        (combined_str, combined_toks)
   in
   let rec combine_str acc vs res =
-    match acc, vs with
+    match (acc, vs) with
     | [], [] -> List.rev res
-    | _, [] -> List.rev @@ (List.rev acc) :: res
-    | [], x :: xs -> combine_str [x] xs res
-    | (`Temp_chars _ | `Esc_seq _) :: _, (`Temp_chars _ | `Esc_seq _ as x) :: xs ->
-      combine_str (x :: acc) xs res
-    | _, x :: xs -> combine_str [x] xs ((List.rev acc) :: res)
+    | _, [] -> List.rev @@ (List.rev acc :: res)
+    | [], x :: xs -> combine_str [ x ] xs res
+    | ( (`Temp_chars _ | `Esc_seq _) :: _,
+        ((`Temp_chars _ | `Esc_seq _) as x) :: xs ) ->
+        combine_str (x :: acc) xs res
+    | _, x :: xs -> combine_str [ x ] xs (List.rev acc :: res)
   in
   let v2 =
     List_.map
@@ -1496,14 +1848,23 @@ and template_string (env : env) ((v1, v2, v3) : CST.template_string) :
         | _ -> assert false)
       (combine_str [] v2 [])
   in
-  let v3 = token env v3 (* "`" *) in
+  let v3 =
+    token env v3
+    (* "`" *)
+  in
   (v1, v2, v3)
 
 and template_substitution (env : env) ((v1, v2, v3) : CST.template_substitution)
     : expr =
-  let l = token env v1 (* "${" *) in
+  let l =
+    token env v1
+    (* "${" *)
+  in
   let e = expressions env v2 in
-  let r = token env v3 (* "}" *) in
+  let r =
+    token env v3
+    (* "}" *)
+  in
   ParenExpr (l, e, r)
 
 and map_template_literal_type (env : env)
@@ -1535,7 +1896,10 @@ and map_template_type (env : env) ((v1, v2, v3) : CST.template_type) :
   (l, ty, r)
 
 and decorator (env : env) ((v1, v2) : CST.decorator) : attribute =
-  let v1 = token env v1 (* "@" *) in
+  let v1 =
+    token env v1
+    (* "@" *)
+  in
   let ids, args_opt =
     match v2 with
     | `Id x ->
@@ -1551,7 +1915,10 @@ and decorator (env : env) ((v1, v2) : CST.decorator) : attribute =
   NamedAttr (v1, ids, args_opt)
 
 and internal_module (env : env) ((v1, v2) : CST.internal_module) =
-  let _v1 = token env v1 (* "namespace" *) in
+  let _v1 =
+    token env v1
+    (* "namespace" *)
+  in
   let v2 = module__ env v2 in
   v2
 
@@ -1570,7 +1937,10 @@ and anon_opt_opt_choice_exp_rep_COMMA_opt_choice_exp_208ebb4 (env : env)
 
 and for_header (env : env) ((v1, v2, v3, v4, v5) : CST.for_header) : for_header
     =
-  let _open = token env v1 (* "(" *) in
+  let _open =
+    token env v1
+    (* "(" *)
+  in
   let var_or_expr =
     match v2 with
     | `Choice_choice_choice_member_exp x ->
@@ -1602,7 +1972,10 @@ and for_header (env : env) ((v1, v2, v3, v4, v5) : CST.for_header) : for_header
     | `In tok -> (* "in" *) ForIn (var_or_expr, token env tok, exprs)
     | `Of tok -> (* "of" *) ForOf (var_or_expr, token env tok, exprs)
   in
-  let _close = token env v5 (* ")" *) in
+  let _close =
+    token env v5
+    (* ")" *)
+  in
   for_header
 
 and expr_or_prim_expr (env : env) (x : CST.anon_choice_exp_9cd0ed5) : expr =
@@ -1615,7 +1988,10 @@ and expression (env : env) (x : CST.expression) : expr =
   | `As_exp (v1, v2, v3) ->
       (* type assertion of the form 'exp as type' *)
       let e = expression env v1 in
-      let tas = token env v2 (* "as" *) in
+      let tas =
+        token env v2
+        (* "as" *)
+      in
       let ty =
         match v3 with
         | `Type x -> type_ env x
@@ -1624,7 +2000,10 @@ and expression (env : env) (x : CST.expression) : expr =
       TypeAssert (e, tas, ty)
   | `Satiss_exp (v1, v2, v3) ->
       let e = expression env v1 in
-      let tsat = token env v2 (* "satisfies" *) in
+      let tsat =
+        token env v2
+        (* "satisfies" *)
+      in
       let ty = type_ env v3 in
       TypeAssert (e, tsat, ty)
   | `Inte_module x -> (
@@ -1663,7 +2042,10 @@ and expression (env : env) (x : CST.expression) : expr =
       Xml xml
   | `Assign_exp (v1, v2, v3) ->
       let v1 = paren_expr_or_lhs_expr env v1 in
-      let v2 = token env v2 (* "=" *) in
+      let v2 =
+        token env v2
+        (* "=" *)
+      in
       let v3 = expression env v3 in
       Assign (v1, v2, v3)
   | `Augm_assign_exp (v1, v2, v3) ->
@@ -1677,7 +2059,10 @@ and expression (env : env) (x : CST.expression) : expr =
                 let id = reserved_identifier env x in
                 idexp id
             | `Id tok ->
-                let id = identifier env tok (* identifier *) in
+                let id =
+                  identifier env tok
+                  (* identifier *)
+                in
                 idexp id
             | `Paren_exp x -> parenthesized_expression env x)
         | `Non_null_exp x -> non_null_expression env x
@@ -1708,21 +2093,33 @@ and expression (env : env) (x : CST.expression) : expr =
       else
         Assign (lhs, tok, Apply (IdSpecial (ArithOp op, tok), fb [ lhs; rhs ]))
   | `Await_exp (v1, v2) ->
-      let v1 = token env v1 (* "await" *) in
+      let v1 =
+        token env v1
+        (* "await" *)
+      in
       let v2 = expression env v2 in
       Apply (IdSpecial (Await, v1), fb [ v2 ])
   | `Un_exp x -> unary_expression env x
   | `Bin_exp x -> binary_expression env x
   | `Tern_exp (v1, v2, v3, v4, v5) ->
       let v1 = expression env v1 in
-      let _v2 = token env v2 (* "?" *) in
+      let _v2 =
+        token env v2
+        (* "?" *)
+      in
       let v3 = expression env v3 in
-      let _v4 = token env v4 (* ":" *) in
+      let _v4 =
+        token env v4
+        (* ":" *)
+      in
       let v5 = expression env v5 in
       Conditional (v1, v3, v5)
   | `Update_exp x -> update_expression env x
   | `New_exp (v1, v2, v3, v4) ->
-      let v1 = token env v1 (* "new" *) in
+      let v1 =
+        token env v1
+        (* "new" *)
+      in
       let v2 = primary_expression env v2 in
       (* TODO types *)
       let _v3TODO =
@@ -1737,11 +2134,17 @@ and expression (env : env) (x : CST.expression) : expr =
       in
       New (v1, v2, (t1, xs, t2))
   | `Yield_exp (v1, v2) ->
-      let v1 = token env v1 (* "yield" *) in
+      let v1 =
+        token env v1
+        (* "yield" *)
+      in
       let v2 =
         match v2 with
         | `STAR_exp (v1bis, v2) ->
-            let _v1bis = token env v1bis (* "*" *) in
+            let _v1bis =
+              token env v1bis
+              (* "*" *)
+            in
             let v2 = expression env v2 in
             Apply (IdSpecial (YieldStar, v1), fb [ v2 ])
         | `Opt_exp opt -> (
@@ -1764,7 +2167,10 @@ and paren_expr_or_lhs_expr (env : env)
 and primary_type (env : env) (x : CST.primary_type) : type_ =
   match x with
   | `Union_type (v1, v2, v3) -> (
-      let v2 = token env v2 (* "|" *) in
+      let v2 =
+        token env v2
+        (* "|" *)
+      in
       let v3 = type_ env v3 in
       match v1 with
       | Some x ->
@@ -1773,7 +2179,10 @@ and primary_type (env : env) (x : CST.primary_type) : type_ =
       | None -> v3
       (* ?? *))
   | `Inte_type (v1, v2, v3) -> (
-      let v2 = token env v2 (* "&" *) in
+      let v2 =
+        token env v2
+        (* "&" *)
+      in
       let v3 = type_ env v3 in
       match v1 with
       | Some x ->
@@ -1783,16 +2192,25 @@ and primary_type (env : env) (x : CST.primary_type) : type_ =
       (* ?? *))
   | `Temp_lit_type x -> map_template_literal_type env x
   | `Paren_type (v1, v2, v3) ->
-      let _v1 = token env v1 (* "(" *) in
+      let _v1 =
+        token env v1
+        (* "(" *)
+      in
       let v2 = type_ env v2 in
-      let _v3 = token env v3 (* ")" *) in
+      let _v3 =
+        token env v3
+        (* ")" *)
+      in
       v2
   | `Pred_type x ->
       let id = predefined_type env x in
       (* less: could also be a G.TyBuiltin *)
       TyName [ id ]
   | `Id tok ->
-      let id = identifier env tok (* identifier *) in
+      let id =
+        identifier env tok
+        (* identifier *)
+      in
       TyName [ id ]
   | `Nested_type_id x ->
       let xs = nested_type_identifier env x in
@@ -1810,26 +2228,47 @@ and primary_type (env : env) (x : CST.primary_type) : type_ =
       TyRecordAnon (t1, xs, t2)
   | `Array_type (v1, v2, v3) ->
       let type_ = primary_type env v1 in
-      let open_ = token env v2 (* "[" *) in
-      let close = token env v3 (* "]" *) in
+      let open_ =
+        token env v2
+        (* "[" *)
+      in
+      let close =
+        token env v3
+        (* "]" *)
+      in
       TyArray (type_, (open_, (), close))
   | `Tuple_type (v1, v2, v3, v4) ->
-      let open_ = token env v1 (* "[" *) in
+      let open_ =
+        token env v1
+        (* "[" *)
+      in
       let members =
         match v2 with
         | None -> []
         | Some (v1, v2) -> map_sep_list env v1 v2 tuple_type_member
       in
-      let _trailing_comma = optional env v3 token (* "," *) in
-      let close = token env v4 (* "]" *) in
+      let _trailing_comma =
+        optional env v3 token
+        (* "," *)
+      in
+      let close =
+        token env v4
+        (* "]" *)
+      in
       TyTuple (open_, members, close)
   | `Flow_maybe_type (v1, v2) ->
-      let v1 = token env v1 (* "?" *) in
+      let v1 =
+        token env v1
+        (* "?" *)
+      in
       let v2 = primary_type env v2 in
       TyQuestion (v1, v2)
   | `Type_query x -> type_query env x
   | `Index_type_query (v1, v2) ->
-      let keyof = token env v1 (* "keyof" *) in
+      let keyof =
+        token env v1
+        (* "keyof" *)
+      in
       let type_ = primary_type env v2 in
       TypeTodo (("KeyOf", keyof), [ Type type_ ])
   | `This tok ->
@@ -1837,24 +2276,42 @@ and primary_type (env : env) (x : CST.primary_type) : type_ =
       (* "this" *)
       TypeTodo (("This", v1), [])
   | `Exis_type tok ->
-      let v1 = token env tok (* "*" *) in
+      let v1 =
+        token env tok
+        (* "*" *)
+      in
       TypeTodo (("*", v1), [])
   | `Lit_type x ->
       let v1 = literal_type env x in
       TypeTodo (("LitType", fake), [ Expr v1 ])
   | `Lookup_type (v1, v2, v3, v4) ->
       let v1 = primary_type env v1 in
-      let v2 = token env v2 (* "[" *) in
+      let v2 =
+        token env v2
+        (* "[" *)
+      in
       let v3 = type_ env v3 in
-      let _v4 = token env v4 (* "]" *) in
+      let _v4 =
+        token env v4
+        (* "]" *)
+      in
       TypeTodo (("LookupType", v2), [ Type v1; Type v3 ])
   | `Cond_type (v1, v2, v3, v4, v5, v6, v7) ->
       let v1 = type_ env v1 in
-      let _v2 = token env v2 (* "extends" *) in
+      let _v2 =
+        token env v2
+        (* "extends" *)
+      in
       let v3 = type_ env v3 in
-      let v4 = token env v4 (* "?" *) in
+      let v4 =
+        token env v4
+        (* "?" *)
+      in
       let v5 = type_ env v5 in
-      let _v6 = token env v6 (* ":" *) in
+      let _v6 =
+        token env v6
+        (* ":" *)
+      in
       let v7 = type_ env v7 in
       TypeTodo (("ConditionalType", v4), [ Type v1; Type v3; Type v5; Type v7 ])
 
@@ -1867,31 +2324,49 @@ and index_signature (env : env) ((v1, v2, v3, v4, v5) : CST.index_signature) =
           | Some tok -> Some (token env tok) (* "-" *)
           | None -> None
         in
-        let v2 = token env v2 (* "readonly" *) in
+        let v2 =
+          token env v2
+          (* "readonly" *)
+        in
         Some (v1, v2)
         (* TODO add to AST *)
     | None -> None
   in
-  let v2 = token env v2 (* "[" *) in
+  let v2 =
+    token env v2
+    (* "[" *)
+  in
   let v3 =
     match v3 with
     | `Choice_id_COLON_type (v2, v3, v4) ->
         let v2 = id_or_reserved_id env v2 in
-        let v3 = token env v3 (* ":" *) in
+        let v3 =
+          token env v3
+          (* ":" *)
+        in
         let v4 = type_ env v4 in
         TypeTodo (("IndexKey", v3), [ Type (TyName [ v2 ]); Type v4 ])
     | `Mapped_type_clause x -> mapped_type_clause env x
   in
-  let _v4 = token env v4 (* "]" *) in
+  let _v4 =
+    token env v4
+    (* "]" *)
+  in
   let v5 =
     match v5 with
     | `Type_anno x -> type_annotation env x |> snd
     | `Omit_type_anno (v1, v2) ->
-        let _v1_TODO = token env v1 (* "-?:" *) in
+        let _v1_TODO =
+          token env v1
+          (* "-?:" *)
+        in
         let v2 = type_ env v2 in
         v2
     | `Opting_type_anno (v1, v2) ->
-        let _v1_TODO = token env v1 (* "?:" *) in
+        let _v1_TODO =
+          token env v1
+          (* "?:" *)
+        in
         let v2 = type_ env v2 in
         v2
   in
@@ -1974,25 +2449,46 @@ and unary_expression (env : env) ((v1, v2) : CST.unary_expression) =
   let v2 = expression env v2 in
   match v1 with
   | `BANG v1 ->
-      let v1 = token env v1 (* "!" *) in
+      let v1 =
+        token env v1
+        (* "!" *)
+      in
       Apply (IdSpecial (ArithOp G.Not, v1), fb [ v2 ])
   | `TILDE v1 ->
-      let v1 = token env v1 (* "~" *) in
+      let v1 =
+        token env v1
+        (* "~" *)
+      in
       Apply (IdSpecial (ArithOp G.BitNot, v1), fb [ v2 ])
   | `DASH v1 ->
-      let v1 = token env v1 (* "-" *) in
+      let v1 =
+        token env v1
+        (* "-" *)
+      in
       Apply (IdSpecial (ArithOp G.Minus, v1), fb [ v2 ])
   | `PLUS v1 ->
-      let v1 = token env v1 (* "+" *) in
+      let v1 =
+        token env v1
+        (* "+" *)
+      in
       Apply (IdSpecial (ArithOp G.Plus, v1), fb [ v2 ])
   | `Typeof v1 ->
-      let v1 = token env v1 (* "typeof" *) in
+      let v1 =
+        token env v1
+        (* "typeof" *)
+      in
       Apply (IdSpecial (Typeof, v1), fb [ v2 ])
   | `Void v1 ->
-      let v1 = token env v1 (* "void" *) in
+      let v1 =
+        token env v1
+        (* "void" *)
+      in
       Apply (IdSpecial (Void, v1), fb [ v2 ])
   | `Delete v1 ->
-      let v1 = token env v1 (* "delete" *) in
+      let v1 =
+        token env v1
+        (* "delete" *)
+      in
       Apply (IdSpecial (Delete, v1), fb [ v2 ])
 
 and pat_or_assign_pat (env : env) (x : CST.anon_choice_pat_3297d92) :
@@ -2001,7 +2497,10 @@ and pat_or_assign_pat (env : env) (x : CST.anon_choice_pat_3297d92) :
   | `Pat x -> pattern env x
   | `Assign_pat (v1, v2, v3) ->
       let pat = pattern env v1 in
-      let _eq = token env v2 (* "=" *) in
+      let _eq =
+        token env v2
+        (* "=" *)
+      in
       let _default = expression env v3 in
       pat
 
@@ -2036,7 +2535,10 @@ and formal_parameter (env : env) (x : CST.formal_parameter) : parameter =
   | `Opt_param (v1, v2, v3, v4) ->
       (* optional_parameter *)
       let id_or_pat = parameter_name env v1 in
-      let _questionmark_TODO = token env v2 (* "?" *) in
+      let _questionmark_TODO =
+        token env v2
+        (* "?" *)
+      in
       let opt_type =
         match v3 with
         | Some x -> Some (type_annotation env x |> snd)
@@ -2050,26 +2552,41 @@ and formal_parameter (env : env) (x : CST.formal_parameter) : parameter =
 
 and formal_parameters (env : env) ((v1, v2, v3) : CST.formal_parameters) :
     parameter list bracket =
-  let open_ = token env v1 (* "(" *) in
+  let open_ =
+    token env v1
+    (* "(" *)
+  in
   let params =
     match v2 with
     | Some (v1, v2, v3) ->
         let params = map_sep_list env v1 v2 formal_parameter in
-        let _trailing_comma = optional env v3 token (* "," *) in
+        let _trailing_comma =
+          optional env v3 token
+          (* "," *)
+        in
         params
     | None -> []
   in
-  let close = token env v3 (* ")" *) in
+  let close =
+    token env v3
+    (* ")" *)
+  in
   (open_, params, close)
 
 (* class Component<Props = any, State = any> { ... *)
 and default_type (env : env) ((v1, v2) : CST.default_type) =
-  let _v1 = token env v1 (* "=" *) in
+  let _v1 =
+    token env v1
+    (* "=" *)
+  in
   let v2 = type_ env v2 in
   v2
 
 and switch_body (env : env) ((v1, v2, v3) : CST.switch_body) =
-  let _v1 = token env v1 (* "{" *) in
+  let _v1 =
+    token env v1
+    (* "{" *)
+  in
   let v2 =
     List_.map
       (fun x ->
@@ -2078,12 +2595,21 @@ and switch_body (env : env) ((v1, v2, v3) : CST.switch_body) =
         | `Switch_defa x -> switch_default env x)
       v2
   in
-  let _v3 = token env v3 (* "}" *) in
+  let _v3 =
+    token env v3
+    (* "}" *)
+  in
   v2
 
 and mapped_type_clause (env : env) ((v1, v2, v3, v4) : CST.mapped_type_clause) =
-  let id = str env v1 (* identifier *) in
-  let tin = token env v2 (* "in" *) in
+  let id =
+    str env v1
+    (* identifier *)
+  in
+  let tin =
+    token env v2
+    (* "in" *)
+  in
   let ty = type_ env v3 in
   let asopt =
     match v4 with
@@ -2104,7 +2630,10 @@ and statement (env : env) (x : CST.statement) : stmt list =
       let xs = export_statement env x in
       xs
   | `Import_stmt (v1, v2, v3, v4) ->
-      let v1 = token env v1 (* "import" *) in
+      let v1 =
+        token env v1
+        (* "import" *)
+      in
       let import_tok = v1 in
       let _v2 =
         match v2 with
@@ -2125,7 +2654,10 @@ and statement (env : env) (x : CST.statement) : stmt list =
       let _v4 = semicolon env v4 in
       v3 |> List_.map (fun m -> M m)
   | `Debu_stmt (v1, v2) ->
-      let v1 = identifier env v1 (* "debugger" *) in
+      let v1 =
+        identifier env v1
+        (* "debugger" *)
+      in
       let v2 = semicolon env v2 in
       [ ExprStmt (idexp v1, v2) ]
   | `Exp_stmt x -> (
@@ -2144,26 +2676,41 @@ and statement (env : env) (x : CST.statement) : stmt list =
       vars |> List_.map (fun x -> DefStmt x)
   | `Stmt_blk x -> [ statement_block env x ]
   | `If_stmt (v1, v2, v3, v4) ->
-      let v1 = token env v1 (* "if" *) in
+      let v1 =
+        token env v1
+        (* "if" *)
+      in
       let v2 = parenthesized_expression env v2 in
       let v3 = statement1 env v3 in
       let v4 =
         match v4 with
         | Some (v1, v2) ->
-            let _v1 = token env v1 (* "else" *) in
+            let _v1 =
+              token env v1
+              (* "else" *)
+            in
             let v2 = statement1 env v2 in
             Some v2
         | None -> None
       in
       [ If (v1, v2, v3, v4) ]
   | `Switch_stmt (v1, v2, v3) ->
-      let v1 = token env v1 (* "switch" *) in
+      let v1 =
+        token env v1
+        (* "switch" *)
+      in
       let v2 = parenthesized_expression env v2 in
       let v3 = switch_body env v3 in
       [ Switch (v1, v2, v3) ]
   | `For_stmt (v1, v2, v3, v4, v5, v6, v7) ->
-      let v1 = token env v1 (* "for" *) in
-      let _v2 = token env v2 (* "(" *) in
+      let v1 =
+        token env v1
+        (* "for" *)
+      in
+      let _v2 =
+        token env v2
+        (* "(" *)
+      in
       let v3 =
         match v3 with
         | `Lexi_decl x ->
@@ -2176,7 +2723,10 @@ and statement (env : env) (x : CST.statement) : stmt list =
             let e, _t = expression_statement env x in
             Right e
         | `Empty_stmt tok ->
-            let _x = token env tok (* ";" *) in
+            let _x =
+              token env tok
+              (* ";" *)
+            in
             Left []
       in
       let v4 =
@@ -2185,7 +2735,10 @@ and statement (env : env) (x : CST.statement) : stmt list =
             let e, _t = expression_statement env x in
             Some e
         | `Empty_stmt tok ->
-            let _x = token env tok (* ";" *) in
+            let _x =
+              token env tok
+              (* ";" *)
+            in
             None
       in
       let v5 =
@@ -2193,11 +2746,17 @@ and statement (env : env) (x : CST.statement) : stmt list =
         | Some x -> Some (expressions env x)
         | None -> None
       in
-      let _v6 = token env v6 (* ")" *) in
+      let _v6 =
+        token env v6
+        (* ")" *)
+      in
       let v7 = statement1 env v7 in
       [ For (v1, ForClassic (v3, v4, v5), v7) ]
   | `For_in_stmt (v1, v2, v3, v4) ->
-      let v1 = token env v1 (* "for" *) in
+      let v1 =
+        token env v1
+        (* "for" *)
+      in
       let _v2TODO =
         match v2 with
         | Some tok -> Some (token env tok) (* "await" *)
@@ -2207,19 +2766,31 @@ and statement (env : env) (x : CST.statement) : stmt list =
       let v4 = statement1 env v4 in
       [ For (v1, v3, v4) ]
   | `While_stmt (v1, v2, v3) ->
-      let v1 = token env v1 (* "while" *) in
+      let v1 =
+        token env v1
+        (* "while" *)
+      in
       let v2 = parenthesized_expression env v2 in
       let v3 = statement1 env v3 in
       [ While (v1, v2, v3) ]
   | `Do_stmt (v1, v2, v3, v4, v5) ->
-      let v1 = token env v1 (* "do" *) in
+      let v1 =
+        token env v1
+        (* "do" *)
+      in
       let v2 = statement1 env v2 in
-      let _v3 = token env v3 (* "while" *) in
+      let _v3 =
+        token env v3
+        (* "while" *)
+      in
       let v4 = parenthesized_expression env v4 in
       let _v5 = semicolon env v5 in
       [ Do (v1, v2, v4) ]
   | `Try_stmt (v1, v2, v3, v4) ->
-      let v1 = token env v1 (* "try" *) in
+      let v1 =
+        token env v1
+        (* "try" *)
+      in
       let v2 = statement_block env v2 in
       let v3 =
         match v3 with
@@ -2233,12 +2804,18 @@ and statement (env : env) (x : CST.statement) : stmt list =
       in
       [ Try (v1, v2, v3, v4) ]
   | `With_stmt (v1, v2, v3) ->
-      let v1 = token env v1 (* "with" *) in
+      let v1 =
+        token env v1
+        (* "with" *)
+      in
       let v2 = parenthesized_expression env v2 in
       let v3 = statement1 env v3 in
       [ With (v1, v2, v3) ]
   | `Brk_stmt (v1, v2, v3) ->
-      let v1 = token env v1 (* "break" *) in
+      let v1 =
+        token env v1
+        (* "break" *)
+      in
       let v2 =
         match v2 with
         | Some tok -> Some (identifier env tok) (* identifier *)
@@ -2247,7 +2824,10 @@ and statement (env : env) (x : CST.statement) : stmt list =
       let v3 = semicolon env v3 in
       [ Break (v1, v2, v3) ]
   | `Cont_stmt (v1, v2, v3) ->
-      let v1 = token env v1 (* "continue" *) in
+      let v1 =
+        token env v1
+        (* "continue" *)
+      in
       let v2 =
         match v2 with
         | Some tok -> Some (identifier env tok) (* identifier *)
@@ -2256,7 +2836,10 @@ and statement (env : env) (x : CST.statement) : stmt list =
       let v3 = semicolon env v3 in
       [ Continue (v1, v2, v3) ]
   | `Ret_stmt (v1, v2, v3) ->
-      let v1 = token env v1 (* "return" *) in
+      let v1 =
+        token env v1
+        (* "return" *)
+      in
       let v2 =
         match v2 with
         | Some x -> Some (expressions env x)
@@ -2265,14 +2848,20 @@ and statement (env : env) (x : CST.statement) : stmt list =
       let v3 = semicolon env v3 in
       [ Return (v1, v2, v3) ]
   | `Throw_stmt (v1, v2, v3) ->
-      let v1 = token env v1 (* "throw" *) in
+      let v1 =
+        token env v1
+        (* "throw" *)
+      in
       let v2 = expressions env v2 in
       let v3 = semicolon env v3 in
       [ Throw (v1, v2, v3) ]
   | `Empty_stmt tok -> [ empty_stmt env tok (* ";" *) ]
   | `Labe_stmt (v1, v2, v3) ->
       let v1 = id_or_reserved_id env v1 in
-      let _v2 = token env v2 (* ":" *) in
+      let _v2 =
+        token env v2
+        (* ":" *)
+      in
       let v3 = statement1 env v3 in
       [ Label (v1, v3) ]
 
@@ -2310,8 +2899,14 @@ and method_definition (env : env)
 and class_declaration (env : env)
     ((v1, v2, v3, v4, v5, v6, v7) : CST.class_declaration) : definition =
   let v1 = List_.map (decorator env) v1 in
-  let v2 = token env v2 (* "class" *) in
-  let v3 = identifier env v3 (* identifier *) in
+  let v2 =
+    token env v2
+    (* "class" *)
+  in
+  let v3 =
+    identifier env v3
+    (* identifier *)
+  in
   (* TODO types: type_parameters *)
   let _v4 =
     match v4 with
@@ -2337,9 +2932,15 @@ and class_declaration (env : env)
   (basic_entity v3, ClassDef c)
 
 and array_ (env : env) ((v1, v2, v3) : CST.array_) =
-  let v1 = token env v1 (* "[" *) in
+  let v1 =
+    token env v1
+    (* "[" *)
+  in
   let v2 = anon_opt_opt_choice_exp_rep_COMMA_opt_choice_exp_208ebb4 env v2 in
-  let v3 = token env v3 (* "]" *) in
+  let v3 =
+    token env v3
+    (* "]" *)
+  in
   Arr (v1, v2, v3)
 
 (* 'export { a, b as c } from "mod"': desugar each specifier into an import of
@@ -2373,20 +2974,35 @@ and export_statement (env : env) (x : CST.export_statement) : stmt list =
   | `Choice_export_choice_STAR_from_clause_choice_auto_semi x -> (
       match x with
       | `Export_choice_STAR_from_clause_choice_auto_semi (v1, v2) ->
-          let export_tok = token env v1 (* "export" *) in
+          let export_tok =
+            token env v1
+            (* "export" *)
+          in
           let v2 =
             match v2 with
             | `STAR_from_clause_choice_auto_semi (v1, v2, v3) ->
                 (* export * from 'foo'; *)
-                let star = token env v1 (* "*" *) in
+                let star =
+                  token env v1
+                  (* "*" *)
+                in
                 let from, path = from_clause env v2 in
                 let _v3 = semicolon env v3 in
                 [ M (ReExportNamespace (export_tok, star, None, from, path)) ]
             | `Name_import_export_from_clause_choice_auto_semi (v1, v2, v3) ->
                 (* export * as foo from "module"; *)
-                let star, alias = namespace_import env v1 (* * as foo *) in
-                let from, path = from_clause env v2 (* from "module" *) in
-                let _semi = semicolon env v3 (* ; *) in
+                let star, alias =
+                  namespace_import env v1
+                  (* * as foo *)
+                in
+                let from, path =
+                  from_clause env v2
+                  (* from "module" *)
+                in
+                let _semi =
+                  semicolon env v3
+                  (* ; *)
+                in
                 [
                   M
                     (ReExportNamespace (export_tok, star, Some alias, from, path));
@@ -2406,7 +3022,10 @@ and export_statement (env : env) (x : CST.export_statement) : stmt list =
           v2
       | `Rep_deco_export_choice_decl (v1, v2, v3) ->
           let decorators = List_.map (decorator env) v1 in
-          let export_tok = token env v2 (* "export" *) in
+          let export_tok =
+            token env v2
+            (* "export" *)
+          in
           let v3 =
             match v3 with
             | `Decl x ->
@@ -2418,7 +3037,10 @@ and export_statement (env : env) (x : CST.export_statement) : stmt list =
                        let ent = { ent with attrs = ent.attrs @ decorators } in
                        [ DefStmt (ent, defkind); M (Export (export_tok, n)) ])
             | `Defa_choice_decl (v1, v2) -> (
-                let tok_default (* TODO *) = token env v1 (* "default" *) in
+                let tok_default (* TODO *) =
+                  token env v1
+                  (* "default" *)
+                in
                 match v2 with
                 | `Decl x ->
                     let defs = declaration env x in
@@ -2489,7 +3111,10 @@ and export_statement (env : env) (x : CST.export_statement) : stmt list =
       [ M (Export (export_tok, id)) ]
 
 and type_annotation (env : env) ((v1, v2) : CST.type_annotation) =
-  let v1 = token env v1 (* ":" *) in
+  let v1 =
+    token env v1
+    (* ":" *)
+  in
   let v2 = type_ env v2 in
   (v1, v2)
 
@@ -2497,7 +3122,10 @@ and anon_rep_COMMA_opt_choice_exp_ca698a5 (env : env)
     (xs : CST.anon_rep_COMMA_opt_choice_exp_ca698a5) =
   List_.filter_map
     (fun (v1, v2) ->
-      let _v1 = token env v1 (* "," *) in
+      let _v1 =
+        token env v1
+        (* "," *)
+      in
       let v2 =
         match v2 with
         | Some x -> Some (anon_choice_exp_9818c1b env x)
@@ -2563,7 +3191,10 @@ and anon_choice_export_stmt_f90d83f (env : env)
       Left (Field fld)
   | `Cons_sign (v0, v1, v2, v3, v4) ->
       let v0 = kwd_attr_opt_to_list env Abstract v0 in
-      let v1 = token env v1 (* "new" *) in
+      let v1 =
+        token env v1
+        (* "new" *)
+      in
       let _tparams =
         match v2 with
         | Some x -> type_parameters env x
@@ -2671,7 +3302,10 @@ and map_extends_clause (env : env) ((v1, v2, v3, v4) : CST.extends_clause) :
   tyname_or_expr_of_expr v2 v3 :: v4
 
 and enum_body (env : env) ((v1, v2, v3) : CST.enum_body) =
-  let v1 = token env v1 (* "{" *) in
+  let v1 =
+    token env v1
+    (* "{" *)
+  in
   let body =
     match v2 with
     | Some (v1, v2, v3) ->
@@ -2684,7 +3318,10 @@ and enum_body (env : env) ((v1, v2, v3) : CST.enum_body) =
         body
     | None -> []
   in
-  let v3 = token env v3 (* "}" *) in
+  let v3 =
+    token env v3
+    (* "}" *)
+  in
   (v1, body, v3)
 
 and class_heritage (env : env) (x : CST.class_heritage) :
@@ -2714,23 +3351,41 @@ and property_name (env : env) (x : CST.property_name) =
       let s = string_ env x in
       PN s
   | `Num tok ->
-      let n = number_as_string env tok (* number *) in
+      let n =
+        number_as_string env tok
+        (* number *)
+      in
       PN n
   | `Comp_prop_name (v1, v2, v3) ->
-      let _v1 = token env v1 (* "[" *) in
+      let _v1 =
+        token env v1
+        (* "[" *)
+      in
       let v2 = expression env v2 in
-      let _v3 = token env v3 (* "]" *) in
+      let _v3 =
+        token env v3
+        (* "]" *)
+      in
       PN_Computed v2
 
 and switch_case (env : env) ((v1, v2, v3, v4) : CST.switch_case) =
-  let v1 = token env v1 (* "case" *) in
+  let v1 =
+    token env v1
+    (* "case" *)
+  in
   let v2 = expressions env v2 in
-  let v3 = token env v3 (* ":" *) in
+  let v3 =
+    token env v3
+    (* ":" *)
+  in
   let v4 = List.concat_map (statement env) v4 in
   Case (v1, v2, stmt1 v3 v4)
 
 and spread_element (env : env) ((v1, v2) : CST.spread_element) =
-  let v1 = token env v1 (* "..." *) in
+  let v1 =
+    token env v1
+    (* "..." *)
+  in
   let v2 = expression env v2 in
   (v1, v2)
 
@@ -2742,7 +3397,10 @@ and expressions (env : env) (x : CST.expressions) : expr =
 and abstract_method_signature (env : env)
     ((v1, v2, v3, v4, v5, v6) : CST.abstract_method_signature) =
   let v1 = accessibility_modifier_opt_to_list env v1 in
-  let v2 = [ (Abstract, token env v2) ] (* "abstract" *) in
+  let v2 =
+    [ (Abstract, token env v2) ]
+    (* "abstract" *)
+  in
   let v3 =
     match v3 with
     | Some x -> [ anon_choice_get_8fb02de env x ]
@@ -2760,7 +3418,10 @@ and abstract_method_signature (env : env)
   { fld_name = v4; fld_attrs = attrs; fld_type = Some t; fld_body = None }
 
 and finally_clause (env : env) ((v1, v2) : CST.finally_clause) =
-  let v1 = token env v1 (* "finally" *) in
+  let v1 =
+    token env v1
+    (* "finally" *)
+  in
   let v2 = statement_block env v2 in
   (v1, v2)
 
@@ -2768,25 +3429,40 @@ and map_type_predicate (env : env) ((v1, v2, v3) : CST.type_predicate) =
   let v1 =
     match v1 with
     | `Id tok ->
-        let id = identifier env tok (* identifier *) in
+        let id =
+          identifier env tok
+          (* identifier *)
+        in
         idexp_or_special id
     | `This tok -> this env tok
     | `Pred_type v1 ->
         let id = predefined_type env v1 in
         idexp_or_special id
   in
-  let tis = token env v2 (* "is" *) in
+  let tis =
+    token env v2
+    (* "is" *)
+  in
   let ty = type_ env v3 in
   TypeTodo (("IsType", tis), [ Expr v1; Type ty ])
 
 and map_asserts (env : env) ((v1, v2, v3) : CST.asserts) : type_ =
-  let tcolon = token env v1 (* ":" *) in
-  let _asserts = token env v2 (* "asserts" *) in
+  let tcolon =
+    token env v1
+    (* ":" *)
+  in
+  let _asserts =
+    token env v2
+    (* "asserts" *)
+  in
   let any =
     match v3 with
     | `Type_pred x -> Type (map_type_predicate env x)
     | `Id tok ->
-        let id = identifier env tok (* identifier *) in
+        let id =
+          identifier env tok
+          (* identifier *)
+        in
         Expr (idexp_or_special id)
     | `This tok -> Expr (this env tok)
   in
@@ -2809,7 +3485,10 @@ and call_signature (env : env) ((v1, v2, v3) : CST.call_signature) :
             let ty = map_asserts env x in
             Some ty
         | `Type_pred_anno (v1, v2) ->
-            let _v1 = token env v1 (* ":" *) in
+            let _v1 =
+              token env v1
+              (* ":" *)
+            in
             let v2 = map_type_predicate env v2 in
             Some v2)
     | None -> None
@@ -2817,7 +3496,10 @@ and call_signature (env : env) ((v1, v2, v3) : CST.call_signature) :
   (v1, (v2, v3))
 
 and object_ (env : env) ((v1, v2, v3) : CST.object_) : a_obj =
-  let v1 = token env v1 (* "{" *) in
+  let v1 =
+    token env v1
+    (* "{" *)
+  in
   let properties =
     match v2 with
     | Some (v1, v2) ->
@@ -2828,7 +3510,10 @@ and object_ (env : env) ((v1, v2, v3) : CST.object_) : a_obj =
         |> List_.flatten
     | None -> []
   in
-  let v3 = token env v3 (* "}" *) in
+  let v3 =
+    token env v3
+    (* "}" *)
+  in
   (v1, properties, v3)
 
 and type_predicate (env : env) ((v1, v2, v3) : CST.type_predicate) : type_ =
@@ -2838,7 +3523,10 @@ and type_predicate (env : env) ((v1, v2, v3) : CST.type_predicate) : type_ =
     | `This tok -> (* "this" *) this env tok
     | `Pred_type v1 -> predefined_type env v1 |> idexp
   in
-  let _is = token env v2 (* "is" *) in
+  let _is =
+    token env v2
+    (* "is" *)
+  in
   let type_ = type_ env v3 in
   type_
 
@@ -2852,7 +3540,10 @@ and type_ (env : env) (x : CST.type_) : type_ =
         | None -> []
       in
       let params = formal_parameters env v2 in
-      let _arrow = token env v3 (* "=>" *) in
+      let _arrow =
+        token env v3
+        (* "=>" *)
+      in
       let type_ =
         match v4 with
         | `Type x -> type_ env x
@@ -2862,39 +3553,60 @@ and type_ (env : env) (x : CST.type_) : type_ =
       in
       mk_functype (params, Some type_)
   | `Read_type (v1, v2) ->
-      let _TODOreadonly = token env v1 (* "readonly" *) in
+      let _TODOreadonly =
+        token env v1
+        (* "readonly" *)
+      in
       let type_ = type_ env v2 in
       type_
   | `Cons_type (v0, v1, v2, v3, v4, v5) ->
       let _v0TODO = kwd_attr_opt_to_list env Abstract v0 in
-      let v1 = token env v1 (* "new" *) in
+      let v1 =
+        token env v1
+        (* "new" *)
+      in
       let _tparams =
         match v2 with
         | Some x -> type_parameters env x
         | None -> []
       in
       let v3 = formal_parameters env v3 in
-      let _v4 = token env v4 (* "=>" *) in
+      let _v4 =
+        token env v4
+        (* "=>" *)
+      in
       let v5 = type_ env v5 in
       let ty = mk_functype (v3, Some v5) in
       TypeTodo (("New", v1), [ Type ty ])
   | `Infer_type x -> map_infer_type env x
 
 and map_infer_type env (v1, v2) =
-  let v1 = token env v1 (* "infer" *) in
-  let v2 = identifier env v2 (* identifier *) in
+  let v1 =
+    token env v1
+    (* "infer" *)
+  in
+  let v2 =
+    identifier env v2
+    (* identifier *)
+  in
   TypeTodo (("Infer", v1), [ Type (TyName [ v2 ]) ])
 
 and type_parameters (env : env) ((v1, v2, v3, v4, v5) : CST.type_parameters) :
     a_type_parameter list =
-  let _v1 = token env v1 (* "<" *) in
+  let _v1 =
+    token env v1
+    (* "<" *)
+  in
   let type_params = map_sep_list env v2 v3 type_parameter in
   let _v4 =
     match v4 with
     | Some tok -> Some (token env tok) (* "," *)
     | None -> None
   in
-  let _v5 = token env v5 (* ">" *) in
+  let _v5 =
+    token env v5
+    (* ">" *)
+  in
   type_params
 
 and constraint_ (env : env) ((v1, v2) : CST.constraint_) :
@@ -2928,7 +3640,10 @@ and parameter_name (env : env) ((v1, v2, v2bis, v3, v4) : CST.parameter_name) :
     | `Pat x -> pattern env x
     | `This tok ->
         (* treating 'this' as a regular identifier for now *)
-        let id = identifier env tok (* "this" *) in
+        let id =
+          identifier env tok
+          (* "this" *)
+        in
         Left id
   in
   id_or_pat
@@ -2947,9 +3662,15 @@ and lhs_expression (env : env) (x : CST.lhs_expression) : expr =
   | `Non_null_exp x -> non_null_expression env x
 
 and statement_block (env : env) ((v1, v2, v3, v4) : CST.statement_block) =
-  let v1 = token env v1 (* "{" *) in
+  let v1 =
+    token env v1
+    (* "{" *)
+  in
   let v2 = List.concat_map (statement env) v2 in
-  let v3 = token env v3 (* "}" *) in
+  let v3 =
+    token env v3
+    (* "}" *)
+  in
   let _v4 = automatic_semicolon_opt env v4 in
   Block (v1, v2, v3)
 
@@ -2960,8 +3681,14 @@ and function_declaration (env : env)
     | Some tok -> [ attr (Async, token env tok) ] (* "async" *)
     | None -> []
   in
-  let v2 = token env v2 (* "function" *) in
-  let v3 = identifier env v3 (* identifier *) in
+  let v2 =
+    token env v2
+    (* "function" *)
+  in
+  let v3 =
+    identifier env v3
+    (* identifier *)
+  in
   let _tparams, (v4, tret) = call_signature env v4 in
   let v5 = statement_block env v5 in
   let _v6 = automatic_semicolon_opt env v6 in
@@ -2983,7 +3710,10 @@ and id_or_destructuring_pattern env x : (a_ident, a_pattern) Either.t =
 (* TODO: change type signature, return just a_pattern *)
 and rest_pattern (env : env) ((v1, v2) : CST.rest_pattern) :
     tok * (a_ident, a_pattern) Either.t =
-  let dots = token env v1 (* "..." *) in
+  let dots =
+    token env v1
+    (* "..." *)
+  in
   let e = lhs_expression env v2 in
   (dots, Right e)
 
@@ -3004,15 +3734,24 @@ and tuple_type_member (env : env) (x : CST.tuple_type_member) :
   | `Opt_tuple_param (v1, v2, v3) ->
       (* optional_tuple_parameter *)
       let _v1_TODO = identifier env v1 in
-      let _v2_TODO = token env v2 (* "?" *) in
+      let _v2_TODO =
+        token env v2
+        (* "?" *)
+      in
       let v3 = type_annotation env v3 |> snd in
       TyTupMember v3
   | `Opt_type (v1, v2) ->
       let v1 = type_ env v1 in
-      let v2 = token env v2 (* "?" *) in
+      let v2 =
+        token env v2
+        (* "?" *)
+      in
       TyTupOpt (v1, v2)
   | `Rest_type (v1, v2) ->
-      let v1 = token env v1 (* "..." *) in
+      let v1 =
+        token env v1
+        (* "..." *)
+      in
       let v2 = type_ env v2 in
       TyTupRest (v1, v2)
   | `Type x -> TyTupMember (type_ env x)
@@ -3054,8 +3793,14 @@ and declaration (env : env) (x : CST.declaration) : definition list =
         | Some tok -> [ (Async, token env tok) ] (* "async" *)
         | None -> []
       in
-      let v2 = token env v2 (* "function" *) in
-      let v3 = identifier env v3 (* identifier *) in
+      let v2 =
+        token env v2
+        (* "function" *)
+      in
+      let v3 =
+        identifier env v3
+        (* identifier *)
+      in
       let _tparams, x = call_signature env v4 in
       let ty = mk_functype x in
       let _v5 =
@@ -3070,9 +3815,18 @@ and declaration (env : env) (x : CST.declaration) : definition list =
       ]
   | `Abst_class_decl (v1, v2, v3, v4, v5, v6, v7) ->
       let _v1_TODO = List_.map (decorator env) v1 in
-      let v2 = attr (Abstract, token env v2) (* "abstract" *) in
-      let v3 = token env v3 (* "class" *) in
-      let v4 = identifier env v4 (* identifier *) in
+      let v2 =
+        attr (Abstract, token env v2)
+        (* "abstract" *)
+      in
+      let v3 =
+        token env v3
+        (* "class" *)
+      in
+      let v4 =
+        identifier env v4
+        (* identifier *)
+      in
       let _tparams =
         match v5 with
         | Some x -> type_parameters env x
@@ -3105,14 +3859,23 @@ and declaration (env : env) (x : CST.declaration) : definition list =
       let id, opt_body = internal_module env x in
       [ (basic_entity id, ModuleDef opt_body) ]
   | `Type_alias_decl (v1, v2, v3, v4, v5, v6) ->
-      let typekwd = token env v1 (* "type" *) in
-      let id = str env v2 (* identifier *) in
+      let typekwd =
+        token env v1
+        (* "type" *)
+      in
+      let id =
+        str env v2
+        (* identifier *)
+      in
       let _tparamsTODO =
         match v3 with
         | Some x -> type_parameters env x
         | None -> []
       in
-      let _teq = token env v4 (* "=" *) in
+      let _teq =
+        token env v4
+        (* "=" *)
+      in
       let ty = type_ env v5 in
       let sc = semicolon env v6 in
       let ent = basic_entity id in
@@ -3123,14 +3886,26 @@ and declaration (env : env) (x : CST.declaration) : definition list =
         | Some tok -> [ token env tok ] (* "const" *)
         | None -> []
       in
-      let _v2 = token env v2 (* "enum" *) in
-      let _v3 = identifier env v3 (* identifier *) in
+      let _v2 =
+        token env v2
+        (* "enum" *)
+      in
+      let _v3 =
+        identifier env v3
+        (* identifier *)
+      in
       let _v4 = enum_body env v4 in
       []
       (* TODO *)
   | `Inte_decl (v1, v2, v3, v4, v5) ->
-      let v1 = token env v1 (* "interface" *) in
-      let v2 = identifier env v2 (* identifier *) in
+      let v1 =
+        token env v1
+        (* "interface" *)
+      in
+      let v2 =
+        identifier env v2
+        (* identifier *)
+      in
       let _v3 =
         match v3 with
         | Some x -> type_parameters env x
@@ -3159,20 +3934,35 @@ and declaration (env : env) (x : CST.declaration) : definition list =
       in
       [ (basic_entity v2, ClassDef c) ]
   | `Import_alias (v1, v2, v3, v4, v5) ->
-      let _v1 = token env v1 (* "import" *) in
-      let _v2 = identifier env v2 (* identifier *) in
-      let _v3 = token env v3 (* "=" *) in
+      let _v1 =
+        token env v1
+        (* "import" *)
+      in
+      let _v2 =
+        identifier env v2
+        (* identifier *)
+      in
+      let _v3 =
+        token env v3
+        (* "=" *)
+      in
       let _v4 = id_or_nested_id env v4 in
       let _v5 = semicolon env v5 in
       []
       (* TODO *)
   | `Ambi_decl (v1, v2) ->
-      let _v1 = token env v1 (* "declare" *) in
+      let _v1 =
+        token env v1
+        (* "declare" *)
+      in
       let v2 =
         match v2 with
         | `Decl x -> declaration env x
         | `Global_stmt_blk (v1, v2) ->
-            let v1 = token env v1 (* "global" *) in
+            let v1 =
+              token env v1
+              (* "global" *)
+            in
             let v2 = statement_block env v2 in
             let name = ("!global!", v1) in
             let f_kind = (G.LambdaKind, fake) in
@@ -3196,10 +3986,22 @@ and declaration (env : env) (x : CST.declaration) : definition list =
                   } );
             ]
         | `Module_DOT_id_COLON_type_choice_auto_semi (v1, v2, v3, v4, v5, v6) ->
-            let tok_module = token env v1 (* "module" *) in
-            let _dot = token env v2 (* "." *) in
-            let name = identifier env v3 (* identifier *) in
-            let _colon = token env v4 (* ":" *) in
+            let tok_module =
+              token env v1
+              (* "module" *)
+            in
+            let _dot =
+              token env v2
+              (* "." *)
+            in
+            let name =
+              identifier env v3
+              (* identifier *)
+            in
+            let _colon =
+              token env v4
+              (* ":" *)
+            in
             let type_ = type_ env v5 in
             let _semi = semicolon env v6 in
             (* TODO: DefTodo *)
@@ -3262,7 +4064,8 @@ let guess_dialect opt_dialect file : dialect =
   | None ->
       (* TODO: should remove the no_sem below, bug in ml_to_generic.ml *)
       if file =~ ".*\\.tsx" then (* nosem *)
-        `TSX else `Typescript
+        `TSX
+      else `Typescript
 
 type cst_result =
   ( CST_tree_sitter_typescript.program,
@@ -3296,7 +4099,7 @@ let parse_pattern str =
   H.wrap_parser
     (* TODO Should we use Tree_sitter_tsx so that we permit TSX constructs in
      * patterns? Or try both, since TSX is not strictly a superset? *)
-      (fun () -> (Tree_sitter_typescript.Parse.string str :> cst_result))
+    (fun () -> (Tree_sitter_typescript.Parse.string str :> cst_result))
     (fun cst _extras ->
       let file = Fpath.v "<pattern>" in
       let env = { H.file; conv = H.line_col_to_pos_pattern str; extra = () } in
diff --git a/libs/ast_generic/AST_generic_helpers.ml b/libs/ast_generic/AST_generic_helpers.ml
index ae04d83..192ade8 100644
--- a/libs/ast_generic/AST_generic_helpers.ml
+++ b/libs/ast_generic/AST_generic_helpers.ml
@@ -250,7 +250,7 @@ let rec expr_to_pattern e =
    * reference into a [PatId] binding, which Naming_AST later resolves. *)
   | Record (t1, fields, t2) ->
       let pat_fields =
-        List.filter_map
+        List.map
           (fun f ->
             match f with
             | F
@@ -266,7 +266,9 @@ let rec expr_to_pattern e =
             | _ -> None)
           fields
       in
-      PatRecord (t1, pat_fields, t2)
+      if List.exists Option.is_none pat_fields then
+        OtherPat (("ExprToPattern", t1), [ E e ])
+      else PatRecord (t1, List.filter_map Fun.id pat_fields, t2)
   | Constructor (n, (_, args, _)) ->
       PatConstructor (n, args |> List_.map expr_to_pattern)
   | Ellipsis t -> PatEllipsis t
diff --git a/src/analyzing/Implicit_return.ml b/src/analyzing/Implicit_return.ml
index 9188f85..820c610 100644
--- a/src/analyzing/Implicit_return.ml
+++ b/src/analyzing/Implicit_return.ml
@@ -76,8 +76,13 @@ let rec mark_first_instr_ancestor (cfg : IL.cfg) i =
 
 let lang_supports_implicit_return (lang : Lang.t) =
   match lang with
+  | Groovy
+  | Perl
   | Clojure
   | Crystal
+  | Ocaml
+  | Lisp
+  | Scheme
   | Elixir
   | Ruby
   | Rust
diff --git a/src/analyzing/Visit_function_defs.ml b/src/analyzing/Visit_function_defs.ml
index 94d0755..15b3041 100644
--- a/src/analyzing/Visit_function_defs.ml
+++ b/src/analyzing/Visit_function_defs.ml
@@ -194,7 +194,7 @@ let g_name_to_il_name (g_name : G.name) : IL.name option =
   match g_name with
   | G.Id ((str, tok), id_info) ->
       let id_info = { id_info with G.id_resolved = ref None } in
-      Some IL.{ ident = (str, tok); sid = G.SId.unsafe_default; id_info }
+      Some IL.{ ident = (str, tok); sid = G.SId.unsafe_default; id_info; value_origin = None }
   | _ -> None
 
 (* Convert G.entity to IL.name for fn_id path construction. *)
@@ -212,8 +212,7 @@ let synth_lambda_il_name_of_tok (tok : Tok.t) : IL.name =
   IL.{
     ident = ("_tmp_lambda", fake_tok);
     sid = G.SId.unsafe_default;
-    id_info = G.empty_id_info ();
-  }
+    id_info = G.empty_id_info (); value_origin = None }
 
 let synth_lambda_il_name (fdef : G.function_definition) : IL.name =
   synth_lambda_il_name_of_tok (snd fdef.fkind)
diff --git a/src/naming/Naming_AST.ml b/src/naming/Naming_AST.ml
index 0050973..2feacbb 100644
--- a/src/naming/Naming_AST.ml
+++ b/src/naming/Naming_AST.ml
@@ -611,6 +611,7 @@ let assign_implicitly_declares lang =
   || lang =*= Lang.Python
   || lang =*= Lang.Ruby
   || lang =*= Lang.Crystal
+  || lang =*= Lang.Powershell
   || Lang.is_js lang
 
 (*****************************************************************************)
@@ -809,6 +810,11 @@ class ['self] resolve_visitor env lang =
            *
            *     https://github.com/semgrep/semgrep/issues/2787.
            *)
+          | Lang.Powershell ->
+              let sid = SId.mk () in
+              let resolved = untyped_ent (resolved_name_kind env lang, sid) in
+              add_ident_current_scope id resolved env.names;
+              set_resolved env id_info resolved
           | Lang.Js
           | Lang.Ts ->
               let sid = SId.mk () in
@@ -893,6 +899,12 @@ class ['self] resolve_visitor env lang =
       | ImportFrom (_, FileName (s, tok), imported_names) ->
           List.iter
             (function
+              | (name, _), Some (alias, id_info)
+                when Lang.is_js lang && name = Ast_js.default_entity ->
+                  let sid = SId.mk () in
+                  let resolved = untyped_ent (ImportedModule [s], sid) in
+                  set_resolved env id_info resolved;
+                  add_ident_imported_scope alias resolved env.names
               | id, None
                 when Lang.is_js lang && fst id <> Ast_js.default_entity ->
                   (* for JS we consider import { x } from 'a/b/foo' as foo.x.
@@ -1272,6 +1284,9 @@ class ['self] resolve_visitor env lang =
       (* | Block (_, stmts, _) when has_block_scope lang ->
              with_new_block_scope env.names (fun () ->
                  List.iter (fun stmt -> self#visit_stmt venv stmt) stmts) *)
+      | Block (_, stmts, _) when List.mem lang [Lang.Lisp; Lang.Scheme; Lang.Ocaml; Lang.Cairo; Lang.Groovy; Lang.Perl] ->
+          with_new_block_scope env.names (fun () ->
+              List.iter (fun stmt -> self#visit_stmt venv stmt) stmts)
       | _else_ -> super#visit_stmt venv x
   end
   
@@ -1284,5 +1299,5 @@ let resolve lang prog =
   let visitor = new resolve_visitor env lang
   in
   visitor#visit_program () prog;
-  ()
+  if Lang.is_js lang then Javascript_bindings.resolve prog else prog
 [@@profiling]
diff --git a/src/naming/Naming_AST.mli b/src/naming/Naming_AST.mli
index 957c408..d334def 100644
--- a/src/naming/Naming_AST.mli
+++ b/src/naming/Naming_AST.mli
@@ -1,8 +1,8 @@
-(* Works by side effect on the generic AST by modifying its refs.
+(* Resolves binding refs and returns the normalized generic AST.
  * We pass the lang because some name resolution algorithm may be
  * specific to a language.
  *)
-val resolve : Lang.t -> AST_generic.program -> unit
+val resolve : Lang.t -> AST_generic.program -> AST_generic.program
 
 val pro_hook_normalize_ast_generic_type :
   (Lang.t -> AST_generic.type_ -> AST_generic.type_) option ref
diff --git a/src/naming/Test_naming_generic.ml b/src/naming/Test_naming_generic.ml
index 4acb7d3..c88506d 100644
--- a/src/naming/Test_naming_generic.ml
+++ b/src/naming/Test_naming_generic.ml
@@ -3,7 +3,7 @@ module H = AST_generic_helpers
 let test_naming_generic ~parse_program file =
   let ast = parse_program file in
   let lang = Lang.lang_of_filename_exn file in
-  Naming_AST.resolve lang ast;
+  let ast = Naming_AST.resolve lang ast in
   let s = AST_generic.show_any (AST_generic.Pr ast) in
   UCommon.pr2 s
 
diff --git a/src/naming/Unit_naming_generic.ml b/src/naming/Unit_naming_generic.ml
index 85e7e48..2eac1a9 100644
--- a/src/naming/Unit_naming_generic.ml
+++ b/src/naming/Unit_naming_generic.ml
@@ -31,7 +31,7 @@ let tests parse_program =
                       into infinite loops *)
                    let ast = parse_program file in
                    let lang = Lang.lang_of_filename_exn file in
-                   Naming_AST.resolve lang ast;
+                   let ast = Naming_AST.resolve lang ast in
                    (* this used to loop forever if you were not handling correctly
                       possible cycles with id_type *)
                    let _v = AST_generic.show_any (AST_generic.Pr ast) in
diff --git a/src/naming/Unit_typing_generic.ml b/src/naming/Unit_typing_generic.ml
index 80293cb..fbd6d7a 100644
--- a/src/naming/Unit_typing_generic.ml
+++ b/src/naming/Unit_typing_generic.ml
@@ -20,7 +20,7 @@ let tests parse_program parse_pattern =
           try
             let ast = parse_program file in
             let lang = Lang.lang_of_filename_exn file in
-            Naming_AST.resolve lang ast;
+            let ast = Naming_AST.resolve lang ast in
 
             let v =
               object (_self : 'self)
@@ -47,7 +47,7 @@ let tests parse_program parse_pattern =
           try
             let ast = parse_program file in
             let lang = Lang.lang_of_filename_exn file in
-            Naming_AST.resolve lang ast;
+            let ast = Naming_AST.resolve lang ast in
 
             let v =
               object (_self : 'self)
@@ -95,7 +95,7 @@ let tests parse_program parse_pattern =
           try
             let ast = parse_program file in
             let lang = Lang.lang_of_filename_exn file in
-            Naming_AST.resolve lang ast;
+            let ast = Naming_AST.resolve lang ast in
 
             let v =
               object (_self : 'self)
@@ -135,7 +135,7 @@ let tests parse_program parse_pattern =
           try
             let ast = parse_program file in
             let lang = Lang.lang_of_filename_exn file in
-            Naming_AST.resolve lang ast;
+            let ast = Naming_AST.resolve lang ast in
 
             let v =
               object (_self : 'self)
@@ -196,7 +196,7 @@ let tests parse_program parse_pattern =
           try
             let ast = parse_program file in
             let lang = Lang.lang_of_filename_exn file in
-            Naming_AST.resolve lang ast;
+            let ast = Naming_AST.resolve lang ast in
 
             let v =
               object (_self : 'self)
@@ -223,7 +223,7 @@ let tests parse_program parse_pattern =
           try
             let ast = parse_program file in
             let lang = Lang.lang_of_filename_exn file in
-            Naming_AST.resolve lang ast;
+            let ast = Naming_AST.resolve lang ast in
 
             let v =
               object (_self : 'self)
@@ -270,7 +270,7 @@ let tests parse_program parse_pattern =
           try
             let ast = parse_program file in
             let lang = Lang.lang_of_filename_exn file in
-            Naming_AST.resolve lang ast;
+            let ast = Naming_AST.resolve lang ast in
 
             let v =
               object (_self : 'self)
diff --git a/src/parsing_languages/Parse_pattern2.ml b/src/parsing_languages/Parse_pattern2.ml
index a945de2..810bdec 100644
--- a/src/parsing_languages/Parse_pattern2.ml
+++ b/src/parsing_languages/Parse_pattern2.ml
@@ -142,6 +142,12 @@ let parse_pattern options lang str =
       in
       let res = parse_pattern str in
       extract_pattern_from_tree_sitter_result res
+  | Lang.Groovy ->
+      extract_pattern_from_tree_sitter_result (Parse_groovy_native.parse_pattern str)
+  | Lang.Perl ->
+      extract_pattern_from_tree_sitter_result (Parse_perl_native.parse_pattern str)
+  | Lang.Powershell ->
+      extract_pattern_from_tree_sitter_result (Parse_powershell_native.parse_pattern str)
   | Lang.Cairo ->
       let res = Parse_cairo_tree_sitter.parse_pattern str in
       extract_pattern_from_tree_sitter_result res
@@ -164,7 +170,11 @@ let parse_pattern options lang str =
   | Lang.Lisp
   | Lang.Scheme
   | Lang.Clojure ->
-      let res = Parse_clojure_tree_sitter.parse_pattern str in
+      let dialect = match lang with
+        | Lang.Lisp -> Parse_clojure_tree_sitter.CommonLisp
+        | Lang.Scheme -> Parse_clojure_tree_sitter.Scheme
+        | _ -> Parse_clojure_tree_sitter.Clojure in
+      let res = Parse_clojure_tree_sitter.parse_pattern ~dialect str in
       extract_pattern_from_tree_sitter_result res
   | Lang.Lua ->
       let res = Parse_lua_tree_sitter.parse_pattern str in
diff --git a/src/parsing_languages/Parse_target2.ml b/src/parsing_languages/Parse_target2.ml
index d4fefbf..c490d81 100644
--- a/src/parsing_languages/Parse_target2.ml
+++ b/src/parsing_languages/Parse_target2.ml
@@ -167,6 +167,12 @@ let just_parse_with_lang lang file : Parsing_result2.t =
    * tree-sitter, and there's no ast_xxx.ml either so we directly generate
    * a generic AST (no calls to an xxx_to_generic() below)
    *)
+  | Lang.Groovy ->
+      run file [ TreeSitter Parse_groovy_native.parse ] (fun x -> x)
+  | Lang.Perl ->
+      run file [ TreeSitter Parse_perl_native.parse ] (fun x -> x)
+  | Lang.Powershell ->
+      run file [ TreeSitter Parse_powershell_native.parse ] (fun x -> x)
   | Lang.Cairo ->
       run file [ TreeSitter Parse_cairo_tree_sitter.parse ] (fun x -> x)
   | Lang.Crystal ->
@@ -194,7 +200,11 @@ let just_parse_with_lang lang file : Parsing_result2.t =
   | Lang.Lisp
   | Lang.Scheme
   | Lang.Clojure ->
-      run file [ TreeSitter Parse_clojure_tree_sitter.parse ] (fun x -> x)
+      let dialect = match lang with
+        | Lang.Lisp -> Parse_clojure_tree_sitter.CommonLisp
+        | Lang.Scheme -> Parse_clojure_tree_sitter.Scheme
+        | _ -> Parse_clojure_tree_sitter.Clojure in
+      run file [ TreeSitter (Parse_clojure_tree_sitter.parse ~dialect) ] (fun x -> x)
   | Lang.Lua -> run file [ TreeSitter Parse_lua_tree_sitter.parse ] (fun x -> x)
   | Lang.Promql ->
       run file [ TreeSitter Parse_promql_tree_sitter.parse ] (fun x -> x)
diff --git a/src/parsing_languages/dune b/src/parsing_languages/dune
index e91da08..68fd38e 100644
--- a/src/parsing_languages/dune
+++ b/src/parsing_languages/dune
@@ -9,6 +9,7 @@
  (wrapped false)
  (libraries
    commons
+   semgrep.native_groovy semgrep.native_powershell semgrep.native_perl
    semgrep.parsing
 
    ; has an xxx.ast, xxx.menhir, xxx.tree_sitter, xxx.generic
diff --git a/src/targeting/Guess_lang.ml b/src/targeting/Guess_lang.ml
index 7fe436a..7c71a26 100644
--- a/src/targeting/Guess_lang.ml
+++ b/src/targeting/Guess_lang.ml
@@ -206,7 +206,8 @@ let is_executable_script cmd_names =
 *)
 let matches_lang lang =
   let has_ext =
-    And (has_lang_extension lang, Not (has_excluded_lang_extension lang))
+    Or (is_named (Lang.filenames_of_lang lang),
+        And (has_lang_extension lang, Not (has_excluded_lang_extension lang)))
   in
   match Lang.shebangs_of_lang lang with
   | [] -> has_ext
@@ -283,6 +284,9 @@ let inspect_file_p (lang : Lang.t) path =
     | Vb
     | Vue
     | Xml
+    | Groovy
+    | Powershell
+    | Perl
     | Yaml ->
         matches_lang lang
   in
